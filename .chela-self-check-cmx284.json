{
  "experiments": [
    {
      "guard": "WIRING: block_body must hand a SURVIVED rework the NEW catalog location — `docs/defeat_shapes/`, one file per shape. This is the production instruction that makes the split actually happen; if it still says 'add an entry' to `docs/DEFEAT_SHAPES.md`, agents keep appending numbered sections to the monolith (the exact thing test_defeat_shapes_index_carries_no_numbered_sections_of_its_own now forbids) and the collision this PR exists to close comes straight back. Reverts item 4 to its pre-PR wording. test_block_body_points_the_rework_agent_at_the_defeat_shapes_catalog asserts only `'docs/DEFEAT_SHAPES.md' in body`, which the reverted text still satisfies.",
      "file": "chela/judge.py",
      "before": "        \"4. Check `docs/DEFEAT_SHAPES.md` (see `docs/defeat_shapes/` for the catalog itself) \"\n        \"— if the shape above (what the guard asserted vs. what the mutation actually got \"\n        \"away with) isn't already catalogued there, add ONE NEW FILE to `docs/defeat_shapes/` \"\n        \"as part of this fix. This checkout is thrown away when the judge finishes, so it \"\n        \"cannot land the entry itself; the agent doing the rework is the one with a branch \"\n        \"to put it on.\",\n",
      "after": "        \"4. Check `docs/DEFEAT_SHAPES.md` — if the shape above (what the guard asserted vs. \"\n        \"what the mutation actually got away with) isn't already catalogued there, add an \"\n        \"entry as part of this fix. This checkout is thrown away when the judge finishes, so \"\n        \"it cannot land the entry itself; the agent doing the rework is the one with a \"\n        \"branch to put it on.\",\n",
      "kind": "wiring"
    },
    {
      "guard": "WIRING: the rework brief (REWORK_PROMPT step 2) must tell the reworking agent to add ONE NEW FILE under `docs/defeat_shapes/`. The reworking agent is the only actor with a branch to land a new shape on, so this string is the production wiring for 'growth is a new file, not an append'. Reverts step 2 to its pre-PR wording ('add an entry' to the monolith). test_rework_prompt_points_at_the_defeat_shapes_catalog asserts only `'docs/DEFEAT_SHAPES.md' in prompt`, still true after the revert.",
      "file": "chela/dispatcher.py",
      "before": "2. Fix every defect it names, in this worktree. If a guard SURVIVED corruption, check\n   `docs/DEFEAT_SHAPES.md` (the catalog itself lives one file per shape under\n   `docs/defeat_shapes/`) — it catalogs known ways a guard looks like it works but doesn't\n   (dead-coded statements, fixtures parked on a default, source-constant checks instead of\n   the rendered value…). If the shape the verdict just found isn't catalogued yet, add ONE\n   NEW FILE to `docs/defeat_shapes/` as part of this fix — it is the only durable place that\n   knowledge can land; the judge's own checkout is thrown away when it finishes and cannot\n   commit it itself.\n",
      "after": "2. Fix every defect it names, in this worktree. If a guard SURVIVED corruption, check\n   `docs/DEFEAT_SHAPES.md` — it catalogs known ways a guard looks like it works but doesn't\n   (dead-coded statements, fixtures parked on a default, source-constant checks instead of\n   the rendered value…). If the shape the verdict just found isn't listed there yet, add an\n   entry as part of this fix — it is the only durable place that knowledge can land; the\n   judge's own checkout is thrown away when it finishes and cannot commit it itself.\n",
      "kind": "wiring"
    },
    {
      "guard": "NEW: test_defeat_shapes_file_headings_are_well_formed_and_match_their_filename — a shape file's H2 heading must match '## N. Title' (WITH the period) and N must match the filename's leading number. A heading that loses its period is invisible to the catalog scan in test_defeat_shapes_catalog_documents_every_seeded_shape (re.split(r\"^## \\\\d+\\\\. \", ...) drops the whole file silently, and a sequential-numbering assertion can't see the gap either since the malformed heading was never counted as a section). This orphaned three shapes on cmx-279's own hand-resolved merge conflict.",
      "file": "docs/defeat_shapes/21-a-shared-field-value-tested-on-the-sibling-that-motivated.md",
      "before": "## 21. A shared field value tested on the sibling that motivated it, not on the one that already had it",
      "after": "## 21 A shared field value tested on the sibling that motivated it, not on the one that already had it"
    },
    {
      "guard": "NEW: test_defeat_shapes_cross_references_resolve_to_shapes_that_exist — every 'shape N' / '[[N|...]]' cross-reference inside docs/defeat_shapes/*.md must resolve to a shape file that actually exists. Under the old single-file catalog, renumbering to resolve a merge conflict could silently orphan a reference with no mechanical way to catch it; one file per shape makes the existing-shape set concrete (the filenames), so this is now checkable.",
      "file": "docs/defeat_shapes/08-a-differential-guard-that-cancels.md",
      "before": "shape 6",
      "after": "shape 66"
    }
  ]
}
