# TODO

## Open — interactive UI (phone approvals)

- [x] **Interactive UI Slice B — ExitPlanMode plan-approval buttons (reuses Slice A plumbing).** When an agent calls `ExitPlanMode`, the bridge already relays the plan text to its topic (`ExitPlanMode` ∈ `INTERACTIVE_TOOL_NAMES`, `chela/telegram/relay.py:47`). Attach an **inline keyboard** so the human can approve / keep-planning from the phone. Detection = transcript (structured `tool_use`; plan in `input.plan`). ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read the numbered items 1–6 + Landmines + Verify beneath this line — they carry the load-bearing constraints (the KEY difference from Slice A, the option-count-independent keystrokes, the VALIDATE-empirically step, and what to report). Do not implement from this summary line alone.**
  1. **KEY difference from Slice A (read first):** unlike AskUserQuestion, ExitPlanMode's approval CHOICES are **NOT in the transcript** — `input` carries only `plan`, not the "Yes / Yes-auto / No, keep planning" options (those are harness-rendered TUI). So do **NOT** enumerate choices from the transcript, and do **NOT** introduce general pane-scraping (that's deferred to Slice C). Use **option-count-independent** keystrokes.
  2. **Keyboard (two semantic buttons + the always-present nav row):** `✅ Approve plan` → sends **`Enter`** (accept the default-highlighted proceed option); `📝 Keep planning` → sends **`Escape`** (dismiss the plan-exit, stay in plan mode). Both are single keystrokes, independent of how many Yes-variants exist. **Reuse Slice A's existing `qa:nav:*` plumbing:** `Approve` = callback `qa:nav:ent`, `Keep planning` = `qa:nav:esc` — both already route through `_on_qa` → `NAV_ACTIONS` → `send_key`, so **no inbound handler change is needed**. Keep the full nav-row (`_nav_row()`) too, so the human can arrow to a *specific* variant (e.g. "Yes, manually approve edits") when the default isn't what they want.
  3. **Where to build it:** extend `ask_reply_markup(msg)` in `chela/telegram/interactive.py:119` to return the ExitPlanMode keyboard when `msg.tool_name == "ExitPlanMode"` (dispatch on `tool_name`; the AskUserQuestion path stays unchanged). The relay already calls `ask_reply_markup(msg)` at both attach sites (`relay.py:163,206`) and threads `reply_markup` through `BotSender.send` — so **no relay change is needed** beyond what A shipped. Rename to `interactive_reply_markup` only if it reads cleaner (then update the 2 call sites + import); otherwise just branch inside.
  4. **VALIDATE EMPIRICALLY (load-bearing):** trigger a real ExitPlanMode in a live test agent (present a plan), `capture-pane`, and confirm: (a) **`Enter` proceeds** — and **REPORT which option it selects** (does the default silently enable *auto-accept edits*, or is it manual-approve?); (b) **`Escape` keeps planning** — stays in plan mode, does NOT cancel the whole task. If `Enter`'s default is auto-accept-edits (a real behaviour change the user may not want), either **label the button accurately** (e.g. `✅ Approve (auto-accept edits)`) or make the **nav-row primary** — report which you shipped and the exact behaviour of Enter/Esc.
  5. **Callback / handler:** reusing `qa:nav:ent`/`qa:nav:esc` (recommended) means **nothing new in `inbound.py`**. If instead you add a dedicated `pa:` (plan-approve) scheme for nicer toasts, mirror `_on_qa` (`inbound.py:453`): resolve the window from the callback's **own topic** via `router.resolve` (never trust a window id off the wire — CMX-8), chat-gate fail-closed, `send_key`, `query.answer(<label>)`, register its `CallbackQueryHandler(pattern=…)` **before** the catch-all, and ensure `^qa:` and the new pattern don't overlap.
  6. **Keep it additive:** the ExitPlanMode keyboard attaches the same additive way as A's — if building it fails, still relay the plan text (never crash the relay). The AskUserQuestion (`qa:<i>`) and `/screenshot` (`k:`) keyboards must be untouched and keep working.
  **Landmines:** don't regress Slice A (AskUserQuestion buttons) or the `k:` screenshot keyboard — both must coexist. No general pane-scraping (defer to C). callback_data ≤64 bytes. Never crash the relay. Attribution already covered by the top-level `NOTICE` (ccbot). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference — chela (Slice A, now on `dev`):** `chela/telegram/interactive.py` (`ask_reply_markup:119`, `_nav_row:85`, `NAV_KEYS:51`, `NAV_ACTIONS:61`, `select_keystrokes:68`, `decode_callback:144`, `QA_NAV_PREFIX`), `relay.py:47,163,206`, `inbound.py:453,514`, `parser.py` (`Message.tool_name`/`tool_input`). **ccbot** (context only — do NOT port the scraper): `src/ccbot/terminal_parser.py` ExitPlanMode patterns. **Verify:** unit-test (a) an ExitPlanMode `tool_use` message → keyboard with Approve(`qa:nav:ent`)/Keep-planning(`qa:nav:esc`) + nav-row; (b) AskUserQuestion still returns its `qa:<i>` semantic buttons (no regression); (c) a plain/text message → no keyboard. Plus the live validation; report Enter/Esc behaviour in the PR.

- [x] **Interactive UI Slice A — AskUserQuestion inline-keyboard answering (single-select MVP).** When an agent calls `AskUserQuestion`, the bridge already relays the question text to its Telegram topic (it's special-cased in `INTERACTIVE_TOOL_NAMES`, `chela/telegram/relay.py:45`). Add an **inline keyboard** so the human can answer from the phone instead of typing the answer back. **Detection = transcript (structured), NOT pane-scrape.** The `AskUserQuestion` `tool_use` block carries `input.questions[]` with `{question, header, multiSelect, options:[{label, description, preview}]}` — parse this where the relay builds the message. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read the FULL spec in the numbered items 1–6 + Landmines + Verify directly beneath this line — they carry the load-bearing constraints (MVP boundary, the VALIDATE-keystrokes-empirically-first step, the resolve-window-from-topic security rule, and the ccbot files to port from). Do not implement from this summary line alone.**
  1. **MVP scope (HARD boundary):** handle ONLY a **single question** with **`multiSelect == false`** and **no "Other"/free-text**. If the tool_use has `len(questions) > 1`, OR `multiSelect == true`, OR a free-text option — do NOT attach semantic option buttons; attach only the navigation-fallback row (below) so the human drives manually. Never present a broken keyboard.
  2. **Semantic option buttons (primary UX):** one button per `options[i].label` (truncate to Telegram's button-text limit), `callback_data = f"qa:{i}"` — **index only; NEVER put the label in callback_data (64-byte limit).** Tapping option *i* selects it.
  3. **Navigation-fallback row (PORT from ccbot, don't reinvent):** mirror ccbot's `_build_interactive_keyboard` (`~/projects/ccbot/src/ccbot/handlers/interactive_ui.py:81`) — a compact `↑ / ↓ / ⎋ Esc / ⏎ Enter / 🔄` row driving chela's existing `messenger.send_key` (the same primitive the `/screenshot` control-key keyboard already uses, `chela/messenger.py:121`). Always present, so the human can navigate manually if a semantic pick misfires or in the fallback cases above.
  4. **Answer-injection contract — VALIDATE EMPIRICALLY FIRST (load-bearing unknown):** Claude Code's AskUserQuestion selector highlights option 0 on render; sending `Down`×i then `Enter` should select option i and (single question) submit. **Before wiring the semantic buttons, trigger a real single-select AskUserQuestion in a live test agent, `capture-pane`, and confirm `Down`×i + `Enter` lands on and submits option i** (watch for cursor-start offset or a leading header/"Other" row). If unreliable, make the **navigation keyboard primary** and downgrade the semantic buttons — **report which you shipped and the exact keystroke sequence that worked** (Slice B / ExitPlanMode reuses it).
  5. **Callback handler (mirror `_on_key`, `chela/telegram/inbound.py:371`):** add a `CallbackQueryHandler` (or branch inside `_on_key`, which early-returns on non-`k:` data at `inbound.py:385`). On `qa:{i}`: **resolve the window from the callback message's OWN topic via `router.resolve(chat_id, thread_id)` — NEVER trust a window id from the payload** (CMX-8 isolation boundary), chat-gate fail-closed, fire the keystrokes via `send_key`, and `query.answer(<label>)` for instant feedback. Register it before the catch-all `MessageHandler` (`inbound.py:434`).
  6. **Wiring:** pass the injection fn (index → `send_key` sequence) into `build_application` the way `send_key`/`send_escape`/`capture` already are (`chela/telegram/inbound.py:200`, `chela/main.py:533`). Attach the keyboard to the AskUserQuestion message emitted in `RegistryRelay.on_message` (`chela/telegram/relay.py:182`); `BotSender.send` (`relay.py:112`) must accept an optional `reply_markup` — add it, keep the existing MarkdownV2→plain fallback intact.
  **Landmines:** keyboard is ADDITIVE — if building it fails, still relay the text (never crash the relay). callback_data ≤64 bytes, index only. Resolve window from topic, not payload. Don't regress the `k:` screenshot keyboard (`_on_key` must still handle `k:`; the new handler ignores non-`qa:` data). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (port mechanics): ccbot** `src/ccbot/handlers/interactive_ui.py` (`_build_interactive_keyboard`, edit-in-place tracking), `src/ccbot/handlers/callback_data.py` (`CB_ASK_*` keystroke callbacks), `src/ccbot/bot.py` (callback_handler round-trip). **chela:** `relay.py:45,112,182`, `inbound.py:200,273,371,427-450`, `messenger.py:121`, `bindings.py:96,103`, `main.py:492,533`, `parser.py:101-170`. **Verify:** unit-test (a) single-question single-select → reply_markup with one `qa:{i}` button per option; (b) multiSelect==true OR >1 question OR Other → NO semantic buttons (nav-only); (c) callback for `qa:2` resolves window from topic + fires index-2 injection, ignores non-`qa:` data. Plus the live validation; report the working keystroke sequence in the PR.

- [x] **Render assistant markdown properly in Telegram (add `telegramify-markdown`).** `chela/telegram/format.py::to_markdown_v2` currently **blind-escapes** the body via `escape_markdown_v2` (every MarkdownV2 special char backslashed), so Claude's markdown renders LITERALLY on Telegram — ` ``` code blocks ``` ` show as escaped backticks, `*bold*` shows the asterisks, tables show raw pipes. Port ccbot's approach (it deliberately was NOT ported to stay dep-free — now we want the fidelity):
  1. Add **`telegramify-markdown`** to the `[telegram]` extra in `pyproject.toml` (what ccbot used; pulls `mistletoe`).
  2. In `to_markdown_v2`, render the **assistant/text body** through `telegramify-markdown` → valid MarkdownV2 (real code-block + bold + inline-code entities), replacing the blind `escape_markdown_v2` for that body. Optionally port ccbot's `convert_markdown_tables` (tables → card-style key/value, since Telegram has no tables) — `src/ccbot/markdown_v2.py`.
  3. **Keep the fallback:** import `telegramify-markdown` **lazily**; if it's absent (import fails) or raises on a message, fall back to the current `escape_markdown_v2` blind-escape so the core still works without the extra. The relay's existing MarkdownV2→plain-text-on-reject path stays as the outer safety net.
  4. The emoji **header** (`🔧`/`✅`/`🤖` + tool/role) must remain valid MarkdownV2 — escape it or keep it outside the converted body; don't double-escape. **Leave `to_code_block`** (used by `/screenshot`'s text fallback) untouched.
  **Don't touch** routing / registry / lifecycle / the relay's send+fallback contract. **Landmines:** don't double-escape the header; a telegramify failure on ONE message must degrade to escaped-plain for that message, never crash the relay; keep it lazy so pure format tests don't need the extra. **Verify:** unit-test that an assistant message with a fenced code block + `*bold*` renders MarkdownV2 with a real code entity (backticks NOT backslash-escaped) when telegramify is available, and that the fallback path (telegramify absent/raising) still produces safe escaped output. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. Report back. Reference: ccbot `src/ccbot/markdown_v2.py` (telegramify usage + `convert_markdown_tables`), `chela/telegram/format.py`, `chela/telegram/relay.py` (send + plain fallback), `pyproject.toml` `[telegram]` extra.

## Open — orchestrator awareness

- [ ] **Dashboard toast on dispatcher run-state transitions + `chela dispatch-runs --awaiting` summary.** So the orchestrator/human learns a run needs review without polling. Two small parts. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify beneath this line — they carry the load-bearing constraints (edge-trigger, reuse existing SSE + toast + runs-query, the no-auto-push scope guard). Do not implement from this summary line alone.**
  1. **Toast (frontend, EDGE-TRIGGERED):** in the dashboard, when a dispatcher run transitions **→ `awaiting_review`** (secondarily → `done` / → `failed`), pop a toast. **Reuse existing pieces:** the SSE `/api/events` stream that already keeps the Kanban reactive (`chela/dashboard/app.py:~2573`) and the existing toast styling (`.kanban-toast`, `chela/dashboard/static/style.css:1506`; cf. `.kanban-merge-toast`/`.kanban-promote-toast`). The frontend already consumes run/dispatcher state; track the **previous per-run status** and fire the toast **only on the transition** (NOT every ~1s poll while it sits in `awaiting_review`). Text e.g. `🔍 CMX-20 → awaiting review` with the **PR link** when present. Hang a mute toggle off the existing **"Notifications"** overflow-menu entry (persist in localStorage like other dashboard prefs).
  2. **Backend (only if needed):** if `/api/events` doesn't already carry per-run `status` + `pr_url`, add them to the named frame it emits on change (the REST `/api/dispatcher` already exposes them — mirror that source; do NOT add a new poll).
  3. **CLI summary:** add `chela dispatch-runs --awaiting` (or `--status <state>`) — a concise filter listing runs in `awaiting_review` with task id, PR URL, and age, so the orchestrator answers "what's up for review?" instantly on demand. Extend the existing command (`chela/main.py` `cmd_dispatch_runs` + the run listing in `chela/dispatcher.py`); reuse the existing query, don't add a second source of truth.
  4. **Scope guard:** this is the **pull/awareness** feature ONLY — do NOT auto-`send_keys` the event into any agent session (autonomous push into the orchestrator is a separate, deliberate decision — the future "decisions inbox"). Toasts are for the dashboard viewer; the CLI is on-demand.
  **Landmines:** toast MUST be edge-triggered (transition only) or it spams every poll; don't regress the existing Kanban toasts (promote/merge) or the SSE stream; keep the Notifications mute working; `--awaiting` reuses the existing runs query. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dashboard/app.py` (`/api/events` SSE ~2573, `/api/dispatcher`), `chela/dashboard/static/style.css:1506` (`.kanban-toast`), the dashboard frontend JS that renders the Kanban + consumes `/api/events`, `chela/dispatcher.py` (run statuses: claimed/running/awaiting_review/done/failed), `chela/main.py` (`cmd_dispatch_runs`). **Verify:** (a) a run entering `awaiting_review` pops exactly ONE toast (not one per poll) with the PR link; (b) muting via Notifications suppresses it; (c) `chela dispatch-runs --awaiting` lists only awaiting-review runs with PR URLs. Unit-test the CLI filter + the edge-trigger (prev vs new status) logic; the toast DOM may need a light JS test or manual check.

## Backlog (not yet dispatchable)

- **Settings view — editable toggles (follow-up)**: in-UI write-back + daemon restart.
- **Retire ccbot** ~07-19 after warm standby: `pm2 delete ccbot` + `pm2 save` + archive repo (ops).
- Interactive UI: AskUserQuestion / ExitPlanMode / Permission phone **approvals (buttons)** + message merging.
- `/kill` (explicit agent-kill + close topic) — optional, higher-stakes; `/unbind` `/history` `/usage` skipped (don't fit auto-topics).
- Privacy scrub (forum IDs, `CCBOT_PANE_FALLBACK`, abs paths → env) before public `main`.
- **Cost view** (cockpit): transcript tokens × price → $ per agent / run / fleet.
- **Unified graph viewer** (cockpit): memory `[[wikilinks]]` + fleet; renderer = **Sigma.js + Graphology (MIT, WebGL)**, clean-room (not a GitNexus fork — PolyForm-NC). Colorblind-safe.

## Done

- [x] telegram-setup (#14) · scaffold+extra (#15) · dashboard workflow discovery (#16) · monitor port (#17) · outbound relay+CLI (#18).
- [x] Fold steps 1-3 — paste-submit (#19), INBOUND (#21), monitor resolve-by-record (#22) [CMX-6/8/9].
- [x] Mobile Kanban — Swipe + Rows (#20, CMX-7).
- [x] Multi-topic Slice A registry (#23) + Slice B auto-create/lifecycle (#24) [CMX-10/11].
- [x] Topic naming by project cwd (#25, CMX-12).
- [x] `CHELA_SHOW_TOOL_CALLS` toggle — hide tool spam (#26, CMX-13).
- [x] Settings view — Connections & Status (#27) + Telegram-bridge row (#28) + Work-dispatcher-badge fix [CMX-14/15].
- [x] Bridge commands `/screenshot` (PNG) + `/esc` + `/` menu (#29, CMX-16) + `/screenshot` control-key keyboard + Refresh (#30, CMX-17).
- [x] **CUTOVER LIVE 2026-07-12** — `chela-telegram` (pm2 id 12) on @chelamuxbot / test forum ↔ 4 agents; tool-spam hidden.
