# TODO

## Open — interactive UI (phone approvals)

- [ ] **🔴 A GATE IS RENDERED FROM THE PANE, SO A SHAPE THE SCRAPER CANNOT PARSE ARRIVES ON THE PHONE AS AN UNANSWERABLE STUB — while the hook already holds the full payload.** Hit live 2026-07-14: the orchestrator asked a 3-question `AskUserQuestion` (single-select, each option carrying a `preview`). Telegram showed **the question text and a bare nav row** (`↑ ↓ ↺ ⏎ 🔄`) — **zero options, zero descriptions, zero previews, nothing to tap.** Meanwhile `~/.chela/events.jsonl` **seq 184** (`hook.pre_tool_use`) and **seq 185** (`hook.permission_request`) carried the *complete* `tool_input`: all 3 questions, every option's `label`, `description` **and** `preview`, `header`, `multiSelect`, `permission_mode=auto`. **The data was in the log before the message ever reached the phone. The relay threw it away.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  **Root cause — this is a designed fallback firing, not a crash.** `panescan.AskUQ.multi` is documented as *"True for a multi-tab / multi-select selector **(or an otherwise unparseable one)**: the relay then offers the nav row only"* (`chela/telegram/panescan.py` ~289-300). The option regexes were measured against *"a single-select, single-question selector"* (~249-282): `_RE_OPTION` expects `❯ 1. Label` to **start** a line, and `_RE_MULTI_TAB` (`^\s*←\s+[☐✔☒]`) treats a tab strip as unparseable. A **multi-question** selector draws that tab strip, and a selector whose options carry a **`preview`** switches the TUI to a **side-by-side layout** (option list left, preview box right) — so the option rows no longer start their line. Either alone trips the fallback. **The scraper will keep meeting shapes it was not measured against; the hook will not.** ⛔ Do **not** fix this by extending the regexes to cover previews/tab-strips — that is re-deriving, from a lossy render, data you are **already being handed structurally**. Fix the *source*, not the parser.
  1. **The HOOK payload becomes the CONTENT authority for a gate; the pane stays only for liveness/fallback.** When a gate is detected for a bridged window, look up that window's most recent **unresolved** `hook.pre_tool_use` for an interactive tool (`AskUserQuestion` / `ExitPlanMode`) in the event log and render **from its `tool_input`** — full fidelity, every question, every option's `label` + `description` + `preview`, `header`, `multiSelect`. **Use `PreToolUse`, not `PermissionRequest`, as the content source: `PreToolUse` carries a `tool_use_id` and `PermissionRequest` does NOT** (already recorded in this file, item "EVENT LOG — part B"), so `PreToolUse` is the only one you can pair with its `PostToolUse` to know the gate is **resolved**. Define "pending" as *a `pre_tool_use` for an interactive tool with no `post_tool_use` bearing the same `tool_use_id`* — and say so in a docstring.
  2. **One Telegram message PER QUESTION — not one per gate.** Three questions × three options × a dozen preview lines is a wall of text on a phone, and the TUI itself walks questions **one at a time**. Each message carries that question's options numbered in full, their descriptions, and their previews, with its own selector row. This is also what makes the multi-question case *answerable* at all.
  3. **Previews render, and are NEVER silently dropped.** Put a `preview` in a Telegram `<pre>` block — those **scroll horizontally rather than wrapping** on mobile, so box-drawing ASCII survives (**VERIFY THIS LIVE on a real phone before building on it** — do not take it on faith; this repo's "green tests lied" count is 4-for-4). The >4096 splitter is already entity-safe (PR #50) and will carry them. ⛔ **If a preview exists and the card does not show it, the card MUST say so.** A gate that renders as though it were complete when it is not is the same silent-degrade class as the wrong `wid` (CMX-48) and the silent mis-answer (CMX-32): the human answers with less information than the asker intended, and nothing tells them.
  4. **Be HONEST about what you cannot answer yet.** Answering still goes through keystroke injection in this slice. Where the scraper *can* still locate the cursor (single-question, single-select), keep the existing `1 2 3` selector path — it works. Where it **cannot** (multi-question, `multiSelect`), render the full content, offer the nav row, and **state plainly in the message that this shape must be answered in the terminal for now.** ⛔ Do **not** synthesize a selector whose ordinal mapping you cannot prove — a button that silently picks the wrong option is exactly CMX-32, and it is worse than no button. The zero-keypress answer path is the **next** task; do not build it here.
  **Landmines:** **hooks are read at agent STARTUP** — an agent launched before the plugin was installed emits **no** hook events, so the pane-scraped path **must remain the fallback** and must not regress (do not delete `panescan`/`gatewatch`'s detectors); the log is the content source but the **pane is still what tells you a gate is on screen right now** — a `pre_tool_use` with no `post_tool_use` could also mean the agent died, so **corroborate against the pane before posting** (cf. the false-`DIED` bug, CMX-35); a **subagent's** events carry the **parent's** `session_id` — a parent-window gate and a subagent's tool call must not be confused; events **before PR #61** carry **wrong `wid`s** — do not resolve a gate against them (scope your lookup to the current `boot_id`, or to `seq` above the #61 boundary, and say which); `preview` is **optional** and often absent — an empty preview must render nothing, not an empty `<pre>`; don't regress the tracked-message lifecycle (`_uq_msg` post/edit/poof) or the ephemeral status line (CMX-43), which shares the same tick. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `~/.chela/events.jsonl` **seq 184/185 — the real payload this bug is about; read it first**; `chela/telegram/panescan.py` (`AskUQ` ~285, `_RE_OPTION`/`_RE_MULTI_TAB` ~271-282 — the fallback that fires), `chela/telegram/gatewatch.py` (`format_askuq_message`, `poll` ~354, the post/edit/poof tracker ~393/~436), `chela/telegram/interactive.py` (`scraped_reply_markup`, `NAV_KEYS` ~84, the `qa:*` callback scheme), `chela/event_log.py` (`read(types=…, wid=…)` — "the ONE authority the UI and the inbox both read"), `chela/hooks.py` (the receiver + the CMX-48 origin-dir correlation), `docs/HOOKS.md`. **Verify:** unit-test that a gate whose window has a pending `pre_tool_use` renders **from the payload** (all questions, all options, descriptions, previews) and not from the scrape; that a `post_tool_use` with the same `tool_use_id` marks it resolved; that a window with **no** hook events still falls back to the pane-scraped render (the pre-plugin fleet); that a preview-bearing option whose preview is omitted produces an explicit notice. **LIVE (mandatory — a green suite has lied 4× on this substrate):** from a real agent, fire the **exact shape that broke**: a 3-question `AskUserQuestion` with previews. Every question must arrive as its own message with its options, descriptions and previews **legible on the phone**. Screenshot it.

- [ ] **ANSWER A GATE FROM THE PHONE WITH ZERO KEYPRESSES — the hook can return the answer, and for multi-question / multi-select it is the ONLY safe way. (Depends on the task above.)** Keystroke injection cannot reliably answer the shapes we actually send: CMX-32 already fixed a **silent mis-answer** (tapping option 3 selected option 2 — Enter raced the selector's arrow moves), and a multi-question / `multiSelect` picker multiplies that race by every question. **PROVEN (measured 2026-07-13, Claude Code 2.1.207 — do not re-derive, do not trust the docs over this):** a `PermissionRequest` hook can **ANSWER** an `AskUserQuestion` by returning `{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow","updatedInput":{"questions":[…],"answers":{"<question string>":"<chosen label>"}}}}}` — **zero keypresses, in every permission mode including `auto`.** This retires the keystroke substrate for gates. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **🔴 A BOUNDED WAIT IS MANDATORY — this is the whole risk of the feature.** The hook runs **synchronously inside the agent's process**: the moment the endpoint blocks waiting for a human, **a live agent is frozen**. Symphony `SPEC.md` is explicit: *"A run MUST NOT stall indefinitely waiting for user input."* So the `/hooks/permission_request` handler must wait **at most** a configured budget (`CHELA_GATE_WAIT_S`, default it conservatively) for an answer to arrive from Telegram, then **give up and return `{}`**. ⛔ **The fallback on timeout is FAIL-OPEN to the existing pane path — NOT auto-deny.** Returning `{}` leaves the TUI picker exactly as it is today: still on screen, still answerable in tmux or by the current nav-row keystrokes, and the run is no worse off than before this feature existed. An auto-deny would *destroy* work on a slow human. Say this in a docstring so nobody "hardens" it into a deny later.
  2. **The wait budget must be SHORTER than the hook's own timeout, and that ceiling is a MEASURED number, not a guess.** The plugin manifest declares `{"type":"http", …, "timeout": N}`; if the daemon answers later than `N`, Claude Code kills the hook and fails open anyway — so a wait longer than the timeout is a wait that can never deliver an answer. **Empirically determine the maximum `timeout` Claude Code actually honours for an `http` hook** (raise it for `PermissionRequest` **only** — `PreToolUse`/`PostToolUse` fire at high volume and must stay fast; measured: 78% of the log is tool-call chatter). Then set the wait budget strictly below it. **Do not hardcode a number from memory or from the docs — measure it, and put the measurement in the PR.**
  3. **The answer must be matched to the RIGHT gate, and a stale answer must be refused.** The `answers` map is keyed by the **question string**, and a gate is identified by its `tool_use_id` (from `PreToolUse` — `PermissionRequest` has none, so you must correlate). A tap that arrives after the gate resolved, after the agent died, or for a *different* gate in the same topic **must be dropped and reported**, never applied to whatever is on screen now. `multiSelect` questions take a **list** of labels; single-select takes one. Answer **every** question in the payload — a partial `answers` map is a shape you must decide about explicitly (does Claude Code accept it? **test it**) and document.
  **Landmines:** ⛔ **do NOT flip the dispatcher to `acceptEdits` in this task** — that would extend blocking-on-a-human to every out-of-sandbox Bash command, and the handoff gates it on exactly this bounded-wait work landing first; **an in-cwd Bash command is sandbox-approved and does NOT gate even under `acceptEdits`** — only an **out-of-sandbox** path is a valid gate test, and a naive test gives a false negative; the daemon being **down or slow must never wedge an agent** (fail open, always — this is already the CMX-41 contract, do not weaken it); a blocked hook holds a **request thread** — N simultaneous gates must not exhaust the dashboard's threadpool (bound it, and say what happens at the bound); **`PermissionDenied` does not fire on an interactive deny** (measured, CMX-41) — do not build the resolution path on it; do not answer a gate for an agent the human never saw (an unbound topic must not silently auto-answer anything); keep the pane path working for pre-plugin agents. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/hooks.py` + `chela/dashboard/app.py` (`POST /hooks/<event>` — today returns `{}` and a test asserts the **absence** of `permissionDecision`/`hookSpecificOutput`; **that test now has to change, deliberately and visibly**), `chela/telegram/interactive.py` (the callback that will now deliver an answer instead of keystrokes), `chela/event_log.py`, `docs/HOOKS.md`, the previous task (the render this answers). **Verify:** unit-test that an answer delivered inside the budget returns a well-formed `updatedInput`; that the budget expiring returns `{}` (fail-open, **not** a deny); that a stale/mismatched `tool_use_id` is refused and logged; that a `multiSelect` question round-trips a list; that N concurrent blocked gates do not exhaust the server. **LIVE (mandatory):** drive a real 3-question `AskUserQuestion` with previews from a real agent, answer **all three from the phone**, and confirm from the agent's transcript that it received exactly the chosen labels — **with no keystrokes sent to the pane at all** (prove it: the answer path must not call `send_tmux`). Then let one gate **time out** and confirm the agent is unharmed and still answerable in the terminal.

- [ ] **🔴 THE DISPATCHER WAS SILENTLY OFF FOR ~9 HOURS AND EVERYTHING REPORTED HEALTHY — `chela doctor` was ALL-GREEN, the daemon logged nothing, and reconcile stopped too.** Found live 2026-07-14. `CHELA_DISPATCH_WORKFLOWS` was **missing from `~/.chela/chela.env`** — dropped by the **CMX-42 env migration** (the PR whose entire purpose was to kill config drift). `config.py:257` read `""` → `DISPATCH_WORKFLOWS = []` → `main.py:131`'s `if DISPATCH_WORKFLOWS:` skipped the dispatcher **and the reconcile loop that lives in the same tick**. Consequences, all silent: no task was ever claimed from `TODO.md`; PR #61 merged and its run sat in `awaiting_review` forever; that stale run held the **only** concurrency slot (`concurrency.max: 1`), so even a correctly-configured dispatcher would have been blocked behind it. **The one and only tell was the ABSENCE of a log line** (`Dispatcher enabled for N workflow(s)` never printed). **Nobody reads an absence.** This is the same failure class as the CMX-42 port drift (hooks POSTing into a closed socket, feature silently dead) and it slipped through the very tooling built to catch that. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **A DISABLED SUBSYSTEM MUST ANNOUNCE ITSELF — loudly, at startup, every time.** `cmd_run` logs `Dispatcher enabled for N workflow(s)` when on and says **nothing** when off. Invert that: when `DISPATCH_WORKFLOWS` is empty the daemon must emit a **WARNING** naming the var, what is consequently not happening (**dispatch AND reconcile — say both**, the coupling is the non-obvious part), and how to fix it. Sweep `cmd_run` for **every** other capability behind an `if <config>:` guard (scheduler, inbox, notifications, terminals) and give each the same treatment: a startup line that states **on or off**, never silence-means-off. ⛔ An empty config may remain a *valid* state — it must not remain an *invisible* one.
  2. **`chela doctor` must verify the daemon's JOB, not just its env file.** `doctor.py:46` already lists `CHELA_DISPATCH_WORKFLOWS` among the vars it knows — and doctor still printed **all-green** while the dispatcher was dead. Checking "the env file parses and the running env agrees with it" is checking the wrong thing: **both agreed, and both were wrong.** Doctor must assert the *effective capability*: the dispatcher is enabled, each configured `WORKFLOW.md` **exists and parses**, its tracker file exists, and the daemon that is actually running has it on. A `WARN` (not a silent pass) when the dispatcher is off. Apply the same "assert the capability, not the config" lens to doctor's other checks.
  3. **Ship the var in the repo's env template so a fresh install cannot lose it again.** CMX-42 added `examples/`/`scripts/chela-env.sh`; whatever the canonical template is, `CHELA_DISPATCH_WORKFLOWS` must be **in it, with a comment stating that empty disables dispatch *and* reconcile**. The live `~/.chela/chela.env` was hand-repaired on 2026-07-14 (with that comment) — the repo must not still be able to generate the broken file.
  **Landmines:** ⛔ **do NOT "fix" this by defaulting `DISPATCH_WORKFLOWS` to `./WORKFLOW.md`** — a daemon that silently starts dispatching against whatever repo it happens to be `cwd`'d into is far worse than one that is off; the *empty* state is fine, the *silence* is the bug; don't turn the startup warning into a per-tick log line (it will bury the log within a day — announce at startup, and surface the state in `/api/settings` / the dashboard where a human can actually see it); doctor must not shell out to anything slow (it is run interactively); **doctor must be honest about what it cannot see** — it runs in a *different process* from the daemon (exactly the CMX-42 trap), so "the config says enabled" is **not** "the running daemon has it enabled": read the daemon's real state (the port-file pattern CMX-42 established for the dashboard is the precedent — consider having the daemon publish its effective capabilities the same way) or say plainly that it is inferring. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/config.py` (~250-261 — `DISPATCH_WORKFLOWS`, and the comment *"Empty = dispatcher off in the daemon"* that was accurate and useless), `chela/main.py::cmd_run` (~119-180 — the `if DISPATCH_WORKFLOWS:` guard and the log line that only fires on the happy path), `chela/doctor.py` (~46 — the var it already knows about and does not check), `~/.chela/chela.env` (the live file, hand-repaired), `scripts/chela-env.sh` + the repo's env template (the thing that must ship it), `chela/dashboard/app.py` (`/api/settings` — where an operator-visible "dispatcher: OFF" belongs). **Verify:** unit-test that an empty `CHELA_DISPATCH_WORKFLOWS` produces a **WARNING at startup** naming both dispatch and reconcile; that doctor **WARNs** (not passes) when the dispatcher is off; that doctor FAILs when a configured `WORKFLOW.md` is missing or unparseable; that the shipped env template contains the var. **LIVE (this is the whole point):** start the daemon with the var **empty** and confirm (a) the log says loudly that dispatch and reconcile are OFF, and (b) `chela doctor` does **not** report all-green — the exact scenario that hid for 9 hours must now be impossible to miss.

- [x] **🔴 HOOK EVENTS ARE FILED AGAINST THE WRONG WINDOW — the correlation key (`cwd`) is not stable.** Found the moment the plugin went live, 2026-07-14. The orchestrator's own session (`@0`, cwd `/home/liavedunix`) had been `cd`-ing into `~/projects/chelamux` to work, so **every one of its hook events was filed against `@1` (chelamux)** — a different agent:
  ```
  seq 20  hook.user_prompt_submit  wid=@1  cwd=/home/liavedunix/projects/chelamux  session=cf19ca61   <- actually @0
  seq 21  hook.pre_tool_use        wid=@1  cwd=/home/liavedunix/projects/chelamux  session=cf19ca61   <- actually @0
  ```
  `chela/hooks.py` correlates by matching the payload's **`cwd`** against tmux `#{pane_current_path}`. But **`cwd` is the session's *current* directory, not its identity** — it changes the moment an agent `cd`s anywhere, which is completely normal (any agent inspecting another repo, running a tool from elsewhere, or working across projects). The pane's `pane_current_path` is *also* mutable. So the key is mutable on **both** sides. CMX-41 deliberately chose "ambiguity resolves to `None`, never a guess" — but this is worse than ambiguity: it is a **confident wrong answer**, and an event filed against the wrong agent is exactly the failure that rule existed to prevent. **The whole timeline/inbox UI is per-window — building it on this key would make the UI lie.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **Correlate on `session_id` — it is stable, unique, and ALREADY IN EVERY PAYLOAD.** (Confirmed: `session=cf19ca61` is the orchestrator's real Claude Code session id.) You need a **`session_id` → window** map. The robust source: a window's pane → the `claude` **pid** (`agent_manager.claude_pid`) → the transcript that process holds **open** (`/proc/<pid>/fd/*`, or `lsof`), whose filename **is** `<session_id>.jsonl` (Claude Code writes `~/.claude/projects/<slug>/<session_id>.jsonl`). That gives an identity that survives any `cd`. **Verify this empirically before building on it** — do not assume the fd is held open; check.
  2. **Cache it, and hold no lock across the lookup.** A hook runs **synchronously inside a live agent** — walking `/proc` per event would be a disaster at `PreToolUse` volume. Resolve lazily, cache `session_id → wid`, and refresh on a miss (a new session appears; a window is renamed; a window dies). A cache miss must fall back to **`None`, never to a `cwd` guess.**
  3. **Keep `cwd` only as a last-resort HINT, and only when it is unambiguous** — or drop it entirely and say why. If two windows share a cwd, or the cwd matches a window that is *not* the session's, `None` is the correct answer. **A wrong `wid` is worse than no `wid`.**
  **Landmines:** the existing `hook.*` events already in `events.jsonl` have **wrong `wid`s** — decide whether to backfill, drop, or leave-and-document (they are already in the log; a UI built later will render them); don't regress the ~5ms budget (CMX-41 rejected the `pgrep` + `claude agents --json` path precisely because it takes *seconds*, and the agent is BLOCKED while a `PermissionRequest` hook runs); a subagent's events carry the **parent's** `session_id` — decide what that means for attribution and say so; the dispatcher's worktree agents live in `~/.chela/worktrees/...` and their cwd genuinely *is* their identity — don't break that case while fixing this one. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/hooks.py` (the correlation — the whole bug), `chela/agent_manager.py` (`claude_pid`, the window↔pid authority), `chela/telegram/monitor.py` (it already resolves a window's **transcript** — that is the same session→window link, solved once already: see CMX-9 "resolve transcript by newest record"), `docs/HOOKS.md`. **Verify:** unit-test that a payload whose `cwd` points at a *different* agent's directory still resolves to the **session's own** window; that an unknown session resolves to `None` (not a cwd guess); that the lookup is cached (one `/proc` walk, not one per event). **LIVE (this is the whole point):** from the orchestrator's own session (`@0`, home dir, but `cd`-ed into `~/projects/chelamux`), fire a real tool call and confirm the event lands with **`wid=@0`** — not `@1`. Then confirm a dispatched worktree agent still correlates correctly.

- [x] **FLAKY TEST — `test_wait_sleeps_normally_when_no_signal_arrives` asserts on the real wall clock and fails ~1 run in 5 under load.** Caught 2026-07-14 while verifying CMX-43 (it is **not** that PR's fault — it predates it, from `d82729e`, and is on `dev`). `tests/test_graceful_shutdown.py`:
  ```python
  started = time.monotonic()
  assert stop.wait(0.2) is False        # no signal → a plain sleep
  assert time.monotonic() - started >= 0.15
  ```
  Observed failure: `assert (28437.504260218 - 28437.388405753) >= 0.15` — the 0.2s wait measured **0.116s** on a busy machine. **A flaky test is worse than a failing one**: it teaches everyone to re-run until green, and this repo has *already* shipped four bugs that were green in CI and broken live — a suite nobody trusts is how the fifth gets through. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Verify.**
  1. **Stop asserting on the real clock.** The test's *intent* is "with no signal, `wait()` does not return early — it sleeps." Assert that **behaviourally**, not chronometrically: inject/patch the clock or the underlying `Event`, or assert on what `wait()` **returned** (`False` = timed out, which is already the first assertion and is the real contract) and drop the duration check entirely. If a duration assertion is genuinely wanted, it must tolerate a starved scheduler (a wide margin is a smell — prefer a fake clock, as `tests/test_telegram_status.py` correctly does for its throttle).
  2. **Sweep for the same shape.** Grep the suite for other real-clock assertions (`time.monotonic()`, `time.time()`, `time.sleep` + a duration assert) and fix them the same way. A CI box under load is exactly where these fire.
  **Landmines:** don't delete the test — the graceful-shutdown contract it guards is real (a daemon that returns early from `wait()` would spin); don't "fix" it by widening the margin to something like `>= 0.05` (that is the same bug with a longer fuse); keep the other tests in the file (a second signal must not raise). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `tests/test_graceful_shutdown.py`, `chela/` (`GracefulShutdown` — the thing under test), `tests/test_telegram_status.py` (the **fake-clock** pattern to copy). **Verify:** the decisive check is **statistical, not a single green run** — run the full suite **20×** under concurrent load (e.g. with other pytest processes running) and show **20/20 green**. State that in the PR. One green run proves nothing about a flake.

- [x] **`/screenshot` keyboard: drop the arrow WORD-labels, and add the missing 🔄 refresh.** Liav, 2026-07-13: *"in the screenshot keys, no need to write the arrows labels, and i'm missing the refresh button."* Two defects in `SCREENSHOT_KEYS` (`chela/telegram/inbound.py` ~134):
  ```
  ␣ Space   ↑        ⇥ Tab        <- ↑ is bare, but...
  ← Left    ↓ Down   → Right      <- ...these three carry redundant words
  ⎋ Esc     ^C       ⏎ Enter
  ```
  (a) the arrow captions are **inconsistent and redundant** — `↑` is already a bare glyph while `← Left` / `↓ Down` / `→ Right` spell it out; the glyph *is* the label, and the words only cost width on a phone. (b) **There is no refresh button on this keyboard at all** — `🔄` (`ref`) exists only on the *AskUserQuestion* nav row (`chela/telegram/interactive.py:84`, `qa:nav:ref`), so after driving the terminal with these keys there is **no way to see the result** without sending `/screenshot` again by hand. The re-capture code already exists and is already shared (`inbound.py` ~463, "Shared by the `/screenshot` command and the AskUserQuestion 🔄 refresh") — it is simply not wired to a button here. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Arrows become glyph-only:** `←` `↑` `↓` `→`. Leave the non-arrow keys' words (`␣ Space`, `⇥ Tab`, `⎋ Esc`, `^C`, `⏎ Enter`) — those glyphs are **not** self-evident and the words earn their width. Re-flow the rows so the four arrows sit together (they are now narrow), e.g. a dedicated arrow row; keep it to 3–4 buttons per row so nothing truncates on a phone.
  2. **Add `🔄` refresh to the screenshot keyboard**, firing the existing re-capture (**edit the screenshot message in place** — do not post a new one each tap, or a few taps bury the topic). Reuse the shared refresh path rather than writing a second one.
  **Landmines:** `_KEY_ACTIONS` is derived from `SCREENSHOT_KEYS` precisely so a button and the key it fires **can never drift apart** — keep that derivation; **`ref` has no tmux key** (it is `None` in the nav row — a refresh is not a keypress), so the `(label, key_id, tmux_key)` tuple shape must tolerate that or the flattening will break; don't change the `k:` callback prefix or the `qa:nav:*` scheme (both are live); the target window is re-resolved from the message's **topic**, never trusted from the payload — keep it that way; the toast must still echo what was sent. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/inbound.py` (`SCREENSHOT_KEYS` ~134, `_KEY_ACTIONS` ~146, `_KEY_CB_PREFIX`, the shared snapshot/refresh ~463, the key handler ~540), `chela/telegram/interactive.py` (`NAV_KEYS` ~84 — how `🔄`/`ref` is modelled there; `_snapshot`/refresh handling ~339-351). **Verify:** unit-test that every button in `SCREENSHOT_KEYS` resolves through `_KEY_ACTIONS` (no drift), that `ref` maps to a refresh and not a keypress, and that a refresh **edits** the existing message rather than posting a new one. **Live:** `/screenshot` from the phone → tap an arrow → tap 🔄 → the same message updates in place showing the moved cursor.

- [x] **A passthrough slash command (`/clear`, …) sends SILENTLY — no confirmation, so from a phone it is indistinguishable from being swallowed.** Liav, 2026-07-13: *"when we send escape from telegram i see 'sent escape' but not for /clear — can we add send confirmation as well?"* `/esc` is a **BRIDGE** command, handled in-process, so it replies `⎋ Sent Escape` (`chela/telegram/inbound.py:515`). `/clear` and every other Claude Code command in `PASSTHROUGH_COMMANDS` is forwarded raw to tmux and the handler returns **silently**. Given that this exact path was broken until today (`/clear@chelamuxbot` was forwarded verbatim and Claude Code never recognised it — the session was never cleared, and *nothing said so*), silence is the worst possible feedback. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Confirm the SEND, and claim nothing more.** After the passthrough `send_tmux` returns, reply with what is actually known — e.g. `⏎ Sent /clear` on success, `❌ Couldn't send /clear` on failure — mirroring the Escape reply's shape and tone. ⛔ **Do NOT say "session cleared"** or otherwise assert the command's *effect*: all we know is that the keystrokes were delivered. Over-claiming here is how a silent failure becomes a lying success.
  2. **Scope it to commands — do NOT confirm every plain message.** A `✓` on every chat message would turn a topic into acknowledgement spam. Passthrough **slash commands** only (the `PASSTHROUGH_COMMANDS` path). Consider whether a Telegram **reaction** on the user's own message (`setMessageReaction`, e.g. 👍) is less noisy than a new message — if you choose the reply, keep it terse and consistent with `⎋ Sent Escape`; if you choose the reaction, say why and make sure a *failure* is still loud (a missing reaction is not a visible error).
  **Landmines:** slash commands go through the Escape-first path (`send_tmux` sends Escape, settles, then `/cmd` + Enter) — the confirmation must reflect the **whole** sequence succeeding, not just the first keystroke; don't regress the `@botname` strip (`resolve_command_for_window` — a group delivers `/clear@chelamuxbot`); don't confirm a command that was **dropped** (an unbound topic, a chat-gate rejection) — that must stay a rejection, not a cheerful "sent"; keep the reply out of the transcript-relay path so it can't be mistaken for an agent message. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/inbound.py` (`PASSTHROUGH_COMMANDS` ~75, `resolve_command_for_window` ~86, the Escape reply ~515, the passthrough forward path), `chela/messenger.py::send_tmux` (the return value that tells you it landed). **Verify:** unit-test that a passthrough command replies on success and on failure, that a *dropped* command does not reply "sent", and that a plain message still gets no confirmation. **Live:** send `/clear` from the phone and see the confirmation — then confirm the session actually cleared.

- [x] **`chela msg` says an agent is OFFLINE while `/api/agents` says it is BUSY — two authorities disagreeing about the same window.** Hit live 2026-07-13: `chela msg @32 "..."` → `@32 offline — message not delivered`, while `curl /api/agents` reported `@32 cmx-43 busy` **at the same moment** and the window was plainly alive and working (a `messenger.send_tmux("@32", …)` straight from Python delivered fine). So a real message to a real, working agent was **silently dropped** — the orchestrator's own agent-to-agent channel failed closed with a wrong reason. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Find the second source of truth and kill it.** `/api/agents` (`agent_manager.session_status_map` / `claude_pid` — the `claude agents --json` authority) is the documented one place agent liveness lives. `chela msg` is evidently deciding liveness some other way (a stale cache, a pid check, a window-name lookup, a `liveness` helper). Make `msg` / `broadcast` use **the same authority** as `/api/agents`, or explain in the code why they legitimately cannot and what the fallback is. **Do not add a third.**
  2. **A message must never be silently lost.** If liveness genuinely cannot be established, the honest behaviours are (a) **send anyway** — a paste into a live pane is harmless and the agent picks it up — or (b) fail **loudly** with the real reason and a non-zero exit. Silently reporting "offline" about a busy agent is the worst of both. Pick one, and say why.
  **Landmines:** don't regress the `chela msg` refusal to self-notify (an orchestrator messaging its own window is a loop, and that guard is deliberate); a *genuinely* dead window must still be reported as such (don't invert the bug); `session_status_map` is **cached** — use `force=True` for a fresh read where correctness matters (the dispatcher already learned this); a busy agent's pane shows an empty `❯` **below** its spinner, so "the prompt looks idle" is NOT evidence of death (that misread is what produced the false-`DIED` bug, CMX-35). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/main.py` (`cmd_msg` / `cmd_broadcast`), `chela/messenger.py` (`send_tmux` — the path that *worked*), `chela/agent_manager.py` (`session_status_map`, `claude_pid`, `liveness` — the real authority), `chela/dashboard/app.py::api_agents` (what `/api/agents` actually reports). **Verify:** unit-test that a busy agent is messageable; that a dead window fails loudly with the real reason; that self-notify is still refused. **Live:** dispatch an agent, and while it is **busy**, `chela msg` it — the message must arrive (this is the exact case that failed).

- [x] **Port ccbot's EPHEMERAL STATUS LINE — the live "Claude is working" verb (+ shell count), as a self-deleting message.** Liav, 2026-07-13: *"ccbot showed the 'claude working verb' and 'x shells running' in a 'disappearing' text, can we port this?"* On a phone you currently cannot tell a **thinking** agent from a **dead** one — chela relays only finished turns. ccbot showed Claude Code's own status line (`✻ Boondoggling… (2m 45s · ↓ 12.0k tokens)`) as a single message that **edits in place** while the agent works and **deletes itself** when the turn ends. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  ℹ️ **Hooks cannot provide this and never will** — the working verb is a **TUI render**, not an event (there is no `Boondoggling` hook). Unlike the interactive gates, **the pane is the correct source here permanently.** Do not "modernize" this onto the event log.
  1. **Port the parser** (`chela/telegram/panescan.py`, next to the gate detectors). ccbot's `parse_status_line` — `~/projects/ccbot/src/ccbot/terminal_parser.py:202` — is the reference and its **anchoring is the whole trick**: find the **chrome separator** (the run of `─` in the last ~10 lines) and read the line(s) just **above** it, returning the text after a leading spinner glyph (`· ✻ ✽ ✶ ✳ ✢`). ⛔ Do **not** grep the pane for a spinner char — Claude's normal output is full of `·` bullets, and that is exactly the false-positive this anchoring exists to avoid. **Also: verify empirically what "x shells running" actually is on Claude Code 2.1.207** — capture a live pane with a background shell and look. If it is part of the same status line, it comes along free; if it is separate chrome, decide whether it is worth a second parse and say so. Don't guess.
  2. **Ride the EXISTING per-tick pane capture — add no tmux calls.** `gatewatch.poll()` (`chela/telegram/gatewatch.py:354`) already captures every bridged window's pane once per tick for the three gate detectors. The status line is a fourth read of the **same** text. A separate poller would double the tmux load on the whole fleet for nothing.
  3. **Reuse the tracked-message machinery — the status is a new "kind".** `gatewatch` already posts / **edits in place** / **poofs** a tracked message per window per prompt kind (the `_uq_msg` / `_plan_msg` pattern, `~393` post-or-edit, `~436` clear-and-poof). A status message is the same shape with a different lifecycle: **post on first sight → edit while the verb changes → DELETE when the status line leaves the pane** (the turn ended). That deletion is the "disappearing" behavior and is the point — a topic must not accumulate dead status messages.
  4. **THROTTLE, or Telegram will rate-limit the topic.** The status line ticks roughly every second. ccbot caps edits with a `STATUS_EDIT_MIN_INTERVAL` per chat and **skips the edit entirely when the text is unchanged** — port both. A dropped update is **fine** (the next poll re-sends the latest); a 429 that wedges the relay is not. Also port the **typing indicator**: `sendChatAction(TYPING)` while the pane says `esc to interrupt` — that is what makes a phone feel live.
  **Landmines:** a status edit that fails (429 / message deleted / "not modified") must be **swallowed**, never propagated — this is decoration, and it must never wedge the relay or delay a real message; the existing 429 `retry_after` loop must not be bypassed *or* allowed to block on a status edit (decide which, and say why); **poof only the status message** — do not touch the gate/question/plan trackers, which have their own lifecycle; if the agent's window changes under a topic, delete the old status and post fresh (ccbot does this — `message_queue.py:477`); the status must not be the *last* thing a user sees in a topic after it self-deletes (make sure a real assistant message still lands normally); an **idle** agent has no status line — that is the delete trigger, not an error. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (port from):** ccbot `src/ccbot/terminal_parser.py:196-240` (`parse_status_line`, `STATUS_SPINNERS`) and `src/ccbot/handlers/message_queue.py:470-575` (the tracked status message: edit-in-place, throttle, dedup, typing indicator, delete-on-clear, window-change). **In chela:** `chela/telegram/panescan.py` (put the parser here), `gatewatch.py` (`poll` ~354, the post/edit/poof tracker ~393/~436), `relay.py` (`edit` ~492, and the delete path). **Verify:** unit-test the parser against real captured panes — a working pane (verb extracted), an **idle** pane (None), a pane whose output contains `·` bullets (**None** — the false positive that motivates the anchoring), a pane with no chrome (None); the throttle (no edit inside the interval), the dedup (unchanged text → no API call), delete-on-idle, and that a failed edit is swallowed. **LIVE (mandatory):** watch a real agent work from the phone — the verb appears, **updates as it changes**, and **vanishes when the turn ends**, with no leftover message in the topic and no rate-limit errors in `pm2 logs chela-telegram`.

- [x] **ENV IS THE SINGLE SOURCE OF TRUTH — config must not also live in the PM2 file or in CLI args.** Liav, 2026-07-13: *"env should be single source of truth and not also in pm2 config file."* Today config is scattered across three places and they have already drifted: (a) `~/.chela/ecosystem.config.js` duplicates `CHELA_TMUX_SESSION` / `CHELA_DIR` into **three separate `env:` blocks** — and **all three still said `'ccbot'`** after the tmux session was renamed to `chela` (a clean `pm2 start ecosystem.config.js` would have brought the whole fleet up against a session that no longer exists); (b) the dashboard port is a **CLI arg** (`--port 5005`) which `main.py` writes into `os.environ` **inside that process only**; (c) `run-telegram.sh` already does it right (`set -a; source ~/.chela/telegram.env; set +a`). **This is not cosmetic — it is why CMX-41 is broken:** `config.dashboard_port()` (the "single authority" for the port the hook plugin POSTs to) is a **different process** from the dashboard, so it reads no `CHELA_DASHBOARD_PORT`, falls back to `5001`, and `chela plugin` renders `http://127.0.0.1:5001/hooks/…` while the dashboard actually serves **5005**. Every hook then POSTs into a **closed socket**, fails open, and **the whole hook feature silently does nothing**. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **One env file, sourced by everything.** A single `CHELA_DIR/chela.env` (keep `telegram.env` separate for the **secret**, or fold it in and document the permissions — decide and say why) holding `CHELA_TMUX_SESSION`, `CHELA_DIR`, `CHELA_DASHBOARD_PORT`, `CHELA_TERMINALS_*`, `CHELA_IGNORE_WINDOWS`. Every PM2 app sources it via a thin launcher (`run-telegram.sh` is the pattern that already works) instead of carrying its own `env:` block. **Ship the ecosystem template + the launcher in the repo** so a fresh install gets it right; `chela` must still run correctly from a plain shell with nothing exported (documented defaults).
  2. **The port must be discoverable ACROSS processes — that is the actual bug.** `CHELA_DASHBOARD_PORT` in the env file becomes the authority; the dashboard **binds what the env says** (a `--port` flag may still override for a one-off, but then it is NOT the source of truth and `chela plugin` must not be fooled by it). Belt-and-braces: have the dashboard **publish the port it actually bound** to a small state file under `CHELA_DIR` at startup, and have `config.dashboard_port()` prefer that over the default — so the manifest is right even when someone passes `--port` by hand. Whatever you choose, **`chela plugin` must render the port the dashboard is really listening on**, and a mismatch must be **loud**, not silent.
  3. **Make the drift impossible to repeat.** Add a check (a `chela doctor`-style command, or a startup warning) that fails loudly when the config a service is *running* with disagrees with the env file — that is the exact failure that let `CHELA_TMUX_SESSION: 'ccbot'` sit in the restart path for a day while the live processes said `chela`.
  **Landmines:** ⚠️ **`CHELA_TMUX_SESSION` is LOAD-BEARING** — `config.current_session()` resolves `$CHELA_TMUX_SESSION` → **auto-derive from `$TMUX_PANE`** → `"chela"`, and a stale `TMUX_PANE` leaking through PM2 makes a service silently target a **`webterm_*` mirror** session. Do not "simplify" the override away; start services with `env -u TMUX -u TMUX_PANE`. **`pm2 restart --update-env` MERGES env, it does not delete a removed var** — clearing one truly needs `pm2 delete` + a fresh `pm2 start` from a non-tmux shell, so document the migration. Don't break `chela-telegram` (it is **missing from `ecosystem.config.js` entirely** today, and the daemon is named `chela` there but `chela-daemon` in the live process list — both are drift). The secret (`TELEGRAM_BOT_TOKEN`) must not land in a world-readable file or in git. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/config.py` (`CHELA_DIR`, `current_session`, `dashboard_port`), `chela/main.py` (~911 — where `--port` writes `os.environ` in-process only; ~1096), `chela/dashboard/app.py` (~2893 — the other `5001` default), `chela/hooks.py` + `chela plugin` (the consumer that is broken by this), `~/.chela/run-telegram.sh` (the pattern that already works), `~/.chela/ecosystem.config.js` (the drift — a backup is at `ecosystem.config.js.bak-2026-07-13`; the live file's session pins were hand-fixed to `chela` on 2026-07-13). **Verify:** unit-test that `dashboard_port()` returns the port the dashboard actually bound, from a **separate process**; that a `--port` override still renders correctly in the manifest; that a missing env file falls back to documented defaults without crashing. **LIVE (this is the whole point):** with the real dashboard on **5005**, run `chela plugin` and confirm the rendered manifest says **5005**, not 5001 — then POST a hook to it and see the event land in `chela events`.

- [x] **EVENT LOG — part A: the store, the read API, the CLI. (No hooks, no plugin, no UI — those are part B and later.)** chela has nowhere for an event to *live*: `chela msg` / `broadcast` fire into a pane and vanish, watches exist only as keys in `inbox.json`, the queue is transient. That is the root cause behind every inbox bug fixed today — the false-`DIED` (state inferred, not observed), the stale `awaiting_review` (a snapshot with no expiry), the essay-as-notification (no title/payload split). **You cannot render, filter, thread, or reconcile prose.** PR #51 already made an inbox event a **record** (`kind` + `summary` + `payload`) — that is the shape this log stores. Build the log as a standalone subsystem with a **programmatic append**, so it is fully testable with zero Claude Code involvement; part B then feeds it from hooks. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **The store: append-only JSONL + monotonic `seq` + `boot_id`.** Append to `CHELA_DIR/events.jsonl`, one JSON object per line (the audit trail). Each record: **`seq`** (monotonic, process-local), **`boot_id`** (changes on restart — so a stale cursor is *detectable* rather than silently wrong), `ts`, **`type`**, **`wid`** (the chela window, when known), `session_id`, a one-line **`summary`**, and a structured **`payload`**. Mirror PR #51's record shape — do NOT invent a second event schema. Keep a bounded in-memory ring (~2000) for live reads; older events stay in the JSONL. **Rotate the file** — an unbounded append on a busy fleet is a disk-filler.
  2. **The read API: cursor-based, filterable, resumable.** `chela events [--after-seq N] [--type X] [--wid @N] [--follow]` — replay from a cursor, filter, tail live. On a **`boot_id` change, report a resume gap** rather than pretending the cursor is valid. This is the one authority the dashboard timeline and the inbox will both consume — do **not** add a second event source alongside it.
  3. **A concurrency-safe append.** Multiple writers (daemon, dashboard, CLI, and later N hook calls) append at once. Use an O_APPEND write of a single line (atomic on POSIX for small writes) or a short-held lock — but **never hold a lock across other I/O**, and never let a writer block. Decide and document which, and prove it: hammer it from N concurrent processes and assert every line is intact and `seq` has no duplicates/gaps.
  **Landmines:** don't build the UI, don't build hook ingestion, don't touch `~/.claude/`; the record shape must match PR #51's (`chela/inbox.py`) or the log and the inbox will drift apart; a corrupt/truncated final line (a crash mid-append) must be **skipped on read, not fatal**; `seq` must survive a restart (derive it from the file's last line, or scope it per `boot_id` and say so); don't regress `chela watching` / the inbox. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/inbox.py` (the event record from PR #51 — `kind`/`summary`/`payload`; and the inbox is the log's first consumer), `chela/config.py` (`CHELA_DIR`), `chela/agent_manager.py` (window ↔ cwd ↔ pid correlation). Prior art: Mosaic `docs/events.md` (`seq` / `boot_id` / `after_seq` / categories / heartbeats + a JSONL audit) — same shape, independently arrived at. **Verify:** unit-test append → read → cursor → filter; a `boot_id` change surfaces a **resume gap**; a truncated final line is skipped, not fatal; `seq` is unbroken across a restart; the concurrency hammer above. **Live:** append real events from two processes at once, then `chela events --follow` in a third and show them arriving in order.

- [x] **EVENT LOG — part B: feed it from Claude Code HOOKS, shipped as a PLUGIN. (Depends on part A.)** Hooks deliver *typed, structured* events **before** the fact — unlike the transcript, which only lands interactive records at resolution (the reason the current gates are pane-scraped). ⚠️ **Scope: ingestion only. This slice is OBSERVE-ONLY — it must NOT answer any gate.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  **PROVEN GROUND (measured 2026-07-13, Claude Code 2.1.207 — do not re-derive, do not doubt, and do NOT trust the docs over this):** `AskUserQuestion` / `ExitPlanMode` are **not** hook events — they are **TOOLS**, and `PreToolUse` / `PermissionRequest` fire for every tool carrying `tool_name` + the full `tool_input`. **`PermissionRequest` fires for `AskUserQuestion` in EVERY permission mode, including `--permission-mode auto`** (what the dispatcher uses) — auto does *not* auto-answer a question, it genuinely blocks on the picker. Payloads carry `session_id`, `transcript_path`, `cwd`, `prompt_id`, `permission_mode` (+ `tool_use_id` on `PreToolUse`, **not** on `PermissionRequest`). Hook events include: `PreToolUse`, `PostToolUse`, `PermissionRequest`, `PermissionDenied`, `UserPromptSubmit`, `SessionStart`, `SessionEnd`, `Stop`, `SubagentStart`, `SubagentStop`, `Notification`, `PreCompact`, `PostCompact`, `Elicitation`.
  1. **🔌 SHIP A CLAUDE CODE PLUGIN — do NOT edit the user's `settings.json`.** A plugin can carry hooks: declare them in `hooks/hooks.json` (or inline in `.claude-plugin/plugin.json`), with paths via **`${CLAUDE_PLUGIN_ROOT}`**. **Hooks MERGE additively across all scopes** — plugin hooks fire *alongside* the user's own rather than overriding them — so chela never opens, backs up, or risks corrupting a settings file holding hundreds of the user's permission entries. Install: `/plugin marketplace add <repo>` + `/plugin install chela`. Build the plugin skeleton + document the install. (Plugin hooks have the **lowest precedence** and fire after user/project hooks — fine for observe-only.)
  2. **Prefer the `http` hook type — the daemon is already listening.** `{"type": "http", "url": "http://127.0.0.1:<dashboard port>/hooks/<event>", "timeout": 2}` POSTs the event JSON straight to chela's running process: **no shell script, no process spawn per event, no PATH assumptions.** Add the receiving endpoint; it appends via part A's API and returns immediately.
  3. **Correlate the event to a window WITHOUT the pane.** Use `cwd` / `session_id` against `agent_manager` (the `/api/agents` authority). If you find yourself scraping a pane to identify an event, you have reinvented the thing this replaces.
  **Landmines:** 🔒 **do NOT write `~/.claude/settings.json` at all** — the plugin mechanism exists precisely so you don't have to; if you find yourself opening that file, stop. **A hook runs SYNCHRONOUSLY inside the agent's process** — a slow or crashing hook **stalls or breaks a live agent**: short explicit `timeout` (~2s), append-and-return, no lock held across I/O, **fail OPEN if the daemon is down** (agent proceeds, event lost — never wedge the agent). ⚠️ **OBSERVE-ONLY:** return **no** `hookSpecificOutput` and **no** `permissionDecision` — printing a decision would silently start auto-answering the user's permission prompts. Only exit code `2` blocks a tool call; a command-hook with stray stdout (a chatty `.bashrc`) corrupts the JSON contract — another reason to prefer `http`. **Hooks are read at agent STARTUP**, so an already-running fleet has none → **keep the pane-scraped gates as the fallback**; do not delete them. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** part A's store/API; `chela/dashboard/app.py` (where the endpoint goes); `chela/agent_manager.py` (correlation); `chela/telegram/{panescan,gatewatch,interactive}.py` (the substrate this will eventually replace — **not in this slice**). **Verify:** unit-test the endpoint appends a well-formed record and returns fast; that a malformed payload is dropped without raising; that the daemon being **down** does not wedge the caller. **LIVE (mandatory):** install the plugin into an **ISOLATED** settings/config (⛔ NOT Liav's real `~/.claude/` — he runs `/plugin install` himself), run a real agent, drive a real `AskUserQuestion` + a real Bash gate + a session start/stop, and show the resulting `events.jsonl` lines — **with the AskUserQuestion payload's per-option `label` and `description` present in the log**. Then confirm a normal agent still runs **unaffected** with the hooks installed (no stall, no error).

- [x] **`WORKFLOW.md` is read once at startup — an edit needs a restart, and a BAD edit takes the daemon down.** Symphony (the spec chela's workflow format is derived from — `github.com/openai/symphony`, `SPEC.md` §6.2) makes this a **MUST**: *"The software MUST detect `WORKFLOW.md` changes. On change, it MUST re-read and re-apply workflow config and prompt template without restart"* and *"invalid reloads MUST NOT crash the service; keep operating with the last known good effective configuration and emit an operator-visible error."* chela does neither: `chela run` (PM2 `chela-daemon`) parses the workflow at boot, so changing `polling.interval_ms`, `concurrency.max`, `agent.cmd` or the prompt body means a restart — and a YAML typo means a **broken daemon**, not a skipped tick. This matters more now that the dispatcher runs unattended and writes `dev`. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Detect and re-apply on change, without restart.** Re-read `WORKFLOW.md` when its mtime/hash changes and re-apply the config + prompt template to the *next* tick. Per the spec, an in-flight agent is **not** restarted on a config change (`"Implementations are not REQUIRED to restart in-flight agent sessions automatically"`) — the new config governs the next dispatch. The effective poll interval should be updated when the config is re-applied.
  2. **Keep last-known-good on a bad reload — never crash the loop.** A YAML/parse error must leave the daemon running on the last valid config, emit an **operator-visible** error (log + surface it on `/api/settings` or the dashboard, not just a log line nobody reads), and **block new dispatches until fixed** while **reconciliation keeps running** (`"Workflow file read/YAML errors block new dispatches until fixed. Template errors fail only the affected run attempt."` — note the distinction: a bad *template* fails one run, a bad *config* pauses dispatch).
  **Landmines:** don't reload mid-dispatch (apply at a tick boundary, not inside `_spawn`); a config change must not orphan or re-key in-flight runs; don't re-read the file on every tick if nothing changed (hash/mtime gate — this runs every 30s forever); the daemon must survive a **partially-written** file (an editor saving) — treat a parse failure as "keep last good", not "the workflow is now empty"; don't regress `--dry-run` or `--once`. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (workflow load + the tick loop), `chela/main.py::cmd_run` (the daemon loop — where a reload check belongs), `WORKFLOW.md` (the front-matter schema). Prior art: Symphony `SPEC.md` §6.2 (dynamic reload) + §6.3 (startup and per-tick validation). **Verify:** unit-test that a changed workflow is re-applied without a restart; that a **broken** workflow leaves the last-good config in force, blocks dispatch, keeps reconciling, and surfaces the error; that an unchanged file is not re-parsed every tick. **Live:** with the daemon running, edit `concurrency.max` and confirm the next tick honors it with no restart; then introduce a YAML syntax error and confirm the daemon **stays up**, keeps reconciling, refuses to dispatch, and says why.

- [x] **Any assistant message over ~4096 chars renders as RAW MARKDOWN on Telegram — the 4096 split cuts an inline entity in half.** CONFIRMED live 2026-07-13 (Liav's screenshot, `nautilus` topic: literal `**zero funding**` and `## 3. The funding "tailwind"` on screen). Bridge logs give the exact cause: `Bad Request: can't parse entities: Can't find end of Bold entity at byte offset 4316` and `… Can't find end of Code entity at byte offset 4135`. **Mechanism:** `relay.split_message` chunks at `MAX_LEN = 4096`, each chunk is posted with `parse_mode=MarkdownV2`, and a chunk whose boundary lands inside `*bold*` / `` `code` `` ends with an **unterminated entity** → Telegram rejects it → the relay's fallback re-sends the **whole message** as plain text (`parse_mode=None`), so the reader sees raw `**` and `##`. The splitter **already** handles UTF-16 lengths (`_utf16_len`) and code **FENCES** (`_FENCE`/`_FENCE_RESERVE` — it appends a closer and reopens on the next chunk) — it just never does the same for **inline** entities. Short messages format fine, which is why this looked random: **the trigger is length**. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **Split on entity-safe boundaries.** In `chela/telegram/relay.py::split_message`, prefer to break at a **blank line, then a newline, then a space** at/below the limit, rather than mid-token — a paragraph break can't sit inside an inline entity. This alone fixes the common case and is the cheapest correct move. Keep the existing UTF-16 accounting (`_utf16_len`) — the limit is UTF-16 code units, not Python chars, and the observed byte offsets (4135, 4316) are past 4096 precisely because of multi-byte characters.
  2. **Balance inline entities across a boundary, the way fences already are.** If a chunk still ends inside an open inline entity (`*bold*`, `_italic_`, `__underline__`, `~strike~`, `||spoiler||`, `` `code` ``), **close it at the end of the chunk and reopen it at the start of the next** — the exact trick `_FENCE`/`_FENCE_RESERVE` already performs for ``` fences; generalize that, don't special-case bold. Reserve room for the closer so the reopened chunk still fits (mirror `_FENCE_RESERVE`). Remember `render_markdown`/`telegramify-markdown` escapes text first, so an entity marker may be **escaped** (`\*`) and must NOT be treated as an opener — parse what is actually emitted, not the source markdown.
  3. **Make the fallback per-CHUNK, not whole-message.** Today one rejected chunk downgrades the entire message to plain text (`BotSender.send` returns False on the first failure and the caller re-sends everything unformatted). A single bad chunk should not strip formatting from the other five. Fall back only for the chunk that failed. (Keep the existing 429 / `retry_after` behavior intact — a flood-control 429 is NOT a rejection and must still sleep-and-resend the same payload.)
  **Landmines:** don't regress the fence handling, the 429 retry loop, or the `reply_markup`-on-first-chunk rule (`markup_json and i == 0`); the limit is **UTF-16 code units** — do not "simplify" to `len(text)`; an entity closer you inject must itself be counted against the limit; don't break `skills/telegram-send/send.py`, which shares the same split approach (check whether it should be fixed too or left alone); plain-text fallback must remain byte-safe. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/relay.py` (`MAX_LEN` ~35, `_FENCE`/`_FENCE_RESERVE` ~43-47, `split_message`, `BotSender.send` ~282-306 — the `if not resp.get("ok")` early-return is the whole-message fallback), `chela/telegram/format.py` (`to_markdown_v2` ~199, `render_markdown`, `escape_markdown_v2`). **Verify:** unit-test that a >4096-unit body containing bold/inline-code spanning the boundary produces chunks that are each **individually valid MarkdownV2** (every entity opened in a chunk is closed in it); that a fence spanning the boundary still works; that one rejected chunk no longer downgrades the others. **LIVE (mandatory — this bug was invisible to a green suite):** relay a real >4096-char assistant message containing `**bold**`, `` `code` `` and a `##` heading through the actual bridge and confirm on Telegram that it renders **formatted**, not as raw `**` — reproduce with the nautilus-style long report that triggered it.

- [x] **Inbox events are a PROSE SNAPSHOT, not a record: they go stale before delivery, and the notification text is the entire TODO item.** Two defects, one root cause — an event is a formatted string built at queue time, so it can be neither re-validated nor rendered. Both observed live 2026-07-13. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Re-validate an event at DELIVERY time — a queued event is a claim about the past.** Observed: `📥 run aeaae296a725 (…) is awaiting review — <PR #47 url>` was delivered to the orchestrator **after that PR had already been reviewed and merged**. The event was queued when the PR opened (true then), but delivery is deliberately deferred until the orchestrator is `idle` — and by then the world had moved. So the orchestrator is handed a stale instruction to do work that is already done. **Fix:** before pushing a queued event, re-check the fact it asserts (run state / PR state) and **drop or supersede it if it no longer holds** (a merged PR is not awaiting review). This is distinct from the false-`DIED` bug (state *inferred* rather than observed) — here the state was right when queued and rotted in the queue. Note the inbox already re-resolves the orchestrator every tick; the *events* get no such treatment.
  2. **Give an event a TYPE, a SUMMARY and a PAYLOAD — stop shipping the whole task body as the notification.** Observed: the `awaiting_review` event's text is the **entire multi-paragraph TODO item** (the full "Make the dispatcher's default agent launch mode editable…" brief, landmines and all) pasted into the orchestrator's window as a notification. A notification needs one line; the essay belongs in a payload the reader can open. Today `queue` holds `{"kind", "text", "wid", "clear_watch"}` where `text` is a pre-rendered string — so nothing downstream (a UI, a filter, a de-dup) can do anything with it but print it. Restructure the queued event into a record: **`kind`** (already there), a short **`summary`** (≤1 line — e.g. `cmx-34 awaiting review — PR #47`), and a **`payload`** (run id, wid, PR url, task title, timestamps). Render the one-liner for the tmux push; keep the payload for the log/UI that will consume it next. **This is the first brick of the event log** — do not build the log here, but do not build a shape the log will have to throw away either.
  **Landmines:** don't break the `idle`-only push gate (never push into a `waiting` agent — it would answer a permission prompt with the notification text); a re-validation must not become a slow network call inside the store lock (`tick()` already keeps the critical section short — do the check OUTSIDE it, like the status probes and the runs query); dropping a stale event must be logged, not silent, or a real event lost to a bug becomes undebuggable; keep the existing `runs_seen` de-dup working. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/inbox.py` (`tick` ~328, `run_events` ~263, the queue records + `_line()` formatter, `runs_seen`), `chela/dispatcher.py` (`list_runs` — run state + PR url). **Verify:** unit-test that an `awaiting_review` event whose PR is merged before delivery is **dropped (and logged)**, not pushed; that a still-open PR is pushed normally; that a queued event carries `kind` + a one-line `summary` + a structured `payload`, and the tmux push renders only the summary. **Live:** dispatch a task, merge its PR *before* going idle, and confirm the orchestrator is NOT handed a stale "awaiting review" for it.

- [x] **EVERY dispatched PR conflicts on `TODO.md` — the agent and the orchestrator are two writers on one file.** Hand-resolved **three times** on 2026-07-13 (cmx-33, cmx-34, and again after). `WORKFLOW.md`'s done-criteria tell the agent to strike its own `- [ ] → - [x]` line **in its branch**, while the orchestrator keeps appending new items to `dev` behind it. Both edits land at the top of the same file, so the merge conflicts every single time — a guaranteed, self-inflicted tax on every dispatch, and it will get worse as the queue deepens. The tracker is also the *only* reason most PRs touch `TODO.md` at all. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Make the DISPATCHER own the strike — the agent must not touch `TODO.md`.** Remove the "strike the TODO line in your branch" step from `WORKFLOW.md`'s done-criteria (and the `{{task_file_relative}}` plumbing that supports it). Instead, the dispatcher marks the item `- [x]` **on `dev`** when it reconciles the run to `done` (the PR merged) — it already tracks that transition (`reconciled_done` in the tick counters). One writer, no conflict, and the checkbox now reflects *merged*, not *the agent believed it finished*, which is strictly more truthful. Match the task by its stable **task id** (the dispatcher already hashes it), not by fuzzy title text — titles get edited.
  2. **Decide the failure mode explicitly and document it.** If the dispatcher writes `dev` directly it must handle: a dirty/ahead `dev` (fetch+rebase or skip and retry next tick — do NOT force); a task whose line was edited or removed by a human meanwhile (skip, log, don't guess); and two runs reconciling in the same tick (batch into one commit). It must **never push to `dev` if the strike is the only change and it conflicts** — log and leave it for a human. A missed checkbox is cosmetic; a mangled `dev` is not.
  **Landmines:** don't regress `chela dispatch --dry-run`; don't let the dispatcher's `dev` write race the orchestrator's own commits (it runs under PM2, unattended — pull before write, never force-push, never touch anything but the one checkbox line); the tracker abstraction (`tracker.kind: markdown`) should stay pluggable — do not hardcode `TODO.md` paths into the dispatcher; an agent that still edits `TODO.md` out of habit must not break the merge (the strike being idempotent is fine). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `WORKFLOW.md` (done-criteria step 4 — the strike instruction + `{{task_file_relative}}`), `chela/dispatcher.py` (the reconcile path that sets a run `done` / `reconciled_done`, the markdown tracker + task-id hashing). **Verify:** unit-test that reconciling a merged run strikes the right line by task id on `dev`, that an already-struck line is a no-op, that a missing/edited line is skipped-and-logged rather than guessed, and that two runs reconciling together produce one commit. **Live (the whole point):** dispatch a task, merge its PR, and confirm (a) the PR's diff does **not** touch `TODO.md`, (b) the item is struck on `dev` afterwards, and (c) **the merge had no conflict** — while a *second* new item was added to `dev` during the run.

- [x] **The inbox reports a SUCCESSFUL agent as `DIED mid-task` — it infers death from "window gone" and cannot tell completion from a crash.** Fired **twice** on 2026-07-13, both times about work that had already succeeded and been merged: `📥 @6 (@6) DIED mid-task (window gone) — the work was not finished.` (CMX-32 → PR #45, merged) and the same for `@11` (CMX-33 → PR #46, merged). Each time the inbox **contradicted itself in its own queue** — it also queued the correct `… is awaiting review — <PR url>` event for the same run. A dispatched agent that finishes normally *exits*, and its tmux window goes away; the watcher reads that disappearance as death. So the one message a human is most likely to act on ("your agent died, the work is unfinished") is **false precisely when everything went right**. It also leaves a stale watch behind. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**

- [x] **Make the dispatcher's default agent launch mode editable in Settings (the FIRST editable setting — there is no write path yet).** Today the launch command is hard-wired in `WORKFLOW.md` (`agent.cmd: claude --permission-mode auto`, `chela/dispatcher.py` ~835), so changing how dispatched agents are launched means editing a YAML front-matter file by hand. Liav wants it editable from Settings. ⚠️ **Two constraints are load-bearing — read them before designing.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **🔒 EXPOSE A MODE ENUM, NOT A FREE-TEXT COMMAND.** `agent.cmd` is a shell command that gets executed to spawn an agent. The dashboard is served over the tailnet. **Making that string editable in a web UI is remote code execution by design** — do NOT add a text input for it. Expose only the **permission mode**, as a constrained enum, with the rest of the command line fixed and built in code. Enumerate the *actually valid* modes from the installed CLI (`claude --help`; do not guess or hardcode from memory — the set includes at least `default`, `acceptEdits`, `auto`, `bypassPermissions`, `plan`, but VERIFY) and reject anything not in that set **server-side**, not just in the UI. A rejected value must fail closed (keep the current mode), never fall through to a shell.
  2. **Settings is READ-ONLY today — build the write path.** `/api/settings` (`chela/dashboard/app.py` ~1437) is GET-only and there is no settings UI module in `chela/dashboard/static/js/` (the drawer is rendered elsewhere — find it). Add a persisted store (a small JSON under `CHELA_DIR`, mirroring how `~/.chela/inbox.json` / `telegram-bindings.json` are handled — **not** a new database) plus a write endpoint. Keep the store generic enough that the *next* editable setting doesn't need a second mechanism, but do not build a settings framework — one key, done well.
  3. **Define PRECEDENCE explicitly and document it in the code.** `WORKFLOW.md` currently sets `agent.cmd` outright, so a Settings value would be silently ignored. Proposed order (state it in a docstring): **explicit `agent.cmd` in WORKFLOW.md wins** (a per-workflow override stays authoritative) **→ else the Settings permission mode → else the built-in default**. That means `WORKFLOW.md`'s `cmd:` line should be **removed** (or reduced to no `--permission-mode` flag) so the Settings value is actually reachable — otherwise the feature is dead on arrival and the UI lies. Whichever precedence you choose, **prove it with a test** and make the UI show which source is currently winning.
  **Landmines:** the RCE point in item 1 is the whole reason this task is worded this way — a free-text `agent.cmd` field would be a serious vulnerability, do not add one "for flexibility"; a mode change must only affect **newly dispatched** agents (a running agent's mode is fixed at startup — do not try to mutate a live agent); don't regress `chela dispatch --dry-run`; the dispatcher is run by PM2 **from disk** (`chela-daemon`), so a stale/invalid settings file must not crash the daemon loop — read defensively, fail to the built-in default. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (`agent_cmd` ~828-835 + the comment block explaining the auto default), `WORKFLOW.md` (`agent:` block ~19), `chela/dashboard/app.py` (`/api/settings` ~1437), `chela/config.py` (`CHELA_DIR`), `chela/dashboard/static/js/` (find the Settings drawer — it is not its own module yet). **Verify:** unit-test that an unknown/malicious mode is rejected server-side and the default is kept; that precedence resolves as documented; that a changed setting persists across a daemon restart. **Live:** change the mode in Settings, dispatch a real task, and confirm from the spawned agent's actual command line (`ps`/pane) that it launched in the new mode — and that an already-running agent was untouched.

- [x] **`tests/test_inbox.py` reads the LIVE runs DB — 9 tests fail whenever a real agent is mid-flight.** Found 2026-07-13 while verifying CMX-32: the suite reported `9 failed, 482 passed` on a machine with one dispatched run in `awaiting_review`, and the SAME 9 fail on `dev` (so it is not CMX-32's doing). Root cause: `inbox.tick(prev, runs=None)` falls back to `from chela import dispatcher; runs = dispatcher.list_runs()` (`chela/inbox.py` ~341), and the tests call `inbox.tick({AGENT: BUSY})` **without passing `runs`** — so they query the **real** dispatcher runs database. The `store_file` fixture isolates the inbox JSON (`CHELA_INBOX_FILE`), the session statuses, and `send_tmux` — but **not** the runs DB. With a live run awaiting review, `run_events()` synthesizes a real `awaiting_review` event into the test's tmp store and every count assertion is off by one (`assert 2 == 1`). **These tests are GREEN BY LUCK: they pass only when the machine is idle and fail precisely when chela is being used** — the third instance of this repo's signature bug (cf. the C1 permission gate and the A2 mid-render double-relay, both green while broken live). ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Isolate the runs DB in the fixture.** In `tests/test_inbox.py`, make the `store_file` fixture (or a new autouse fixture in that module) monkeypatch the live query so it returns nothing by default — e.g. `monkeypatch.setattr(dispatcher, "list_runs", lambda: [])` — so `tick()` can never reach the real database. Tests that WANT run events should pass them explicitly via `tick(prev, runs=[...])` (the parameter already exists — that is the seam; use it). Proven diagnosis: with `CHELA_DIR` pointed at an empty scratch dir, the same 9 tests pass (23/23 on `dev`, 491/491 on cmx-32) — but do NOT fix it by requiring an env var, fix it in the fixture so a bare `uv run pytest -q` is hermetic.
  2. **Then sweep for the same leak elsewhere.** Any other test that calls into `inbox`, `dispatcher`, or `agent_manager` without isolating the live state (`~/.chela/*.db`, `~/.chela/inbox.json`, `claude agents --json`, tmux) has the same failure mode. Grep for the live-state entry points and confirm each test either isolates them or passes them in. Add a short comment in the fixture recording WHY the isolation exists (a future refactor will otherwise "simplify" it away and restore the bug).
  **Landmines:** don't just delete or xfail the failing assertions — the assertions are RIGHT, the fixture is wrong; don't make hermeticity depend on the caller exporting `CHELA_DIR` (a bare `uv run pytest -q` on a busy machine must pass); don't weaken the real `tick()` fallback to `dispatcher.list_runs()` — that default is correct in production, it is only the TEST that must not reach it. Keep `uv run ruff check chela tests` green. **Reference:** `chela/inbox.py` (`tick` ~328-343, `run_events` ~263), `tests/test_inbox.py` (the `store_file` / `sends` / `windows` fixtures ~29-45), `chela/dispatcher.py` (`list_runs`). **Verify:** the decisive test is environmental, not a unit test — **dispatch a real run (or hand-insert a row so a run sits in `awaiting_review`) and confirm `uv run pytest -q` is fully green with it in flight**, then confirm it is still green with an idle machine. State both results in the PR. A suite that only passes on an idle machine has not been fixed.

- [x] **AskUserQuestion is UNANSWERABLE on a phone — the option text exists ONLY as a truncated button caption.** CONFIRMED live 2026-07-13 (screenshot): a 4-option question relayed as `❓ <question>` + four buttons that Telegram truncated to one line each — *"Rotate the group, skip the …"*, *"filter-repo + ask GitHub …"*, *"Fresh public repo,"*. The human got four half-sentences and **none** of the reasoning, on a decision that hinged entirely on the reasoning. Root cause is structural, not cosmetic: `gatewatch.format_askuq_message` deliberately puts **only the question** in the body ("the answer buttons carry the options"), and `interactive.scraped_reply_markup` renders each option label as a **button caption** — the one place in Telegram that hard-truncates to a single line. So the labels are simultaneously the only carrier of the options and the one surface that can't display them. This lands directly on the "decisions inbox = killer UX" thesis: the orchestrator asks the human to decide while structurally withholding the basis for the decision. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–3 + Landmines + Verify.**
  1. **Move the options into the message BODY, numbered, in full.** Rewrite `format_askuq_message(uq)` (`chela/telegram/gatewatch.py` ~172) to emit the question followed by a numbered list of the scraped option labels at **full length** — e.g. `❓ <question>` then `1. <label>` / `2. <label>` … The body is the only surface that wraps, so this is what actually fixes readability. Keep it **plain text** (the send path does not escape MarkdownV2 for scraped content).
  2. **Reduce the buttons to compact selectors.** In `interactive.scraped_reply_markup` (`chela/telegram/interactive.py` ~219), stop using the label as the caption. Emit compact numbered buttons (`1`, `2`, `3`, …) that fit a phone's width without truncation, packed a few per row rather than one full-width row each, keyed to the SAME `qa:<i>` callbacks in the SAME order. Keep the `⎋` escape row (`_esc_row`). The body now carries the meaning; the buttons are just the selector.
  3. **Investigate — do per-option descriptions exist in the pane at all?** The `description`/`preview` fields never reach the bridge (the `tool_use` record only lands in the JSONL at answer-time — the reason this path is pane-scraped in the first place). **Establish empirically** whether the Claude Code TUI renders a description for every option or only for the cursor-focused one: capture a live pane with a real multi-option AskUserQuestion (`tmux capture-pane -p`) and look. **If** every option's description is on-screen, scrape it into `AskUQ` (`chela/telegram/panescan.py` ~274, alongside `options`) and indent it under its option in the body. **If only the focused option's description renders, STOP — do not fake, infer, or synthesize descriptions.** Ship items 1–2 (which already fix the reported bug) and write what you found into the PR description. Labels-in-the-body is the fix; descriptions are a bonus that may be impossible.
  **Landmines:** the `qa:<i>` **index mapping is load-bearing** — a tap injects `i - cursor` Down/Up presses to move the caret (`chela/telegram/inbound.py` ~589), so button *i* MUST still mean option *i*, in display order, with no reordering, no filtering, and no gaps; don't regress the **multi/multi-tab fallback** (`uq.multi or not uq.options` → `nav_only_markup()` — unchanged); don't regress **edit-in-place** or **poof-on-resolve** (`_uq_msg`) — note the body text now changes as options render mid-scrape, which is exactly what edit-in-place is for; respect Telegram's **4096-char** message cap (truncate a pathological option, don't drop it); `_askuq_signature` must still change when the scrape changes. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (`format_askuq_message`, `_askuq_markup`, `_askuq_signature`), `chela/telegram/interactive.py` (`scraped_reply_markup`, `_truncate`, `_esc_row`, `QA_CB_PREFIX`), `chela/telegram/panescan.py` (`AskUQ`, `detect_askuserquestion`), `chela/telegram/inbound.py` (`_tapped_label` ~562, the `qa:` tap handler + cursor-relative injection ~589–637). **Verify:** unit-test that the body contains every option label **in full** and numbered; that the keyboard is compact selectors whose `qa:<i>` callbacks preserve option order 1:1; that the multi fallback is untouched; that an over-long body is truncated, not dropped. **LIVE (mandatory — green tests have lied twice on this exact substrate: the C1 permission bug and the A2 mid-render double-relay both passed unit tests while broken in production):** trigger a real AskUserQuestion with 4 long option labels from a bridged agent and confirm on an actual phone that (a) all four option texts are fully readable, and (b) tapping selector `3` really selects option 3 — not option 2 or 4.

- [x] **Dispatcher seed-delivery robustness — CONFIRM delivery via busy status, don't hope.** The dispatcher spawns an agent, waits for `❯`, then `send_tmux(seed)` — but a late boot-splash redraw (the `⚠ MCP servers need authentication` line landing *after* `❯`) can swallow the paste, leaving the agent idle with **no task** (stuck CMX-26 for 22 min). A fixed sleep is exactly what the code already tried and warns against (*"send-keys lands mid-splash and is silently dropped"*). Use the **busy status** to verify the seed landed. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Verify.**
  1. **Verify-at-spawn via busy status:** right after `send_tmux(target_id, prompt)` in `_spawn` (`chela/dispatcher.py` ~772), poll the agent's native session status — `chela.agent_manager.session_status_map(force=True)` keyed by `agent_manager.claude_pid(window)` (the `claude agents --json` authority `/api/agents` uses: `"busy"` while working, `"idle"` at the prompt). If the agent flips **idle → busy** within a short window (~8s, polling every ~1s), the seed landed — done. If it stays **idle** past the window, the paste was dropped → **re-send the seed** (retry 2–3×, short settle-sleep between). Confirms delivery instead of hoping a delay was enough.
  2. **Tie in the existing watchdog:** the dispatcher already re-nudges a `running` row stuck at an idle empty prompt (`_pane_idle_empty_prompt` + `idle_nudged_at` + the `watchdog_renudged` counter) — but only on a reconcile *tick*, so between ticks a dropped seed sits idle (why CMX-26 waited 22 min). The verify-at-spawn loop catches it in seconds; optionally switch that watchdog's idle check to the same busy-status signal for consistency.
  **Landmines:** a freshly-booted agent reads "idle" until the seed makes it busy, so check for the idle→**busy** transition within a timeout (NOT just "is idle now"); use `session_status_map(force=True)` for a fresh read (it's cached); the seed is a long multi-line paste (send_tmux's paste-buffer path — the re-send goes through the same); **cap retries** so a genuinely-broken agent fails cleanly (no infinite loop). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (`_spawn` seed send ~772, `_pane_idle_empty_prompt`, the reconcile watchdog + `idle_nudged_at`), `chela/agent_manager.py` (`session_status_map`, `claude_pid`, `liveness`), `chela/messenger.py` (`send_tmux`). **Verify:** unit-test the verify-resend loop (mock status: stays idle → re-sends up to the cap then gives up; flips busy → stops). Live: spawn an agent; if the seed is dropped it re-sends and the agent starts within seconds instead of sitting idle.

- [x] **Slice C2 — permission Allow/Deny buttons + FIX C1's detection gating (it never fires while a gate is pending) + A2/B2 UX polish.** LIVE-TESTED 2026-07-12: C1's permission relay is **effectively broken**. `detect_permission_gate` works (confirmed on a live `ls /tmp` gate → "Do you want to proceed? 1.Yes/2.Yes-allow/3.No"), but `gatewatch` reads the pane for permission **only when the transcript has an unpaired `tool_use`** (docstring lines 10-12, 190-191) — and the gated command's `tool_use`, exactly like AskUserQuestion, is **only written to the JSONL at approval-time**. So while the gate is pending there is NO unpaired tool_use → the pane is never read → the `❓ Permission` message never posts. (C1's unit tests passed because they mocked that precondition.) Same class of bug A2 fixed. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **Fix the gating — make permission detection UNGATED (pane-triggered), like A2/B2.** In `chela/telegram/gatewatch.py`, run `detect_permission_gate` on the per-tick pane capture for **every** bridged window (remove the "only when an unpaired `tool_use`" precondition — it's false during a live gate). Enrich the command from the **pane gate region** (`detect_permission_gate` already returns the "Bash command / <cmd>" text), NOT the transcript's unpaired `tool_use` (absent while pending). Keep edge-triggered/de-dup + edit-in-place + clear-on-resolve (gate leaves the pane, or its `tool_result` lands).
  2. **Add the Allow/Deny keyboard** (the risk scope chosen earlier): `✅ Allow once` → Enter (`qa:nav:ent`, the default-highlighted option 1 "Yes"), `❌ Deny` → Escape (`qa:nav:esc`), showing the actual command in the message. **No one-tap "allow always"** — the human can arrow to option 2 via nav if wanted. **Validate empirically** (like B2) that Enter approves option 1 and Esc cancels/denies.
  3. **Apply the A2/B2 UX polish, consistently across AskUserQuestion + ExitPlanMode + permission** (one shared helper): (a) **trim the nav row to just ⎋ Esc** when semantic buttons are present — there's no visible caret on Telegram, so ↑↓⏎🔄 are redundant (keep the full nav only in the no-semantic-buttons fallback); (b) **"poof" on resolve** — delete the gate/question/plan message once answered (the `tool_result` is the record), mirroring ccbot `handlers/interactive_ui.py::clear_interactive_msg`.
  **Landmines:** ungated pane-read for permission adds a detector to the per-tick capture (fine — fleet is small, one capture already happens); edit-in-place tolerate "not modified" + fall back to fresh post; poof deletes ONLY the tracked interactive message; don't regress A2 (AskUserQuestion), B2 (ExitPlanMode), C1 detection, or the `k:` keyboard; plain-text send. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (remove the permission gating; mirror the AskUQ/ExitPlan ungated pattern + `_uq_msg`/`_plan_msg` edit-in-place + clear), `panescan.py` (`detect_permission_gate` — the gate text has the command), `interactive.py` (`_nav_row`/`NAV_KEYS` to trim, `qa:nav` scheme), ccbot `src/ccbot/handlers/interactive_ui.py` (`clear_interactive_msg` = poof). **Verify:** unit-test that a permission gate is detected + relayed with **NO unpaired `tool_use` present** (the live-broken case — the core fix); the Allow/Deny keyboard; nav trimmed to Esc when semantic buttons present; poof deletes the message on resolve for all three prompt types. **Live:** trigger a Bash gate on a fresh agent → `❓ Permission — Bash: … [✅ Allow once] [❌ Deny]` appears **while pending**; Allow → runs, Deny → cancels; the message poofs after.

- [x] **Slice B2 — ExitPlanMode PANE-TRIGGERED (same fix A2 got for AskUserQuestion).** ExitPlanMode's approval buttons (Slice B / CMX-20) are still **transcript-triggered**, so — exactly like AskUserQuestion before A2 — the `ExitPlanMode` `tool_use` record only lands in the JSONL **at answer-time**, so the Approve/Keep-planning buttons show up **after the plan is already resolved**, useless. The plan-approval selector ("Would you like to proceed?" with Yes-auto / Yes-manual / No) is a **live pane** UI, so detect it from the **pane** and reuse A2's `panescan`/`gatewatch`/edit-in-place substrate. ⚠️ **BEFORE implementing, open `TODO.md` and read items 1–4 + Landmines + Verify. Mirror how A2 did AskUserQuestion — this is the ExitPlanMode analog.**
  1. **Extend `chela/telegram/panescan.py`** with ccbot's ExitPlanMode `UIPattern`s (`~/projects/ccbot/src/ccbot/terminal_parser.py:50-61`: top `^\s*Would you like to proceed\?` / `^\s*Claude has written up a plan`, bottom `^\s*ctrl-g to edit in ` / `^\s*Esc to (cancel|exit)`). Add `detect_exitplanmode(pane_text) -> ExitPlan | None` returning the **scraped plan region** (the plan text shown above the options) — used as the message body. Keep all regex in `panescan.py`.
  2. **Pane-trigger via `gatewatch`** alongside AskUserQuestion + permission (same per-tick `capture_pane`, ungated — the `tool_use` is post-answer). **Edit-in-place + de-dup** using the A2 machinery (track a per-window message id like `_uq_msg` → a sibling `_plan_msg`; clear when the selector leaves the pane / on the `ExitPlanMode` `tool_result`).
  3. **Message + buttons:** relay the scraped plan (plain text; if long, truncate with a "… (/screenshot for full)" note) + the approval keyboard. Reuse Slice B's option-count-independent mapping: **`✅ Approve (auto mode)` → `qa:nav:ent` (Enter)**, **`📝 Keep planning` → `qa:nav:esc` (Escape)**, plus the glyph-only nav row (so the human can arrow to "Yes, manually approve edits"). Build via the existing `_plan_rows` + `_nav_row` in `chela/telegram/interactive.py`. (Enter/Esc are option-count-independent, so **no cursor-relative injection needed here** — unlike AskUserQuestion.)
  4. **Suppress the transcript double-relay:** in `chela/telegram/relay.py`, drop the transcript-triggered `ExitPlanMode` `tool_use` keyboard the same way A2 dropped AskUserQuestion's (it now arrives post-answer); keep its `tool_result` as the "approved/kept" confirmation + a de-dup clear signal. Remove/blank the ExitPlanMode branch in `ask_reply_markup` (transcript path) so it's pane-only now.
  **Landmines:** edit-in-place tolerate "not modified" + fall back to fresh post; clear on resolve; don't double-relay (pane + transcript); don't regress A2 (AskUserQuestion), C1 (permission), or the `qa:`/`qa:nav:`/`k:` keyboards; plain-text send (no MarkdownV2 escaping of scraped plan). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (A2 is the template):** `chela/telegram/panescan.py` (`detect_askuserquestion`), `gatewatch.py` (AskUQ relay + `_uq_msg` edit-in-place + clear), `interactive.py` (`_plan_rows`, `scraped_reply_markup`, `_nav_row`), `relay.py` (AskUserQuestion `tool_use` suppression), ccbot `terminal_parser.py:50-61`. **Verify:** unit-test `detect_exitplanmode` on a sample plan-approval pane (region scraped; normal/permission/AskUQ panes → None), edit-in-place + clear-on-resolve, transcript ExitPlanMode `tool_use` no longer builds a keyboard. **Live:** trigger a plan approval on a fresh bridged agent → the plan + Approve/Keep-planning buttons appear **while pending**; tapping Approve proceeds, Keep-planning stays in plan mode.

- [x] **Render markdown tables as a card-style list (port ccbot `convert_markdown_tables`).** Telegram has no table rendering. chela passes tables straight to `telegramify-markdown`, which doesn't produce the readable card-style list ccbot does (Liav: "ccbot renders the table as a list") — ccbot **pre-processes** tables before telegramify; chela's markdown port (CMX-18) skipped that. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Verify.**
  1. **Port `convert_markdown_tables`** from ccbot `~/projects/ccbot/src/ccbot/markdown_v2.py` (+ its `_split_table_row` helper) into chela (`chela/telegram/format.py` or a small helper). It turns each markdown table into card-style `**Header**: value` pairs separated by horizontal rules (how Claude Code renders tables in a narrow terminal), and **skips tables inside code blocks**.
  2. **Hook it into the body path:** in `to_markdown_v2` (`chela/telegram/format.py`), run the assistant/text **body** through `convert_markdown_tables` **before** `telegramify-markdown`, so telegramify renders the card text as normal markdown instead of a raw table. Keep the existing fallback: if telegramify is absent, still apply `convert_markdown_tables` then the blind escape. Don't touch the emoji-header escaping or `to_code_block`.
  **Landmines:** skip code blocks (don't convert `|`-lines inside a ``` fence); a malformed/no-body table must pass through gracefully (no crash); keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** ccbot `src/ccbot/markdown_v2.py` (`convert_markdown_tables`, `_split_table_row`), `chela/telegram/format.py` (`to_markdown_v2` + the telegramify path). **Verify:** unit-test a markdown table → card-style `**Header**: value` output (not raw pipes); a table inside a code fence is left untouched; a normal paragraph unchanged. **Live:** an agent message containing a table renders as a readable list on Telegram.

- [x] **BUGFIX — long single-line Telegram messages STRAND on the agent's input row instead of submitting ("hit send → break row").** CONFIRMED live 2026-07-12: `chela/messenger.py::send_tmux` single-line `else` branch (~92-96) does `tmux send-keys -t <t> <text> Enter` in **one** call. Claude Code's TUI treats a long injected blob as fast/paste input and the rapid trailing Enter is absorbed as a **newline**, so the message sits wrapped on the `❯` input line, unsubmitted. Reproduced with a 262-char message (a SHORT message submits fine — only long ones strand). ⚠️ **BEFORE implementing, open `TODO.md` and read items 1–2 + Verify.**
  1. **Fix (empirically proven):** in the single-line `else` branch, send the **text and Enter as SEPARATE tmux calls with a ~0.5s gap** — mirroring ccbot (`~/projects/ccbot/src/ccbot/tmux_manager.py:420-424`: "a 500ms gap lets the TUI process the text before receiving Enter") and chela's OWN multi-line path (paste-buffer → sleep → separate Enter, which works). Send the text with `tmux send-keys -l <text>` (**literal**) so a message containing a tmux key-name (e.g. "Up", "Enter", "C-c") isn't misinterpreted as a keypress — the current combined call omits `-l`. **Verified:** the same 262-char message submitted cleanly as `-l <text>` → `sleep 0.5` → `Enter`.
  2. **Keep robustness:** leave the multi-line paste-buffer path + its `_pane_has_unsubmitted_paste` 2nd-Enter chip-guard intact (that's the collapsed-`[Pasted text]` case; the single-line strand shows inline-wrapped text with NO chip, so that guard doesn't apply — the gap fix is what submits it). Don't change the slash-command Escape-first behavior.
  **Landmines:** `-l` for literal text (don't reintroduce key-name misinterpretation); keep the ~0.5s gap (a rapid Enter IS the bug); don't regress the multi-line/paste path or slash-commands. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/messenger.py` (`send_tmux` — the multi-line path is the template), ccbot `src/ccbot/tmux_manager.py:420-456`. **Verify:** unit-test (mock subprocess) that single-line `send_tmux` issues literal text (`-l`, no Enter) then a SEPARATE Enter (two calls, with the gap); multi-line still uses paste-buffer; slash-command still sends Escape first. Report a live check: a long single-line message submits (doesn't strand).

- [x] **A2 polish — AskUserQuestion double-relays (partial→full) + nav-row label truncation.** From a live screenshot: (a) the question posted **TWICE** — first with only the first option ("Mango"), then again complete ("Mango"/"Peach"/"Kiwi") — because the pane watcher scraped the selector **mid-render** (only option 1 had drawn), relayed it, then re-scraped the full selector (different content signature) and relayed AGAIN; (b) the **nav row** crams 5 buttons (↑ ↓ ⏱Esc ⏎Enter 🔄) into one row so "⏎ Enter" truncates to "⏎ E…" on mobile (the semantic option buttons render full/fine, one per row — leave them). ⚠️ **BEFORE implementing, open `TODO.md` and read items 1–2 + Verify.**
  1. **Fix the double-relay via EDIT-IN-PLACE (ccbot pattern):** when a detected AskUserQuestion's scraped content CHANGES for the same window (more options render / cursor moves), **edit the existing topic message** (text + `reply_markup`) instead of posting a new one. Track the message_id per window (mirror ccbot `src/ccbot/handlers/interactive_ui.py` `_interactive_msgs` + `edit_message_text`, tolerate "Message is not modified"). This collapses the mid-render partial + the full into ONE message that updates as the selector settles. **Clear** the tracked message_id when the selector leaves the pane (answered). Supersedes the current "new message per changed content signature" in `gatewatch`.
  2. **Reflow the nav row so labels fit:** shorten to glyph-only (e.g. `↑ ↓ ⎋ ⏎ 🔄`, drop the words) or split across two rows, so nothing truncates on a phone. Keep the same `qa:nav:*` callbacks/actions (`NAV_KEYS`).
  **Landmines:** edit-in-place must tolerate "Message is not modified" (no-op) and fall back to a fresh message if the edit fails (message deleted); don't double-post; clear tracking on resolve; don't regress the option buttons, the permission-gate relay, or `qa:nav` behavior. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (the AskUQ relay + de-dup marker), `chela/telegram/interactive.py` (`scraped_reply_markup`, `_nav_row`, `NAV_KEYS`), ccbot `src/ccbot/handlers/interactive_ui.py` (edit-in-place message tracking). **Verify:** unit-test that a changed scrape for the same window EDITS (not re-posts) and clears on resolve; the nav row fits (glyph-only or 2 rows); permission-gate path unaffected. **Live:** trigger a single-select question, confirm ONE message that fills in as options render + no truncated nav labels.

- [x] **Inbound image + file sharing (Telegram → agent) — port ccbot's media handling.** Today the bridge only registers `MessageHandler(filters.TEXT, _on_message)` (`chela/telegram/inbound.py:541`), so a **photo or document pasted into a topic is silently ignored** (Liav: "can't paste a screenshot from mobile"). Port ccbot's approach so a shared image/file reaches the agent. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify.** **Reference (port from):** ccbot `~/projects/ccbot/src/ccbot/handlers/document.py` (`document_handler` — download to a dir + forward the path; 20 MB `getFile` cap handling) and `src/ccbot/handlers/message_sender.py::send_photo`.
  1. **Photo handler:** register `MessageHandler(filters.PHOTO, _on_photo)` in `build_application` (`chela/telegram/inbound.py`, **before** the `filters.TEXT` catch-all). On a photo in a bound topic: resolve the window from the message's **own topic** via `router.resolve(chat_id, thread_id)` (chat-gate fail-closed — same as `_on_message`), pick the **largest** `PhotoSize`, `await photo.get_file()` → `download_to_drive()` into `CHELA_DIR/documents/` (create it; use a unique name, `.jpg`), then `messenger.send_tmux(window_id, <abs_path>)` so Claude Code can Read the image. Prefix a short line, e.g. `📎 image: <path>` (a bare path also works — Claude Code reads images by path).
  2. **Document handler:** `MessageHandler(filters.Document.ALL, _on_document)` — same flow; preserve the original filename where possible; download to `CHELA_DIR/documents/`, forward the path. **Reject > 20 MB** before downloading (Telegram caps bot `getFile` at 20 MB) with a friendly reply, mirroring ccbot.
  3. **Wire it:** `CHELA_DIR` is `~/.chela` (`chela/config.py`). Both handlers reuse the existing `router` + `messenger.send_tmux` that `_on_message` uses. Keep handler ORDER correct: PHOTO and Document handlers before `filters.TEXT` (PTB runs one handler per group).
  4. **Scope:** inbound photos + documents only. **Voice transcription is OUT** (ccbot's `transcribe.py` pulls faster-whisper — defer). Outbound agent→Telegram images already exist via `/screenshot`.
  **Landmines:** resolve window from the topic, never trust a payload; chat-gate fail-closed; create `CHELA_DIR/documents/` if missing; unique filenames (don't clobber); reject > 20 MB before download; a caption on the photo/doc (Telegram sends caption separately) should also reach the agent if present. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Verify:** unit-test the handlers with a fake `update`/`bot` (largest PhotoSize chosen; download path under `CHELA_DIR/documents/`; `send_tmux` called with the path + window resolved from topic; oversized document rejected without download; non-bound topic gated out). Note that real photo/doc round-trip needs a live check (report it as manual).

- [x] **Interactive UI Slice A2 — AskUserQuestion PANE-TRIGGERED (fixes "buttons show the selection after it's already answered").** ROOT CAUSE settled 2026-07-12 by hard measurement: Claude Code writes the `AskUserQuestion` `tool_use` record to the JSONL **only when the question is ANSWERED** (measured live: 0 records while the selector was on the pane pending → 1 the instant it was answered; embedded `timestamp` says question-time but the FILE APPEND is at answer-time). So Slice A's transcript-triggered relay can **never** show the buttons while the question is answerable — the record isn't there yet. The **pane** shows the selector live, so detection must come from the **pane** (exactly like C1's permission gates). This EXTENDS C1's `panescan`/`gatewatch` — not a rebuild. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–5 + Landmines + Verify beneath this line.**
  1. **Extend `chela/telegram/panescan.py` with the AskUserQuestion selector.** Port ccbot's AskUserQuestion `UIPattern`s (`~/projects/ccbot/src/ccbot/terminal_parser.py`: single-tab `^\s*[☐✔☒]` + bottom `^\s*Enter to select`; multi-tab `^\s*←\s+[☐✔☒]`). Add `detect_askuserquestion(pane_text) -> AskUQ | None` that returns the **question text**, the **ordered option labels** (parse lines like `❯ 1. Apple` / `  2. Banana` — strip the cursor/number/description; ignore the trailing `Type something.` / `Chat about this` meta-rows), and the **current `❯` cursor index** among the options. Keep ALL regex in `panescan.py` (the signatures table). **MVP = single question, single-select:** if it's multi-tab (multiple questions) or multi-select, relay the scraped **question text + the nav-row only** (no semantic option buttons) — never a broken keyboard.
  2. **Pane-trigger it — WITHOUT the unpaired-tool_use gate.** C1's `gatewatch` reads a pane only when the transcript has an **unpaired `tool_use`** — but a pending AskUserQuestion has **nothing in the transcript yet**, so that gate would never fire. So AskUserQuestion detection must `capture_pane` bridged windows **directly** (run `detect_askuserquestion` on the pane each tick). Since we're already capturing the pane, run it alongside `detect_permission_gate` in the same `gatewatch` poll (one `capture_pane` per window, two detectors). The fleet is small; note the per-tick pane-read cost. **Edge-triggered + de-dup per window** (like the permission gate): relay ONE message per question; clear the marker when the selector disappears from the pane (answered).
  3. **Message + buttons from the SCRAPED question** (transcript is not available): one `qa:<i>` button per scraped option (reuse Slice A's callback scheme + `_on_qa`, `chela/telegram/interactive.py` / `inbound.py`), plus the nav-row. Text is the scraped question.
  4. **Cursor-relative injection (re-read at tap time).** On a `qa:<i>` tap, `_on_qa` must `capture_pane` the target window, run `detect_askuserquestion` to get the **current** `❯` index + options, then send `Down`×(target − current) + `Enter` (NOT blind `Down×i` — the cursor may have moved). Add the cursor-relative variant; keep Slice B's `qa:nav` Enter/Esc working.
  5. **Formatting + no double-relay.** Build the message ourselves; send **plain text** (no MarkdownV2 escaping of scraped text — same choice C1 made for gates; cf. https://core.telegram.org/bots/api#formatting-options). And **suppress Slice A's transcript-triggered AskUserQuestion keyboard** (`ask_reply_markup` on the transcript `tool_use`) — that record now arrives post-answer, so attaching buttons there is useless and would double-post; drop that message (or keep it as a plain "answered: X" confirmation). Use the transcript `tool_result` (which DOES land, at answer) as a belt-and-suspenders signal to clear the pane de-dup marker.
  **Landmines:** edge-triggered/de-dup (one msg per question; clear when the selector leaves the pane); cursor-relative injection (read `❯`, never assume 0); plain-text send; NO double-relay (pane + transcript); don't regress C1 permission detection, Slice B ExitPlanMode buttons, or the `k:`/`qa:` keyboards. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/panescan.py` + `gatewatch.py` (C1 — the substrate to extend), `chela/main.py` `_outbound_loop` (wiring), `chela/telegram/interactive.py` (`ask_reply_markup`, `select_keystrokes`, `qa:` scheme) + `inbound.py` (`_on_qa`), `chela/messenger.py` (`capture_pane`, `send_key`), ccbot `terminal_parser.py` (AskUserQuestion `UIPattern`s). **Verify:** unit-test `detect_askuserquestion` on sample selector panes (question + ordered options + cursor index; multi-tab/multi-select → the fallback), and a normal pane → None; edge-trigger/de-dup; cursor-relative keystroke calc (current=1, target=0 → one `Up`… or `Down`×… as implemented); plain-text formatting; no-double-relay. **Live (report):** spawn a FRESH bridged agent, trigger a single-select AskUserQuestion, confirm the question + option buttons appear on the topic **while it is still pending**, and that tapping an option selects the right one end-to-end.

- [x] **BUGFIX — monitor skips a freshly-appeared / rotated transcript (blocked agents show NOTHING on Telegram, or only the post-answer selection).** ROOT-CAUSED live 2026-07-12: `chela/telegram/monitor.py::_poll_window` (~106-112) seeks to **EOF** on first-sight of a window's transcript to skip old backlog — but that also skips records **already in the file when it first APPEARS** (a fresh agent's first turn) or **ROTATES** (a `/clear` → new session file). Effect: an agent blocked on an **AskUserQuestion / permission prompt** relays **nothing** (fresh window: its whole first turn, question included, is skipped while still pending) or **only the post-answer `tool_result`** (rotation caught between the question's `tool_use` and its `tool_result`) → "the buttons show the selection after it's already answered." This defeats the A/B interactive keyboards for exactly these cases (the keyboards are fine; the message never reaches them). ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–3 + Landmines + Verify beneath this line.**
  1. **The fix — EOF-skip ONLY on cold-start attach to PRE-EXISTING history.** A transcript that **appears or rotates while the monitor is running** must be read from **offset 0** (its content is all new — the agent produced it this session). In `_poll_window`: when `path` is None/absent, remember it (`self._seen_empty: set[str]`, init in `__init__`). When a transcript first resolves for a window that was **seen empty** (`window_id in self._seen_empty`), OR the path **rotates** (`tracked is not None and tracked.path != path`), start at **0** — not `path.stat().st_size`. Keep the EOF seek ONLY for the genuine cold-start case: `tracked is None`, window **never** seen empty, transcript **already existed** on the very first poll (binding an agent that has pre-existing history — must NOT be replayed). `discard(window_id)` from `_seen_empty` once it has a file.
  2. **Guard against replaying a large backlog.** From-0 is safe for a fresh/`/clear` file (starts empty). But if an appeared/rotated file is already **large** at first sight (e.g. a `--resume` that repopulated history into a new file), from-0 would dump backlog — add a size/record guard: if the newly-resolved file already exceeds a sane threshold at first sight, fall back to EOF (and `log`). A fresh first-turn file is small; pick the threshold conservatively.
  3. **Don't regress.** Keep `_read_new`'s partial-write / truncation (`offset > size` → reset) handling; keep the cold-start "skip pre-existing history" behavior for agents bound WITH an existing transcript (binding an existing agent must NOT replay its whole history); `start_at_eof=False` (tests/backfill) still means from-0 always.
  **Landmines:** the whole fix is one distinction — *"did we poll this window and find NO transcript before it got one?"* (→ fresh, from 0) vs *"transcript already there on the first poll"* (→ backlog, EOF). Get it wrong and you either miss fresh turns (the current bug) or replay months of history (spam). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference / repro (confirmed):** bind a window before its transcript exists, then have its first turn contain an AskUserQuestion → today the topic stays empty though the `"name":"AskUserQuestion"` record IS in the JSONL. Files: `chela/telegram/monitor.py` (`_poll_window`, `_Tracked`, `_read_new`, the `start_at_eof` docstring), `chela/telegram/parser.py` (emits the events). **Verify (unit, fake resolver + tmp files):** (a) poll a window whose resolver returns None, then returns a file WITH content → the content **IS** emitted (was skipped — the core fix); (b) cold-start: first poll resolves a pre-existing file with content → **NOT** emitted (EOF); (c) rotation: tracked to file A, resolver switches to file B with content → B emitted from 0; (d) the large-backlog guard falls back to EOF; (e) truncation-reset still works. Report before/after on the repro.

- [x] **Interactive UI Slice C1 — permission-gate DETECTION + enriched relay (pane-scrape; NO buttons yet).** Unlike AskUserQuestion/ExitPlanMode, a tool/permission gate ("Do you want to proceed?" for a Bash/Edit) is **NOT in the transcript** — only the live pane shows it. This slice adds the bridge's **first pane read** so a blocked agent is surfaced to its topic as `❓ Permission — Bash: rm -rf build/`. **Buttons come in C2** — this is detection + enriched relay + the poll loop + de-dup only. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify beneath this line — they carry the load-bearing constraints (two-channel correlation, gated polling, edge-trigger, the one-module TUI-regex rule). Do not implement from this summary line alone.**
  1. **New pane detector (PORT from ccbot; isolate ALL TUI regex here):** create a small pure module (e.g. `chela/telegram/panescan.py`) — port ONLY ccbot's PermissionPrompt + BashApproval `UIPattern`s (`~/projects/ccbot/src/ccbot/terminal_parser.py:74-99`) + its `extract_interactive_content` helper. Expose `detect_permission_gate(pane_text) -> Gate | None` returning the gate's region text + a type tag. Keep **every** Claude-Code-TUI regex in this one module (the "signatures table") so a Claude Code version bump is a one-file edit. Do NOT port the whole `terminal_parser` — just the permission/bash patterns + the extractor.
  2. **Gated pane-poll in the EXISTING loop (no new thread):** in the bridge's outbound cycle (`chela/main.py` `_outbound_loop` ~354 → `monitor.poll`), for each bridged window **gate on the transcript**: only when the window's latest `tool_use` is **unpaired** (no `tool_result` yet — the parser already tracks this in its `pending` map, `chela/telegram/parser.py`) do a `messenger.capture_pane(wid)` (`chela/messenger.py:~103`) and run `detect_permission_gate`. This correlates the two channels (transcript says a call is pending → only then read the pane) and keeps pane-capture cheap — never blind-scrape every window every tick.
  3. **Enriched, EDGE-TRIGGERED relay:** on a NEWLY-detected gate for a window (not already relayed), post ONE message to the window's topic: `❓ Permission — <tool>: <command/args>`, where **tool name + args come from the transcript's unpaired `tool_use`** (Bash `command`, Edit `file_path`, etc.) — that's the two-channel win (you see the real command, not blind pane text). If the transcript identity is unavailable, fall back to the scraped gate-region text. **Edge-triggered:** track the relayed gate per window; **clear** it when the `tool_result` appears OR the pane no longer shows a gate — so a still-open gate is not re-posted every ~2s.
  4. **NO keyboard in this slice.** The `✅ Allow once` / `❌ Deny` + nav-row keyboard is **Slice C2** (reuses the `qa:nav` Enter/Esc plumbing). Keep C1 to detection + enriched relay + poll + de-dup so the new pane-scrape mechanism is reviewed on its own.
  **Landmines:** edge-triggered (ONE message per gate, never per poll); gate the pane-capture on the transcript's unpaired `tool_use` (don't blind-scrape); isolate ALL TUI regex in the one detector module; correlate identity from the transcript, fall back to pane text; don't regress the transcript relay or the A/B (`qa:`) + `/screenshot` (`k:`) keyboards. Attribution: the ccbot port is covered by the top-level `NOTICE` (`chela/telegram/` adapted from ccbot). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference — ccbot:** `src/ccbot/terminal_parser.py` (PermissionPrompt/BashApproval `UIPattern`s:74-99, `extract_interactive_content`), `src/ccbot/handlers/status_polling.py` (its "detect permission UI not in JSONL" poll — model, don't copy wholesale). **chela:** `chela/messenger.py` (`capture_pane`), `chela/telegram/monitor.py` (poll loop) + `parser.py` (tool_use↔tool_result pairing / `pending` unpaired map), `chela/main.py` (`_outbound_loop`), `chela/telegram/relay.py` (posting to a topic). **Design principle:** transcript = identity, pane = liveness, correlated; gated polling; one signatures module (this is the PTY-native answer to ACP). **Verify:** unit-test `detect_permission_gate` on sample permission/bash-approval pane snippets (→ detected + type) and a normal pane (→ None); unit-test the edge-trigger/de-dup (same gate not re-relayed; cleared on resolve; a new gate relayed); unit-test the transcript-identity extraction (unpaired `tool_use` → tool + args). Live: trigger a real Bash permission prompt in a test agent and confirm the bridge relays EXACTLY ONE enriched message naming the command. **Report the exact pane snippets you keyed on** (for the version-canary later).

- [x] **Interactive UI Slice B — ExitPlanMode plan-approval buttons (reuses Slice A plumbing).** When an agent calls `ExitPlanMode`, the bridge already relays the plan text to its topic (`ExitPlanMode` ∈ `INTERACTIVE_TOOL_NAMES`, `chela/telegram/relay.py:47`). Attach an **inline keyboard** so the human can approve / keep-planning from the phone. Detection = transcript (structured `tool_use`; plan in `input.plan`). ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read the numbered items 1–6 + Landmines + Verify beneath this line — they carry the load-bearing constraints (the KEY difference from Slice A, the option-count-independent keystrokes, the VALIDATE-empirically step, and what to report). Do not implement from this summary line alone.**
  1. **KEY difference from Slice A (read first):** unlike AskUserQuestion, ExitPlanMode's approval CHOICES are **NOT in the transcript** — `input` carries only `plan`, not the "Yes / Yes-auto / No, keep planning" options (those are harness-rendered TUI). So do **NOT** enumerate choices from the transcript, and do **NOT** introduce general pane-scraping (that's deferred to Slice C). Use **option-count-independent** keystrokes.
  2. **Keyboard (two semantic buttons + the always-present nav row):** `✅ Approve plan` → sends **`Enter`** (accept the default-highlighted proceed option); `📝 Keep planning` → sends **`Escape`** (dismiss the plan-exit, stay in plan mode). Both are single keystrokes, independent of how many Yes-variants exist. **Reuse Slice A's existing `qa:nav:*` plumbing:** `Approve` = callback `qa:nav:ent`, `Keep planning` = `qa:nav:esc` — both already route through `_on_qa` → `NAV_ACTIONS` → `send_key`, so **no inbound handler change is needed**. Keep the full nav-row (`_nav_row()`) too, so the human can arrow to a *specific* variant (e.g. "Yes, manually approve edits") when the default isn't what they want.
  3. **Where to build it:** extend `ask_reply_markup(msg)` in `chela/telegram/interactive.py:119` to return the ExitPlanMode keyboard when `msg.tool_name == "ExitPlanMode"` (dispatch on `tool_name`; the AskUserQuestion path stays unchanged). The relay already calls `ask_reply_markup(msg)` at both attach sites (`relay.py:163,206`) and threads `reply_markup` through `BotSender.send` — so **no relay change is needed** beyond what A shipped. Rename to `interactive_reply_markup` only if it reads cleaner (then update the 2 call sites + import); otherwise just branch inside.
  4. **VALIDATE EMPIRICALLY (load-bearing):** trigger a real ExitPlanMode in a live test agent (present a plan), `capture-pane`, and confirm: (a) **`Enter` proceeds** — and **REPORT which option it selects** (does the default silently enable *auto-accept edits*, or is it manual-approve?); (b) **`Escape` keeps planning** — stays in plan mode, does NOT cancel the whole task. If `Enter`'s default is auto-accept-edits (a real behaviour change the user may not want), either **label the button accurately** (e.g. `✅ Approve (auto-accept edits)`) or make the **nav-row primary** — report which you shipped and the exact behaviour of Enter/Esc.
  5. **Callback / handler:** reusing `qa:nav:ent`/`qa:nav:esc` (recommended) means **nothing new in `inbound.py`**. If instead you add a dedicated `pa:` (plan-approve) scheme for nicer toasts, mirror `_on_qa` (`inbound.py:453`): resolve the window from the callback's **own topic** via `router.resolve` (never trust a window id off the wire — CMX-8), chat-gate fail-closed, `send_key`, `query.answer(<label>)`, register its `CallbackQueryHandler(pattern=…)` **before** the catch-all, and ensure `^qa:` and the new pattern don't overlap.
  6. **Keep it additive:** the ExitPlanMode keyboard attaches the same additive way as A's — if building it fails, still relay the plan text (never crash the relay). The AskUserQuestion (`qa:<i>`) and `/screenshot` (`k:`) keyboards must be untouched and keep working.
  **Landmines:** don't regress Slice A (AskUserQuestion buttons) or the `k:` screenshot keyboard — both must coexist. No general pane-scraping (defer to C). callback_data ≤64 bytes. Never crash the relay. Attribution already covered by the top-level `NOTICE` (ccbot). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference — chela (Slice A, now on `dev`):** `chela/telegram/interactive.py` (`ask_reply_markup:119`, `_nav_row:85`, `NAV_KEYS:51`, `NAV_ACTIONS:61`, `select_keystrokes:68`, `decode_callback:144`, `QA_NAV_PREFIX`), `relay.py:47,163,206`, `inbound.py:453,514`, `parser.py` (`Message.tool_name`/`tool_input`). **ccbot** (context only — do NOT port the scraper): `src/ccbot/terminal_parser.py` ExitPlanMode patterns. **Verify:** unit-test (a) an ExitPlanMode `tool_use` message → keyboard with Approve(`qa:nav:ent`)/Keep-planning(`qa:nav:esc`) + nav-row; (b) AskUserQuestion still returns its `qa:<i>` semantic buttons (no regression); (c) a plain/text message → no keyboard. Plus the live validation; report Enter/Esc behaviour in the PR.

- [x] **Interactive UI Slice A — AskUserQuestion inline-keyboard answering (single-select MVP).** When an agent calls `AskUserQuestion`, the bridge already relays the question text to its Telegram topic (it's special-cased in `INTERACTIVE_TOOL_NAMES`, `chela/telegram/relay.py:45`). Add an **inline keyboard** so the human can answer from the phone instead of typing the answer back. **Detection = transcript (structured), NOT pane-scrape.** The `AskUserQuestion` `tool_use` block carries `input.questions[]` with `{question, header, multiSelect, options:[{label, description, preview}]}` — parse this where the relay builds the message. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read the FULL spec in the numbered items 1–6 + Landmines + Verify directly beneath this line — they carry the load-bearing constraints (MVP boundary, the VALIDATE-keystrokes-empirically-first step, the resolve-window-from-topic security rule, and the ccbot files to port from). Do not implement from this summary line alone.**
  1. **MVP scope (HARD boundary):** handle ONLY a **single question** with **`multiSelect == false`** and **no "Other"/free-text**. If the tool_use has `len(questions) > 1`, OR `multiSelect == true`, OR a free-text option — do NOT attach semantic option buttons; attach only the navigation-fallback row (below) so the human drives manually. Never present a broken keyboard.
  2. **Semantic option buttons (primary UX):** one button per `options[i].label` (truncate to Telegram's button-text limit), `callback_data = f"qa:{i}"` — **index only; NEVER put the label in callback_data (64-byte limit).** Tapping option *i* selects it.
  3. **Navigation-fallback row (PORT from ccbot, don't reinvent):** mirror ccbot's `_build_interactive_keyboard` (`~/projects/ccbot/src/ccbot/handlers/interactive_ui.py:81`) — a compact `↑ / ↓ / ⎋ Esc / ⏎ Enter / 🔄` row driving chela's existing `messenger.send_key` (the same primitive the `/screenshot` control-key keyboard already uses, `chela/messenger.py:121`). Always present, so the human can navigate manually if a semantic pick misfires or in the fallback cases above.
  4. **Answer-injection contract — VALIDATE EMPIRICALLY FIRST (load-bearing unknown):** Claude Code's AskUserQuestion selector highlights option 0 on render; sending `Down`×i then `Enter` should select option i and (single question) submit. **Before wiring the semantic buttons, trigger a real single-select AskUserQuestion in a live test agent, `capture-pane`, and confirm `Down`×i + `Enter` lands on and submits option i** (watch for cursor-start offset or a leading header/"Other" row). If unreliable, make the **navigation keyboard primary** and downgrade the semantic buttons — **report which you shipped and the exact keystroke sequence that worked** (Slice B / ExitPlanMode reuses it).
  5. **Callback handler (mirror `_on_key`, `chela/telegram/inbound.py:371`):** add a `CallbackQueryHandler` (or branch inside `_on_key`, which early-returns on non-`k:` data at `inbound.py:385`). On `qa:{i}`: **resolve the window from the callback message's OWN topic via `router.resolve(chat_id, thread_id)` — NEVER trust a window id from the payload** (CMX-8 isolation boundary), chat-gate fail-closed, fire the keystrokes via `send_key`, and `query.answer(<label>)` for instant feedback. Register it before the catch-all `MessageHandler` (`inbound.py:434`).
  6. **Wiring:** pass the injection fn (index → `send_key` sequence) into `build_application` the way `send_key`/`send_escape`/`capture` already are (`chela/telegram/inbound.py:200`, `chela/main.py:533`). Attach the keyboard to the AskUserQuestion message emitted in `RegistryRelay.on_message` (`chela/telegram/relay.py:182`); `BotSender.send` (`relay.py:112`) must accept an optional `reply_markup` — add it, keep the existing MarkdownV2→plain fallback intact.
  **Landmines:** keyboard is ADDITIVE — if building it fails, still relay the text (never crash the relay). callback_data ≤64 bytes, index only. Resolve window from topic, not payload. Don't regress the `k:` screenshot keyboard (`_on_key` must still handle `k:`; the new handler ignores non-`qa:` data). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (port mechanics): ccbot** `src/ccbot/handlers/interactive_ui.py` (`_build_interactive_keyboard`, edit-in-place tracking), `src/ccbot/handlers/callback_data.py` (`CB_ASK_*` keystroke callbacks), `src/ccbot/bot.py` (callback_handler round-trip). **chela:** `relay.py:45,112,182`, `inbound.py:200,273,371,427-450`, `messenger.py:121`, `bindings.py:96,103`, `main.py:492,533`, `parser.py:101-170`. **Verify:** unit-test (a) single-question single-select → reply_markup with one `qa:{i}` button per option; (b) multiSelect==true OR >1 question OR Other → NO semantic buttons (nav-only); (c) callback for `qa:2` resolves window from topic + fires index-2 injection, ignores non-`qa:` data. Plus the live validation; report the working keystroke sequence in the PR.

- [x] **Render assistant markdown properly in Telegram (add `telegramify-markdown`).** `chela/telegram/format.py::to_markdown_v2` currently **blind-escapes** the body via `escape_markdown_v2` (every MarkdownV2 special char backslashed), so Claude's markdown renders LITERALLY on Telegram — ` ``` code blocks ``` ` show as escaped backticks, `*bold*` shows the asterisks, tables show raw pipes. Port ccbot's approach (it deliberately was NOT ported to stay dep-free — now we want the fidelity):
  1. Add **`telegramify-markdown`** to the `[telegram]` extra in `pyproject.toml` (what ccbot used; pulls `mistletoe`).
  2. In `to_markdown_v2`, render the **assistant/text body** through `telegramify-markdown` → valid MarkdownV2 (real code-block + bold + inline-code entities), replacing the blind `escape_markdown_v2` for that body. Optionally port ccbot's `convert_markdown_tables` (tables → card-style key/value, since Telegram has no tables) — `src/ccbot/markdown_v2.py`.
  3. **Keep the fallback:** import `telegramify-markdown` **lazily**; if it's absent (import fails) or raises on a message, fall back to the current `escape_markdown_v2` blind-escape so the core still works without the extra. The relay's existing MarkdownV2→plain-text-on-reject path stays as the outer safety net.
  4. The emoji **header** (`🔧`/`✅`/`🤖` + tool/role) must remain valid MarkdownV2 — escape it or keep it outside the converted body; don't double-escape. **Leave `to_code_block`** (used by `/screenshot`'s text fallback) untouched.
  **Don't touch** routing / registry / lifecycle / the relay's send+fallback contract. **Landmines:** don't double-escape the header; a telegramify failure on ONE message must degrade to escaped-plain for that message, never crash the relay; keep it lazy so pure format tests don't need the extra. **Verify:** unit-test that an assistant message with a fenced code block + `*bold*` renders MarkdownV2 with a real code entity (backticks NOT backslash-escaped) when telegramify is available, and that the fallback path (telegramify absent/raising) still produces safe escaped output. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. Report back. Reference: ccbot `src/ccbot/markdown_v2.py` (telegramify usage + `convert_markdown_tables`), `chela/telegram/format.py`, `chela/telegram/relay.py` (send + plain fallback), `pyproject.toml` `[telegram]` extra.

## Open — orchestrator awareness

- [x] **Dashboard toast on dispatcher run-state transitions + `chela dispatch-runs --awaiting` summary.** So the orchestrator/human learns a run needs review without polling. Two small parts. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify beneath this line — they carry the load-bearing constraints (edge-trigger, reuse existing SSE + toast + runs-query, the no-auto-push scope guard). Do not implement from this summary line alone.**
  1. **Toast (frontend, EDGE-TRIGGERED):** in the dashboard, when a dispatcher run transitions **→ `awaiting_review`** (secondarily → `done` / → `failed`), pop a toast. **Reuse existing pieces:** the SSE `/api/events` stream that already keeps the Kanban reactive (`chela/dashboard/app.py:~2573`) and the existing toast styling (`.kanban-toast`, `chela/dashboard/static/style.css:1506`; cf. `.kanban-merge-toast`/`.kanban-promote-toast`). The frontend already consumes run/dispatcher state; track the **previous per-run status** and fire the toast **only on the transition** (NOT every ~1s poll while it sits in `awaiting_review`). Text e.g. `🔍 CMX-20 → awaiting review` with the **PR link** when present. Hang a mute toggle off the existing **"Notifications"** overflow-menu entry (persist in localStorage like other dashboard prefs).
  2. **Backend (only if needed):** if `/api/events` doesn't already carry per-run `status` + `pr_url`, add them to the named frame it emits on change (the REST `/api/dispatcher` already exposes them — mirror that source; do NOT add a new poll).
  3. **CLI summary:** add `chela dispatch-runs --awaiting` (or `--status <state>`) — a concise filter listing runs in `awaiting_review` with task id, PR URL, and age, so the orchestrator answers "what's up for review?" instantly on demand. Extend the existing command (`chela/main.py` `cmd_dispatch_runs` + the run listing in `chela/dispatcher.py`); reuse the existing query, don't add a second source of truth.
  4. **Scope guard:** this is the **pull/awareness** feature ONLY — do NOT auto-`send_keys` the event into any agent session (autonomous push into the orchestrator is a separate, deliberate decision — the future "decisions inbox"). Toasts are for the dashboard viewer; the CLI is on-demand.
  **Landmines:** toast MUST be edge-triggered (transition only) or it spams every poll; don't regress the existing Kanban toasts (promote/merge) or the SSE stream; keep the Notifications mute working; `--awaiting` reuses the existing runs query. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dashboard/app.py` (`/api/events` SSE ~2573, `/api/dispatcher`), `chela/dashboard/static/style.css:1506` (`.kanban-toast`), the dashboard frontend JS that renders the Kanban + consumes `/api/events`, `chela/dispatcher.py` (run statuses: claimed/running/awaiting_review/done/failed), `chela/main.py` (`cmd_dispatch_runs`). **Verify:** (a) a run entering `awaiting_review` pops exactly ONE toast (not one per poll) with the PR link; (b) muting via Notifications suppresses it; (c) `chela dispatch-runs --awaiting` lists only awaiting-review runs with PR URLs. Unit-test the CLI filter + the edge-trigger (prev vs new status) logic; the toast DOM may need a light JS test or manual check.

## Backlog (not yet dispatchable)

- **Settings view — editable toggles (follow-up)**: in-UI write-back + daemon restart.
- **Retire ccbot** ~07-19 after warm standby: `pm2 delete ccbot` + `pm2 save` + archive repo (ops).
- `/kill` (explicit agent-kill + close topic) — optional, higher-stakes; `/unbind` `/history` `/usage` skipped (don't fit auto-topics).
- Privacy scrub (forum IDs, `CCBOT_PANE_FALLBACK`, abs paths → env) before public `main`.
- **Cost view** (cockpit): transcript tokens × price → $ per agent / run / fleet.
- **Unified graph viewer** (cockpit): memory `[[wikilinks]]` + fleet; renderer = **Sigma.js + Graphology (MIT, WebGL)**, clean-room (not a GitNexus fork — PolyForm-NC). Colorblind-safe.

## Done

- [x] **🎯 Decisions inbox** — agent/run events pushed into the orchestrator's session, GATED ON IDLE (never into a `busy` or — critically — a `waiting` pane, which would answer its permission prompt). Durable queue + watch store (`chela watch <wid> --note`); triggers = watched busy→idle / →waiting / died, plus run `awaiting_review` / `failed`. `CHELA_INBOX_ENABLED=false` to disable.

- [x] telegram-setup (#14) · scaffold+extra (#15) · dashboard workflow discovery (#16) · monitor port (#17) · outbound relay+CLI (#18).
- [x] Fold steps 1-3 — paste-submit (#19), INBOUND (#21), monitor resolve-by-record (#22) [CMX-6/8/9].
- [x] Mobile Kanban — Swipe + Rows (#20, CMX-7).
- [x] Multi-topic Slice A registry (#23) + Slice B auto-create/lifecycle (#24) [CMX-10/11].
- [x] Topic naming by project cwd (#25, CMX-12).
- [x] `CHELA_SHOW_TOOL_CALLS` toggle — hide tool spam (#26, CMX-13).
- [x] Settings view — Connections & Status (#27) + Telegram-bridge row (#28) + Work-dispatcher-badge fix [CMX-14/15].
- [x] Bridge commands `/screenshot` (PNG) + `/esc` + `/` menu (#29, CMX-16) + `/screenshot` control-key keyboard + Refresh (#30, CMX-17).
- [x] **CUTOVER LIVE 2026-07-12** — `chela-telegram` (pm2 id 12) on @chelamuxbot / test forum ↔ 4 agents; tool-spam hidden.
