# TODO

## Open — CI drives the loop

- [x] **🧭 THE OUTBOUND RELAY MUST RESOLVE `wid → transcript` BY `session_id`, NOT BY `cwd`.** ⛔ **The transcript monitor is the LAST consumer still correlating on `cwd` — the exact key PR #61 already ripped out of the event log (CMX-48: events "filed against the **wrong window**").** `monitor._default_resolver` does `discovery.get_window_cwd_by_id(wid)` → `transcripts.transcript_for_cwd(cwd)` → *newest `*.jsonl` in `~/.claude/projects/<encoded-cwd>/`*. Its docstring asserts *"tmux is the source of truth"* for this mapping. **That assertion is FALSE, and it failed live on 2026-07-14.**

  🔑 **THE FAILURE IS SILENT — that is what makes it worth fixing over merely surviving.** No transcript resolved → resolver returns `None` → **nothing to relay, no error, no warning, no log line.** The bridge looks perfectly healthy: bindings reconcile, topics get created, **inbound works** (it only needs the `wid`). The human just… never hears back. Liav sat in front of a live nautilus topic sending messages that landed, receiving nothing, with every health surface green. ⛔ **Whatever you build, a resolver that finds no transcript must be LOUD.**

  **THE LIVE REPRO (this is how it was found, not a hypothetical):** the fleet's tmux server died in an OOM; the orchestrator rebuilt window `@2` at cwd `~/projects/nautilus/meme_scalp` and ran **`claude --resume b098b108…`**. ⚠️ **`--resume` keeps a session's transcript in its ORIGINAL project dir** — so the live JSONL kept growing at `…/-home-liavedunix-projects-nautilus/b098b108….jsonl` while the monitor searched `…-projects-nautilus-meme-scalp/`, **which contained zero `.jsonl` files.** Outbound was dead for ~1h.

  1. **RESOLVE BY `session_id`.** `event_log` **already carries an authoritative `wid` AND `session_id` on every hook event** (`event_log.py:249,266-267`) — the hooks fire in the agent's own process, so this is ground truth, not a guess. Resolve `wid` → most-recent `session_id` for that wid → the transcript is `<session_id>.jsonl` (**the session id is globally unique, so glob it across `~/.claude/projects/**/` — you do NOT need to know the project dir at all**). ⛔ **Do not invent a second registry; the event log IS the registry.**
  2. **`cwd` BECOMES THE FALLBACK, NOT THE PRIMARY.** A brand-new window that has not yet fired a single hook has no event → keep today's cwd path for that case only. ⛔ **Never let the cwd guess OVERRIDE a `session_id` the log already knows.**
  3. 🔴 **PRESERVE THE `_FRESH_MAX_BYTES` EOF-FALLBACK (64 KiB).** It exists precisely because **`--resume` repopulates a large prior history into a file the monitor is seeing for the first time**, and replaying it would spam the topic with the whole session. The new resolver makes `--resume` *work*, which makes this guard **more** load-bearing, not less. **Test it under a real `--resume`, not a synthetic file.**
  4. **THE OTHER TWO WAYS cwd LIES** (fix falls out of (1), but **test them**): a session that **`cd`s** moves `pane_current_path` out from under the monitor; and **two windows sharing one cwd** collide on *"most-recently-written wins"* — they silently tail **each other's** transcript. ⛔ A relay that posts one agent's output into another agent's topic is worse than silence.
  5. **CLEAN UP THE ORCHESTRATOR'S DUCT TAPE.** There is a **temporary symlink** at `~/.claude/projects/-home-liavedunix-projects-nautilus-meme-scalp/b098b108….jsonl` → the real transcript, hand-made on 07-14 to restore the relay. **Delete it as part of this task** and confirm the relay still works without it — if it doesn't, you have not fixed the bug, you have inherited the workaround.
  6. **MAKE IT ASSERTABLE.** Add a `chela doctor` fact: every bridged window resolves to a transcript that **exists and was written in the last N minutes**. ⛔ This class of bug is invisible precisely because nothing ever asserted it.

  ⚠️ **THE BUG CLASS, AGAIN (see the 07-14 handoff):** *the artifact you WROTE is not the artifact that RUNS.* A fixture that hands the resolver a tidy cwd reproduces the blind spot instead of the bug. **Drive it live: a real `--resume`, from a window whose cwd is NOT the session's origin dir, and watch the message actually arrive in Telegram.**

- [x] **⚖️ THE JUDGE — auto-adversarial review on `awaiting_review`. ⛔ ITS BLOCKING VERDICTS MUST BE FACTS, NOT OPINIONS.** The carrier is **proven** (CMX-68 rework loop + CMX-69 CI gate). This is the deferred half. 🔑 **THE DESIGN IS NOT "spawn a reviewer and trust it" — read the next paragraph before anything else.**

  **THE EVIDENCE THAT MAKES THIS URGENT (2026-07-14, measured).** Five dispatched PRs reached `awaiting_review`, **all five CI-green**. I hand-spawned five adversarial reviewers. **FOUR were sent back — and NOT ONE for a broken feature.** Every feature worked. They were sent back because **the thing meant to PROVE the feature works could not fail**:
  | PR | the guard | the corruption that left the suite GREEN |
  |---|---|---|
  | #85 sidebar | "sidebar state must never reach `_termSig`" | folded `document.body.className` into `_termSig` → **every live terminal in the fleet reloads** → `15 pass, 0 fail` |
  | #85 sidebar | the **colourblind** glyph cue | emptied the glyph span → hue-only chips (**the deuteranomaly failure mode**) → `0 failures` |
  | #87 lazy-bind | the whole feature | **reverted the entire production wiring** (`main.py`) → feature never runs → `GREEN, 1112 passed` |
  | #88 gate | the whole fix | **deleted both `_pane_loop` thread starts** → no gate can EVER reach Telegram → `100% GREEN` |
  **Without those five reviewers, four PRs with unfalsifiable proofs merge and `dev` looks green.** ⛔ **CI cannot catch this: CI runs the tests, and the tests are the thing that is broken.**

  1. 🔑 **MAKE THE CORE CHECK MECHANICAL — this is what de-risks an autonomous judge.** The 07-14 handoff's stated worry was *"it becomes a load-bearing autonomous judge; a wrong REFUTED burns a rework round."* **That worry dissolves if the judge only BLOCKS on facts.** ⛔ **A guard that survives deliberate corruption is a FACT, not a judgment** — exactly like a red CI check (CMX-69's whole premise: *a failing check is a fact, not a judgment*).
     **→ THE JUDGE'S BLOCKING VERDICTS ARE RESTRICTED TO MECHANICAL FINDINGS:**
     - **MUTATION**: for each guard/invariant the PR claims to add — **corrupt it and re-run the suite. Still green ⇒ BLOCK.** (This alone caught all four today.)
       🔴 **BUT A MUTATION IS ITSELF AN ARTIFACT, AND IT CAN LIE. VALIDATE EVERY MUTATION BEFORE ITS RESULT MEANS ANYTHING.** I made **both** of these mistakes by hand on PR #85 tonight, and either one would have produced a **confidently wrong verdict**:
       - ⛔ **THE MUTATION THAT NEVER APPLIED.** A `sed` whose delimiter collided with a `|` in the target line left the file **UNCHANGED**. The suite stayed green — and a naive judge reads that as **"the guard is broken → BLOCK"**, blocking a good PR on a mutation it never made. **→ After mutating, ASSERT THE FILE ACTUALLY CHANGED** (diff it; confirm the intended text is present).
       - ⛔ **THE MUTATION THAT BROKE THE PARSE.** Deleting an `if (...) {` line unbalanced the braces → **syntax error** → 22 failures. That is RED for the **wrong reason**: it proves nothing about the guard, and a naive judge reads it as **"the guard fired → PASS"**. **→ After mutating, CONFIRM THE CODE STILL PARSES** (`node -c <file>` / `python -m py_compile`) **and that the failure count is CONSISTENT with a guard firing, not with a file that no longer loads.** A mutation that takes the whole suite down is INVALID, not evidence.
       🔑 **The rule: a mutation must be a LIVE, MINIMAL, SYNTACTICALLY VALID change that ONLY a real guard could catch.** Prefer `if (false && <cond>)` / inverting a comparison / emptying a returned value over deleting lines. ⛔ **An unvalidated mutation is exactly the bug class this judge exists to catch — committed by the judge itself.**
     - **WIRING**: **delete the production call-site** and re-run. Still green ⇒ the tests never exercise what runs ⇒ **BLOCK.**
     - **CLAIM CHECK**: numbers in the PR body must reproduce (#88 claimed "1116 passed"; it was 1116 *collected*, 1113 passed) ⇒ **BLOCK** on a false claim.
  2. ⛔ **JUDGMENT-BASED FINDINGS MAY NOT BLOCK.** Style, design taste, "I'd have done it differently" → **post as a PR comment, do not `--request-changes`.** A wrong REFUTED burns a rework round and can push a good PR to `needs_human`. **The judge is allowed to be USELESS; it is not allowed to be WRONG.**
  3. **MERGE AUTHORITY STAYS WITH THE ORCHESTRATOR.** ⛔ **The judge NEVER merges.** It may `--request-changes` (a fact) or mark reviewed-clean; a clean run still lands in the orchestrator's inbox for the human/orchestrator to merge. (Standing auth = the orchestrator's, not the judge's.)
  4. **REUSE THE CARRIER — ⛔ do NOT add a second path back into the loop.** The judge drives `dispatcher.request_changes()`, exactly as the CI gate does. Statuses, rework cap (`CHELA_MAX_REWORKS`), worktree reuse, `needs_human` escalation: **all already exist. Change none of them.**
  5. **BUDGET + LOOP SAFETY.** ⛔ **The judge must not judge its own rework** into an infinite cycle — the existing rework cap bounds it, but **make the judge's verdicts count against the SAME budget** (a judge round is a rework round). ⛔ **A judge failure (crashed, timed out, gh offline) is `CANNOT VERIFY` → it does NOT block and does NOT approve; it hands the run to the human.** *Unknown is never a pass — and never a fail either.*

  ⚠️ **THE IRONY YOU MUST NOT COMMIT: the judge is itself a guard. CORRUPT IT AND WATCH IT GO RED, or you have built the very thing it exists to catch.** Feed it a PR whose guard you have *deliberately* broken and confirm it BLOCKS; feed it a genuinely good PR and confirm it does NOT.

- [ ] **🚨 THE TEST SUITE RUNS THE PRODUCTION DISPATCHER AGAINST THE PRODUCTION TRACKER — AND IT IS BLOCKING THE JUDGE.** ⛔ **HIGHEST PRIORITY: nothing can be adjudicated until this is fixed.**

  **WHAT IT ACTUALLY DOES.** `tests/test_graceful_shutdown.py` spawns a **REAL daemon** (`python -m chela.main run`) with `CHELA_DIR` pointed at a `tmp_path`. But ⛔ **`CHELA_DIR` does NOT isolate worktrees** — the workspace root comes from **`WORKFLOW.md`'s `workspace.root`** (`workflow.py:235`), which the test daemon reads from the **REAL repo**. So it loads the **real `WORKFLOW.md`**, the **real `TODO.md`**, and **dispatches REAL OPEN TASKS into the REAL `~/.chela/worktrees/`.** Captured 2026-07-14:
  ```
  Work dispatcher: ON — 1 workflow: /home/liavedunix/projects/chelamux/WORKFLOW.md
  WARNING chela.worktree: Worktree path ~/.chela/worktrees/chelamux/24d60c64fe21 exists but git has no record
  ERROR   chela.dispatcher: Dispatch failed for task 24d60c64fe21     <-- THE LIVE WALL RUN'S WORKTREE
  ```
  🔴 **On this box it merely collided with a live run and threw. ON A CLEAN BOX IT WOULD HAVE SPAWNED REAL AGENTS.** ⛔ **`pytest` must never be able to do that.**

  ⚠️ **AND IT WAS MISDIAGNOSED FOR HOURS — BY ME.** The assertion is `assert "Traceback" not in out`. I read the *line number*, called it *"a timing flake, env-only, CI is green — do not touch"*, and **told four agents exactly that.** It is not a flake and never was; **the traceback is not a shutdown bug at all.** 🔑 **READ THE ASSERTION, NOT THE LINE NUMBER.**

  1. **STOP THE TEST DAEMON DISPATCHING.** Set `CHELA_DISPATCH_WORKFLOWS` to empty in the test env — the shutdown test only needs the daemon to **boot, nap, and take a signal**. ⛔ It has no business loading the real tracker.
  2. 🔴 **THEN FIX THE FOOTGUN BEHIND IT — a test-only patch leaves the landmine armed.** `CHELA_DIR` isolates the DB but **NOT the workspace**: *any* daemon a human or a test starts with a custom `CHELA_DIR` still writes to the REAL worktrees and can spawn REAL agents. Either **confine the workspace root to `CHELA_DIR`**, or make a daemon with a non-default `CHELA_DIR` **REFUSE, LOUDLY, to dispatch into a workspace outside it.** **Pick one and justify it in the PR body.**
  3. **THE SUITE MUST BE GREEN ON THIS MACHINE, not just in CI.** ⚖️ The judge's **first verdict was `cannot_verify`**: *"the suite is NOT GREEN before any mutation … a suite that is already red measures nothing."* It refused to block **and** refused to clear — correct — **but the judge is INERT until `uv run pytest -q` is green locally.** ⛔ **Do NOT "fix" this by skipping or xfailing the test.** That re-arms the landmine and lies to the judge.
  4. **Guard it:** a test asserting a daemon under a non-default `CHELA_DIR` **cannot** touch the default workspace. ⚠️ **Corrupt it and watch it go RED — the judge will do exactly that to you.**

- [ ] **🗜️ THE WALL: A DISPATCHED AGENT'S PANE OPENS MINIMIZED — and POPS OUT when it wants a human.** Same principle as the Telegram lazy-bind (`CHELA_TELEGRAM_BIND_DISPATCHED`), second surface: ⛔ **a dispatched worker should not occupy human attention surface — a topic OR a Wall tile — until it needs a human.** Liav, 07-14. **⚠️ DEPENDS ON the lazy-bind task's `dispatched_window_ids()` helper (run-row-derived) — reuse it, do not re-derive.**

  ✅ **The machinery EXISTS — do not build a new one.** `terminals.js` already has `_minimized` (a Set), `_minimizeItem`, and a **dock**, persisted to `localStorage` `pc_wall_minimized` (`terminals.js:801-817`).

  1. **AUTO-MINIMIZE ON FIRST SIGHT ONLY.** 🔴 **"Opens minimized" is NOT "stays minimized".** A dispatched wid is auto-added to `_minimized` **the first time it is ever seen** — and never again. **Keep a persisted `seen` set.** ⛔ **Without it, the poll RE-MINIMIZES the pane every tick and the Wall FIGHTS THE HUMAN** who just pulled it out of the dock. **If Liav restores a pane by hand, it STAYS restored.**
  2. **POP OUT ON A GATE.** When a dispatched agent **blocks on a question/permission gate**, its pane **auto-restores** from the dock. ⚠️ **READ THE LAZY-BIND VERDICT FIRST (PR #87 review):** `hookgate.pending_gate` covers **ONLY** `AskUserQuestion`/`ExitPlanMode` — **a `Bash`/`Edit` PERMISSION gate is NOT in the transcript at all and is caught ONLY by the PANE SCRAPE.** ⛔ **Do not repeat that spec's mistake: the gate signal must cover PERMISSION gates too, or the pane stays docked for the most likely gate a worktree agent hits.**
  3. **IDENTIFY FROM THE RUN ROW, NOT THE NAME.** ⛔ **Do NOT regex `cmx-\d+`.** The `runs` table owns the wid. The browser needs it → **expose a `dispatched` flag on the agent record in `/api/agents`** (server-side, from the run row).
  4. 🔴 **THE `_termSig` TRAP — THIS IS THE HIGH-SEVERITY ONE.** `_termSig` (`terminals.js:1122`) is the Wall's **render-cache key**; if it changes, `buildWall` does `stage.innerHTML =` and **EVERY LIVE TERMINAL IN THE FLEET RELOADS**. ⛔ **Minimized/dispatched state must NEVER reach `_termSig`.** Patch the dock in place. **A reviewer corrupted exactly this on PR #85 and the suite stayed GREEN — so a grep-based test does NOT count.** Use the **jsdom** harness (`tests/wall.test.mjs`): real DOM, real `renderTerminals`, assert `frameFor(wid)` is the **SAME NODE** across a minimize/restore.

- [ ] **🔑 `@N` IS AN ADDRESS, NOT AN IDENTITY — STAMP EVERY PERSISTED wid WITH A TMUX EPOCH, AND MAKE A DANGLING ONE LOUD.** ⛔ **This is a REAL, MEASURED outage, not a hypothetical.** On 2026-07-14 an OOM killed the tmux server. The fleet came back **renumbered** (the orchestrator went `@0` → `@6`). `~/.chela/inbox.json` still read `{"orchestrator": "@0"}` — so the decisions inbox **queued 5 `run_review` notifications addressed to a window that no longer existed** and delivered **none** of them. **No error. No warning. No log line. `chela doctor` stayed green (14/14).** The orchestrator sat there believing "the inbox will wake me" while five finished PRs went unreviewed. **A human had to notice.**

  🔑 **THE PRINCIPLE, and it is the whole task:** *a tmux window id is an **ADDRESS**. Addresses change. **Never persist one as a KEY.*** Store the identity; **re-resolve the address every tick**. This is the SAME defect as the relay's `cwd` (CMX-70) and the event log's pre-#61 `cwd` correlation (CMX-48) — **third occurrence, third silent failure.** ⛔ **Do not fix this one consumer at a time; that is how it got to three.**

  1. **THE EPOCH — the systemic half.** When the tmux server dies, **EVERY wid in existence goes invalid AT ONCE.** That is *one* event, not N independent failures. Derive an **epoch** from the running server (its **pid + start-time** — ⛔ pid ALONE is reusable) and **stamp it into every persisted structure that holds a wid**: `~/.chela/inbox.json` (`orchestrator`, `watches`), `~/.chela/telegram-bindings.json`, and the `runs` table's `window_id`. **Epoch mismatch ⇒ every stored wid is stale BY CONSTRUCTION** → invalidate them together and re-reconcile in one tick. ⛔ **Do NOT make each consumer discover its own staleness** — that is exactly today.
  2. **VALIDATE ON USE — a dangling wid is an ERROR, not silence.** Anything that reads a persisted wid must confirm it is a **live window** first. ⛔ **Resolving to nothing must never be indistinguishable from "nothing to do."** That single confounding is what made all three of these bugs invisible.
  3. 🔴 **AN UNDELIVERABLE QUEUE MUST SCREAM — AND NOT THROUGH THE CHANNEL THAT IS BROKEN.** If the orchestrator is unreachable **and** the queue is non-empty, escalate over a path that does **not** depend on the wid: **Telegram** (the human's phone), the **dashboard**, and a **RED `doctor` fact**. ⛔ **A notification system that cannot notify you that it cannot notify you is not a notification system.**
  4. **RE-ADOPT CHEAPLY — but ⛔ NEVER GUESS.** `inbox.py:18` says the orchestrator *"is never guessed, so a notification can never land in the wrong session"* — **that principle is CORRECT; keep it.** Pushing merge authority into the wrong session is worse than a missed push. Today's real flaw is that the pointer is only **ever** refreshed as a **SIDE EFFECT of `chela watch`**, so nothing re-points it after a restart. Give it a **first-class** re-adopt (`chela orchestrator --here`, or equivalent) and make the **dead-pointer state visible** so a human knows to run it. **Self-heal what can be derived; be LOUD about what cannot.**
  5. **WHERE IDENTITY SURVIVES, SELF-HEAL — the pattern is ALREADY IN THIS REPO.** ✅ **`reconcile_bindings` DID heal**: it saw `@1`/`@113` gone, closed their topics, and created fresh ones for `@2`/`@3` — **because it re-compares its registry against live windows every 15s.** The inbox pinned once and never looked again. **Generalise `reconcile`'s discipline; do not reinvent it.** A dispatched agent's wid should be re-resolved **from its run row** (the row is the fact; the wid is a lookup).
  6. **`chela doctor` MUST ASSERT IT.** New fact: **every persisted wid resolves to a live window under the current epoch** — and it goes **RED** when one doesn't. ⛔ **This bug class is invisible precisely because nothing ever asserted it. Doctor was green through the entire outage.**

  ⚠️ **DRIVE IT LIVE, AND THE TEST MUST BE A REAL SERVER RESTART.** ⛔ **A fixture that hands you a tidy wid reproduces the blind spot, not the bug** — three agents have now shipped green tests that asserted the artifact they WROTE rather than the one that RUNS. **Use an isolated `tmux -L <socket>` server** (⛔ **a bare `kill-server` has killed the real fleet 3×** — `conftest.py` pins `CHELA_TMUX_SESSION=chela-tests-no-such-session`), **kill it, bring it back renumbered, and prove a queued notification still finds the orchestrator — or LOUDLY says it cannot.**

- [x] **📄 `docs/RESOURCE_ISOLATION.md` — why a runaway job can kill the whole fleet, and how not to let it.** ⛔ **DOCS ONLY. This task writes NO code and changes NO behaviour** — do not "helpfully" wire cgroups into the dispatcher; that was considered and **explicitly deferred** by Liav on 07-14.

  **THE FACT THE PAGE EXISTS TO CARRY — lead with it, do not bury it:**
  > **A per-job memory ceiling does not bound the box. Only a shared one does.**

  **The incident (2026-07-14, real, twice in one day — use it, it is what makes the page land).** A workflow fanned out 4 agents, **each correctly running `MEMCAP=6G memcap python …`**. That authorises **24 GB on a 19 GB box**. Every worker sat **UNDER** its own 6 GB cap (**4.4–4.7 GB measured**) — so **no cgroup ever came under pressure** — the **box** ran out, and the kernel went **global**. ⛔ **The kernel does not kill the greediest process; it kills whatever scores worst** — so it took **tmux, the PM2 God daemon, and two Claude sessions**, including the orchestrator that would have noticed. **The caps were decorative: individually binding, collectively meaningless.** (Earlier that day a bare backtest hit **14.6 GB** and did the same.)

  **What the page must say:**
  1. **The failure mode**, as above — with the measured numbers. ⛔ **An instruction in a prompt is NOT an enforcement mechanism**: the workflow *told* its agents to use `memcap` and one job still ran **bare** at 14.6 GB. That sentence is the whole lesson; it generalises past memory.
  2. **The fix that shipped (in `~/bin/memcap`, NOT in chela):** a **shared cgroup slice** — `memcap.slice`, `MemoryMax=12G`, unit file at `~/.config/systemd/user/memcap.slice`, survives reboot — bounding the **SUM** of every job at once, however many you fan out. Plus the per-job ceiling and `choom -n 800` as the second and third guards.
  3. 🔴 **THE COUNTERINTUITIVE BIT — DO NOT GET THIS WRONG, IT IS THE MOST LIKELY THING TO BE MIS-DOCUMENTED.** The shared ceiling **does NOT kill the excess job — it caps RAM and spills to swap.** **Measured:** two over-subscribed jobs pinned the slice at `RAM=1023MB / cap 1024MB`, `swap=409MB`, and **both completed**. So a global OOM is **impossible while the slice holds** — but over-subscribing **no longer kills the box, it makes jobs THRASH ON SWAP.** **→ The slice is a SAFETY NET, not a plan.** Size concurrency so the working set fits **RAM**: `concurrency × working-set < 12G`.
  4. ⛔ **NEVER raise `SLICECAP` (or any ceiling) to make a job fit.** That re-opens the incident. Lower concurrency, or make the job leaner. *(Mid-incident, claude's node heap was raised to 8 GB on a box that was already OOMing — that only makes the process a **fatter target**.)*
  5. **How to verify, not assume.** `journalctl -k | grep -i oom` · `systemctl --user show memcap.slice -p MemoryMax` · read `memory.current` / `memory.swap.current` under `/sys/fs/cgroup/…/memcap.slice`. ⚠️ **`dmesg` returning nothing means UNREADABLE, not empty — that is _cannot verify_, NOT a pass.** (That mistake was made during this very incident.)
  6. **Why chela cares** (state it, don't build it): **chela's supervisor shares a failure domain with the workers it spawns** — a dispatched agent runs `pytest` / `uv sync` / a backtest with nothing bounding it, and can take the orchestrator down with it. **Record this as a known, accepted gap with a pointer to the mitigation (run heavy work under `memcap`).** ⛔ Do not implement isolation in this task.

  **Style:** match `docs/HOOKS.md` / `docs/CONFIG.md` — declarative, ⛔-marked landmines, real commands, real numbers. **Link it from the docs index / README where the other docs are listed.** ⛔ **Do not invent numbers: every figure above is measured. If you cannot verify one, say so rather than rounding it into a claim.**

- [x] **🪟 THE SIDEBAR: COLLAPSIBLE ON DESKTOP · KILL THE FILTER · FOLD `Launch` INTO ITS DROPDOWN.** Today `index.html` stacks **three** sections — `Navigate` · `Launch` (Favorites + Recent) · `Sessions` (with a **4-chip `All/Claude/Shell/Server` filter row**). **Target: two sections (`Navigate` + `Sessions`), zero filter chips, collapsible.** Liav, 07-14, after living in it.

  1. **COLLAPSIBLE ON DESKTOP.** ⛔ **Do NOT write a second toggle.** `chela.toggleSidebar()` + `#btn-menu` + `.drawer-scrim` **already exist** — they are wired **mobile-drawer-only**. Extend the SAME control to desktop: expanded ⇄ **icon rail** (see the ASCII target below). **Persist the state** (localStorage) — a collapse that forgets itself on reload is an annoyance, not a feature. Keep the scrim/off-canvas behaviour **unchanged on mobile**.
  2. **DELETE THE `All/Claude/Shell/Server` FILTER.** 🔑 **A filter earns its chrome only when the list outruns the viewport — this one never does** (a live fleet is ~3–6 windows, always fully visible), and it burns a permanent row to filter a list you can already see. **⌘K already does jump-to.** Remove the chips + `chela.setAgentFilter()` and its state. **The TYPE stays as a per-row cue, it just stops being a filter.**
  3. 🔴 **THE TYPE CUE MUST NOT BE HUE-ALONE — Liav is RED-WEAK (deuteranomaly).** ⛔ **Three coloured dots for `claude`/`shell`/`server` is EXACTLY the failure mode.** Use a **glyph or letter** as the primary channel (e.g. `C` / `$` / `⚙`), with colour as **reinforcement only**, from an **Okabe-Ito** palette. **The row must stay fully readable in greyscale — test it that way.** → [[user_colorblind]]
  4. **FOLD `Launch` INTO THE LAUNCH DROPDOWN.** 🔑 **Favorites/Recent are LAUNCH affordances, not NAVIGATION — they were never sidebar-shaped.** Move both into the existing `New…`/launch menu (`chela.openNewMenu`) as **divided sections** (`FAVORITES` ── `RECENT`). Keep every behaviour: click-to-launch, the ★ pin/unpin, the `×` forget-a-recent, the `+` add-a-favourite (`chela.openFavAdd`). ⛔ **This DELETES the `#launcher-section` — it does not merely compress it.** `{% if terminals_enabled %}` gating must survive the move.
  5. **Only then, spacing.** With one section gone and the chips gone, the density problem is mostly solved. **Tighten gaps last, and only if it still reads cramped** — ⛔ do not open with a CSS gap pass; that is treating the symptom.

  **Target shape:**
  ```
  BEFORE                  AFTER (expanded)     AFTER (collapsed)   Launch ▾ (dropdown)
  ┌──────────────┐        ┌──────────────┐     ┌────┐              ┌──────────────┐
  │ Navigate     │        │ Navigate     │     │ ▤  │              │ FAVORITES    │
  │  Wall/Feed…  │        │  Wall/Feed…  │     │ ▦  │              │  chelamux    │
  │──────────────│        │──────────────│     │────│              │  nautilus    │
  │ Launch    +  │  ───▶  │ Sessions 3/4 │     │ ●  │              │ ──────────── │
  │  ★ chelamux  │        │  C nautilus  │     │ ●  │              │ RECENT       │
  │  ⏱ carmel    │        │  S liavedunix│     │ ●  │              │  carmel      │
  │──────────────│        └──────────────┘     └────┘              │  lea-levy    │
  │ Sessions 3/4 │                                                 └──────────────┘
  │ [All][Claude]│         2 sections, 0 chips, collapsible
  │ [Shell][Srv] │
  └──────────────┘         3 sections + 4 chips
  ```
  ⚠️ **`nav.js` renders `Navigate` from the view REGISTRY (`views.js`) — do NOT hand-write nav rows into `index.html`.** ⛔ **And do not touch `_termSig` / `buildWall` (`terminals.js`): if sidebar state leaks into the wall's render-cache key, `buildWall` does `innerHTML =` and EVERY live terminal in the fleet reloads** (the CMX-67 trap — collapsing the sidebar resizes the canvas, so this is a *live* risk here, not a theoretical one). **Collapse the sidebar with a real fleet running and confirm no terminal reloads.**

- [x] **📵 AUTO-TOPICS MUST NOT BIND DISPATCHER-SPAWNED AGENTS BY DEFAULT — but a BLOCKED one must still reach the phone.** Today `reconcile_bindings` binds **every** live *agent* window (`live_agent_windows()` — it only skips shells/servers), so **every `cmx-N` worker churns a Telegram topic**: created on spawn, closed on exit. Observed 07-14: topics `4773 (@118 cmx-69)`, `4897 (@122 cmx-69)`, `@8 cmx-70` — created and destroyed within the hour. **The forum is a human's inbox; a fleet of short-lived workers turns it into a changelog.** Liav's call, 07-14: *"I don't think we should bind topics to workflow agents, or at least default to off."*

  1. **IDENTIFY A DISPATCHED WINDOW FROM THE RUN ROW, NOT THE NAME.** ⛔ **Do NOT regex the window name for `cmx-\d+`.** The `runs` table **owns** the `wid` of every window the dispatcher spawned — read it back from the owner (the same runtime-truth rule CMX-69 turned into a merge gate). A name is a label; the run row is a fact.
  2. **DEFAULT OFF, VIA A TOGGLE.** Add `CHELA_TELEGRAM_BIND_DISPATCHED` (**default `false`**), alongside the existing `CHELA_IGNORE_WINDOWS`. Human-driven sessions (orchestrator, project sessions) keep their topics exactly as today. ⛔ **The default flips behaviour for a LIVE bridge — say so in the PR body.**
  3. 🔴 **THE TRADE-OFF THIS TASK MUST NOT SILENTLY EAT — and the reason it isn't just a filter.** The **entire point** of the 07-14 phone-gate surface is that an agent blocking on a question/permission reaches Liav's phone with zero keypresses. **Unbinding dispatched agents would throw that away** for exactly the agents most likely to need it. **→ Therefore: BIND LAZILY, ON DEMAND.** A dispatched window gets **no topic while it is working**, and the topic is **created the moment it has a PENDING GATE** (`hookgate.pending_gate(wid)` already answers this — it is the same signal `gatewatch` polls). It goes quiet again when the gate resolves / the agent exits. **Result: the forum shows only agents that WANT A HUMAN.** That is the feature, not a compromise.
  4. **Don't regress the room/inbox paths.** Unbinding a topic must not detach the window from the decisions inbox or the Wall — ⛔ **the Telegram binding is a VIEW, not the agent's identity.**

- [x] **❓ A PENDING INTERACTIVE GATE DID NOT REACH TELEGRAM FROM THE ORCHESTRATOR WINDOW — root-cause it.** ⛔ **UNEXPLAINED. Do not "fix" it before you can REPRODUCE it — the first job is a repro, not a patch.** On 2026-07-14 the orchestrator (`@6`, cwd `~`, **bound to topic 5067**) raised **two `AskUserQuestion` gates**; both rendered in the pane and **neither appeared in Telegram**. Liav: *"interesting, I didn't get this question on Telegram."*

  🔑 **THIS IS NOT THE CMX-70 (cwd→session_id) BUG — DO NOT ASSUME IT IS, AND DO NOT LET CMX-70 CLOSE IT.** They are different transports: a **pending** interactive `tool_use` lands in the JSONL **only at RESOLUTION** ([[reference_chela_hooks_supersede_pane_scraping_2026-07-13]]), so the **transcript monitor structurally cannot carry a gate that is still open.** Gates travel by **hooks** (`hookgate.py` / `gatewatch.py`), and hooks **were** firing throughout (`POST /hooks/PreToolUse` → 200 on the dashboard, continuously).

  **What was already ruled out (don't re-do it):** binding existed (topic 5067, created 20:44) · inbound worked (`Telegram → @2` relayed fine) · hooks were posting 200 · `chela-telegram` was online with no errors in its log.

  **Where to look, in order.** (a) `gatewatch._poll_window` **corroborates the log against the PANE before posting** — if the pane check misses an `AskUserQuestion` rendering (vs. a Bash/Edit permission prompt, which is the shape it was built and tested on), the gate is found and then **discarded**. (b) `hookgate.pending_gate` is **scoped to the current `boot_id`** — the tmux server AND the PM2 daemons were **restarted by an OOM at 19:17**; check a gate raised *after* a `boot_id` rollover is still visible. (c) `INTERACTIVE_TOOLS` — confirm `AskUserQuestion` is actually in it on the **installed plugin**, not just in the repo (⚠️ **hooks are read at agent STARTUP**; the orchestrator session predates nothing here, but *verify*, don't assume).

  ⚠️ **The 07-14 bug class applies with full force: 8 green tests lied because they asserted the artifact we WROTE, not the one that RUNS.** ⛔ **A passing unit test is NOT a fix here. Drive a REAL `AskUserQuestion` from a REAL bound window and watch it land on the phone.**

- [x] **🚦 A RED CI SENDS THE PR BACK — automatically, with no reviewer.** ⛔ **Chela is BLIND to CI.** It reads only `state,mergeable` off a PR (`dispatcher.py:_pr_state`) — and `mergeable` is GitHub's **merge-conflict** field, **not the checks**. `statusCheckRollup` is never fetched; nothing in the codebase knows a CI run exists. **This bit us for real on 2026-07-14: CI was RED on BOTH PR #80 and #81 and nobody noticed — #80 was MERGED red, and `dev` was broken until a hotfix (`23664e2`).** The agent reported "tests pass"; the orchestrator reviewed the code; **neither consulted the artifact that actually governs whether the thing can ship.** ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–5 + Landmines + Verify. Do not implement from this summary line alone.**

  🔑 **WHY THIS IS THE RIGHT FIRST AUTOMATIC DRIVER OF THE LOOP: a failing check is a FACT, not a judgment.** The carrier (CMX-68, merged) already sends a PR back to its agent. An *automated reviewer* would need judgment — and a wrong `REFUTED` burns a whole rework round. **CI needs none.** ⛔ **This task therefore does NOT build the judge, and does not review anything.** It wires an existing, mechanical, un-arguable signal into an existing loop.

  1. **READ THE CHECKS FROM THEIR OWNER.** Extend the PR poll to `gh pr view <n> --json state,mergeable,statusCheckRollup` (or `gh pr checks`). ⛔ **GitHub owns this fact — read it back from GitHub; never infer it from a local test run** (rule (b) of the runtime-truth registry: *a fact another system owns must be READ BACK from that system*). The local suite passing is **not** evidence: that is the exact hole this task closes.
  2. **RED CI → `changes_requested`, with the failure as the verdict.** Reuse **`dispatcher.request_changes()`** (CMX-68) — ⛔ **do not add a second path back into the loop.** The verdict body = the failing job names **plus the tail of the failing log** (`gh run view <id> --log-failed`, truncated — a whole CI log will not fit a prompt). The agent then does what it already does: read the PR thread, fix, push, `task-finished`.
  3. 🔴 **ONLY WHEN THE CHECKS HAVE SETTLED.** A PR opens with checks **`PENDING`** — ⛔ **a pending run is NOT a red one**, and sending an agent back mid-CI would be a loop that fights itself. Act only on a **conclusive** failure (`FAILURE`/`ERROR`/`TIMED_OUT`/`CANCELLED`), never on `PENDING`/`QUEUED`/`IN_PROGRESS`/`NEUTRAL`/`SKIPPED`. **Re-check on the tick** — the poll already runs.
  4. 🔴 **EACH RED FIRES ONCE — the loop must not re-fire on a verdict already delivered.** Key the trigger on the **head SHA** of the run that failed (persist it), so the *same* failing commit cannot spend a second rework round; a **new push** that is *also* red is a **new** verdict and may. ⛔ Without this, a run that fails CI, gets reworked, and fails again on an unchanged SHA will burn the whole `CHELA_MAX_REWORKS` budget in three ticks and land in `needs_human` having done nothing.
  5. **THE GATE ON MERGE.** `chela review --approve` (and the `dispatch-runs --awaiting` view) should **refuse / loudly flag** a run whose checks are red — ⛔ **the orchestrator must not be ABLE to merge a red PR by accident, because it did exactly that today.** Surface the check state in `dispatch-runs` and on the Kanban card. (Merge authority itself stays with the orchestrator — ⛔ this task does **not** auto-merge.)

  **Landmines:**
  - 🔴 **A red CI on a PR whose run is already `done`/merged must do NOTHING** — no resurrection. (CMX-68 added the compare-and-swap; use it.)
  - 🔴 **`gh` missing / offline / rate-limited = UNKNOWN, not GREEN.** ⛔ A check state you could not read is **never** a pass — that is the doctor rule (*an owner that cannot be read is CANNOT VERIFY, loudly*), and treating unknown as green re-opens the very hole this closes.
  - A PR with **no** CI configured must not be treated as red (no checks ≠ failing checks) — but **say so** rather than silently passing.
  - **Cost:** `gh run view --log-failed` on every tick is heavy. Fetch the log **only** on the transition into red, not per tick.
  - The dispatcher is a live PM2 process — `pm2 restart chela-daemon` after merge or nothing changes.
  - Keep `uv run ruff check chela tests` green + `uv run pytest -q` + `node --test tests/*.test.mjs`.

  **Verify — ⛔ a check never SEEN to go red is not a check (CMX-65/66 bar). And ⛔ CHECK CI ON YOUR OWN PR before you declare done: `gh pr checks <n>`. A local pass is NOT evidence — that is the whole point of this task.**
  - **Unit** (stub `gh`): (a) a conclusive **FAILURE** → `changes_requested` **once**, verdict carries the job name + log tail; (b) **PENDING/QUEUED/IN_PROGRESS → NOTHING happens** (the single most important test here); (c) the **same failing SHA** cannot fire twice; a **new** red SHA can; (d) `gh` unavailable → **UNKNOWN**, never a pass, and it says so; (e) a red PR whose run is `done` → **no resurrection**; (f) `--approve` refuses a red PR; (g) no-checks ≠ red.
  - **LIVE (required):** on a **throwaway branch**, open a PR that **fails CI on purpose** (one deliberately broken test), and show the loop turn end to end **with no human**: CI goes red → the run flips to `changes_requested` → the dispatcher re-spawns the agent **in its original worktree** → the verdict it receives **contains the real failing job + log tail**. **Report the observed transitions.** Then delete the branch.

  **Reference:** `chela/dispatcher.py` — `_pr_state` (the PR poll: where checks must be read), `request_changes` (**the existing way back into the loop — reuse it**), `REVIEW_STATUSES`/`NOT_CLAIMABLE`, `_respawn_rework`, the reconcile loop · `chela/main.py` (`cmd_review`, `cmd_dispatch_runs`) · `chela/runtime_truth.py` (**read a fact back from its OWNER; unreadable = CANNOT VERIFY, never a silent pass**) · `.github/workflows/ci.yml` (node is **pinned** — an unpinned runtime is how #80 went red unnoticed) · PR **#80/#81** and commit **`23664e2`** (the incident this task exists to prevent).

## Open — the rework loop

- [x] **🔁 THE REWORK LOOP — a PR that FAILS REVIEW has nowhere to go.** Found live on 2026-07-14 reviewing PR #80: the dispatcher can **claim** a task and **reconcile** a merged PR, but `awaiting_review` is a **terminal state unless a human intervenes**. Three real defects were found in #80; there was **no mechanism to send them back**, so the orchestrator had to **hand-spawn** a fix agent into the worktree. **This task builds THE CARRIER (the loop). It does NOT build the judge** — the orchestrator stays the reviewer. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–6 + Landmines + Verify — they carry load-bearing constraints (the GitHub self-review block, the slot arithmetic, the worktree reuse). Do not implement from this summary line alone.**

  **Decided with Liav (07-14):** carrier now, **judge next** (a separate later task auto-spawns the adversarial reviewer *on top of a loop that already works*). ⛔ **Do not build an automated reviewer in this PR.**

  1. 🔴 **THE AUTHORITY IS THE RUN ROW — NOT GITHUB. This is MEASURED, not a preference.** GitHub **refuses** `gh pr review --request-changes` on a PR authored by the same account: *"Can not request changes on your own pull request"*. Our whole fleet is one account → **`reviewDecision` is `null` FOREVER** and is **USELESS as a trigger.** ⛔ **Do not build on it.** So: add a **`changes_requested`** status to the `runs` table (`chela/dispatcher.py`), and it is **the** authority. The PR comment is the **human-readable projection**, never the source of truth. (This is exactly rule (a) from the runtime-truth registry: *the process that ACTS publishes what it actually DID*, and readers read **that**.)

  2. **The verdict command** — `chela review <run-or-task-id> --request-changes --body-file -` (read the body from stdin; a verdict is long-form markdown and must not be shell-quoted). It (a) writes `changes_requested` + stores the verdict body + increments nothing yet, (b) posts the body as a **PR comment** (`gh pr comment`, **not** `gh pr review` — see item 1). Also `chela review <id> --approve` → leaves the run in `awaiting_review` for the human merge (⛔ **this task does NOT auto-merge; merge authority stays with the orchestrator**).

  3. **THE RE-SPAWN — reuse the EXISTING worktree.** A run in `changes_requested` is picked up by the dispatcher tick and **re-spawned in its original worktree, on its original branch**, so the branch history, the PR, and the agent's context are preserved (this is what the human did by hand today, and it worked). Its prompt = **the stored verdict**, plus an instruction to **read the PR comments itself** (`gh pr view <N> --comments`) — the comment is the durable record. Transition `changes_requested` → **`running`**. ⚠️ **If the worktree is GONE** (cleaned up / never existed), **recreate it from the branch** — do not fork a fresh one from `dev` (you would lose the branch). If the **branch** is gone, that is a hard error → `needs_human`.

  4. 🔴 **SLOT ARITHMETIC — get this right or the fleet jams.** `active` counts **only** `('claimed','running')` (`dispatcher.py:1116-1118`) — **`awaiting_review` does NOT hold a slot** (that is why a new task is claimed the moment a PR opens). Therefore: a run re-entering `running` for rework **RE-CONSUMES a slot** and must respect `max_concurrent` — ⛔ **a rework must NOT be able to exceed the concurrency cap.** If every slot is busy, the rework **waits its turn** like any other work. ⛔ **It must NEVER preempt a claimed/running run** — `hold.py` says preemption is *deliberately* NO, and that stands.

  5. **THE CAP — `MAX_REWORKS = 2`, then ESCALATE. Never spin.** Track `rework_count` on the run. On the 3rd would-be round: transition to **`needs_human`**, **preserve the branch AND the worktree** (nothing is thrown away), **FREE the concurrency slot** (⛔ a stuck run must not block the queue forever), and **push it to the decisions inbox** with **the history of what was tried** (every prior verdict, not just the last). Mirrors `rooms.py`'s `MAX_HOPS` — *a bounded loop that surfaces rather than spins.* Make the cap configurable (`CHELA_MAX_REWORKS`).

  6. **CLOSING THE LOOP.** The reworking agent pushes → the existing PR updates → the run goes back to **`awaiting_review`** → the **inbox wakes the orchestrator** (that path already works — it is how #80 was reviewed). Surface `changes_requested` / `needs_human` in **`chela dispatch-runs`** (extend the existing `--awaiting` filter; **reuse the existing query — no second source of truth**) and as a **run-state toast** in the dashboard (the edge-triggered toast from the orchestrator-awareness task already exists — **extend it, do not add a second poller**).

  **Landmines:**
  - 🔴 **`gh pr review --request-changes` DOES NOT WORK on our own PRs — measured today, it hard-errors.** Use `gh pr comment`. If you find yourself reading `reviewDecision`, you have built on sand.
  - 🔴 **A rework re-spawn must use the TWO-STEP window pattern:** `tmux new-window -a -t chela -n <name> -c <worktree>` **then** `send-keys 'claude …' Enter`. ⛔ **NOT `tmux new-window '<cmd>'`** — claude then *is* the pane process, `agent_manager.claude_pid()` (`pgrep -P`) never correlates it, and **it never gets a Telegram topic**. (The dispatcher already does this correctly — **copy its existing spawn path, do not write a new one**.)
  - The `runs` table needs a **migration** for the new statuses + `rework_count` + the verdict body. Follow the existing migration pattern; an old DB must keep working.
  - **Reconciliation must not fight the loop:** a run in `changes_requested`/`needs_human` whose PR gets **merged anyway** (a human merged it) must reconcile to `done` cleanly — check the `status IN (...)` lists at `dispatcher.py:930` and `:1138` and add the new states everywhere they belong. ⛔ **Every hard-coded status list you miss is a silent hole.** Grep for **all** of them.
  - **The dispatcher is a running PM2 process** — it loads Python from disk. `pm2 restart chela-daemon` after merge or nothing changes.
  - Keep `uv run ruff check chela tests` green + `uv run pytest -q` + `node --test tests/*.test.mjs`.

  **Verify — ⛔ GREEN TESTS HAVE LIED 9-FOR-9 HERE. A check never SEEN to go red is not a check (CMX-65/66 bar).**
  - **Unit:** (a) `chela review --request-changes` writes `changes_requested` **and** posts a PR comment (gh stubbed); (b) the tick re-spawns **into the existing worktree on the existing branch** — assert the worktree path and branch are the *original* ones, **not a fresh fork from `dev`**; (c) `rework_count` increments and **round 3 → `needs_human`**, slot freed, **all prior verdicts** carried into the inbox payload; (d) 🔴 **a rework CANNOT exceed `max_concurrent`** (set max=1, one run already `running`, a `changes_requested` run must **wait**, not spawn); (e) a merged PR in `changes_requested` still reconciles to `done`; (f) worktree-missing → recreated from branch; branch-missing → `needs_human`.
  - **REGISTRY (CMX-66 discipline):** if this introduces a runtime fact that lives on **both sides of a process boundary**, add it to `chela/runtime_truth.py` **with its red test** — ⛔ **do not hand-write a doctor check.**
  - **LIVE (required — this is a dispatcher change; a green suite is not evidence):** drive one **full round trip** end to end on a throwaway task: dispatch → PR → `chela review --request-changes` → **watch the agent re-spawn in the SAME worktree** → it pushes → run returns to `awaiting_review` → orchestrator woken. **Report the observed state transitions and the worktree path in the PR body.** Then drive the **cap**: force `MAX_REWORKS=0` or `1` and show it lands in `needs_human` with the slot freed.

  **Reference:** `chela/dispatcher.py` (run states + `mark_awaiting_review` :743, reconcile :888-961, the claim loop + slot count :1115-1140, the **existing** worktree + spawn path) · `chela/hold.py` (**preemption is deliberately NO** — read the comment) · `chela/inbox.py` (the escalation path) · `chela/rooms.py` (`MAX_HOPS` — the bounded-loop precedent) · `chela/runtime_truth.py` (the registry + the red-test bar) · `chela/main.py` (`cmd_dispatch_runs`) · PR **#80** (the review that exposed the gap — read its comment for the shape of a verdict).

## Open — the wire gesture

- [x] **🔌 THE WIRE GESTURE — drag a line between two Wall tiles to put their agents in a room.** The relationship already exists (CMX-61: `chela/rooms.py` — rooms, typed kinds, active dispatch, loop guards, `SessionStart` recap, all shipped and live-verified). **This task is a UI for `chela room join`, plus the HTTP API it needs. NOTHING MORE.** ⛔ **If you find yourself editing `rooms.py`'s protocol (`RELAY_HEADER`, `MAX_HOPS`, `sanitize`, `build_prompt`, the kind semantics), you have drifted out of scope — stop.** ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify beneath this line — they carry the load-bearing constraints (the `_termSig` trap, the pointer-events idiom, the mousedown steal). Do not implement from this summary line alone.**

  **The decision is SETTLED (Liav, 07-14): the wire lives on the WALL, not in a new graph view.** A graph view that *renders* the rooms the wire creates is a later, separate task — ⛔ **do not build both at once, and do not add a view.**

  1. **BACKEND — the rooms HTTP API (~70 LOC py).** `rooms.py` is wired only to the **CLI and hooks**; `grep api/room chela/dashboard/app.py` returns **nothing**. Add to `chela/dashboard/app.py`: **`GET /api/rooms`** → rooms + members (keyed by **wid** — which is exactly what the Wall has) and **`POST /api/rooms/join`** → `{wids: ["@88", "@4"], room?: "..."}` → `rooms.create()` + `rooms.join()` for both. **Reuse `chela/rooms.py`'s existing functions** (`create` :192, `join` :205, `leave` :219, `members` :228, `rooms_for` :236) — ⛔ **no second store, no reimplementation of membership.** **Naming a room on drop:** auto-name it (derive from the two agents / a short slug); do **not** block the gesture on a modal text prompt — a drop must complete in one motion. Adding a third agent = drag a wire onto a tile already in a room → it **joins that room** (don't create a second one).

  2. **THE GESTURE — a drag overlay on the Wall (~230–280 LOC JS/CSS).** ✅ **The iframe fear is DEAD — the repo already solves it TWICE, and you must reuse the existing idiom rather than invent a shield div:**
     - `guard(on)` flips `.gs-dragging` on the grid → CSS `style.css:613` sets **`pointer-events: none` on every `.term-frame`** (`terminals.js:1511-1518`, wired at `:1527-1533`). **Flip that same class on wire-drag-start and `document.elementFromPoint` works.** There is also a `.term-dragging` mode (`style.css:622`) that hides frames entirely — that's for gridstack's own drag; **the wire should NOT hide the terminals** (watching them is the whole point), so use the `pointer-events` half only.
     - Gridstack's **locked-swap** mode is already a drag-a-tile-onto-another-tile gesture with live hover highlight (`_swapTargetWid` :1751, `_setSwapHover` :1766, `.swap-target` `style.css:633`). **Read it before writing yours.**
     - **The port:** `paneHead()` (`terminals.js:671-704`) is real chrome **outside** the iframe, already `position: relative` (`style.css:191`), already a flex row with a right-hand `.gs-keys` cluster. Add the port (`○`) there — ~5 LOC.
     - **The overlay:** an absolutely-positioned **SVG** inside `#term-stage`, `pointer-events: none`, drawing a **live bezier** that follows the cursor. **There is no SVG/canvas overlay anywhere in the dashboard yet — you are adding the first one.** (Precedent for SVG node-link drawing, static only: `knowledge.js:309-339`.) Mind the stacking: maximized pane is **z-index 150** (`style.css:513`), share popover **100000**, dragging item **100**.
     - **Discoverability — copy Mosaic's trick, it is what makes the feature findable: EVERY tile sprouts a temporary drop socket (`◉`) while a drag is in flight.** Without it nobody discovers the wire exists. Clear them all on drop/cancel. **Esc cancels** the drag.
     - **Resolving the drop:** `e.target.closest('.grid-stack-item').getAttribute('gs-id')` → the wid (`gs-id="@88"`, `terminals.js:1452`) → `_agentByWid()` (`:54-56`). ⛔ **A drop on the source tile, on empty stage, or on a tile with no wid = CANCEL, not a room of one.**

  3. **THE ACCENT — in-room tiles wear a shared border.** Patch it **in place**, in the mould of `_colorTermDots` (`terminals.js:897-907`, which already does `classList.toggle` on `.grid-stack-item-content`): write `_applyRoomAccents(rooms)` → `classList.toggle('in-room', …)` + a CSS var for the accent on the `[gs-id]` tile. ~20 LOC. ⚠️ **Liav is red-weak (Okabe-Ito, hue is NEVER the only signal)** — the shared accent border must be paired with a **non-hue cue** (the room's name/slug on the header, a glyph — e.g. `🔌 wire`). A colour alone is not an answer.

  4. **Scope guard:** this task creates rooms **from the Wall**. It does **not** add a graph/topology view, does **not** add read-only data-source panes (a later idea), and does **not** touch the room *protocol*. Room **leave** from the UI is in scope only if it falls out for free (a click on the accent → leave); otherwise leave it for later and say so.

  **Landmines:**
  - 🔴 **THE ONE HARD RULE: room state must NEVER reach `_termSig`.** The wall's render loop (`renderTerminals`, `terminals.js:1073`) early-returns on an unchanged signature `_termSig = mode | selectedWid | sorted-wid-list` (`:1110`). If the signature changes, `buildWall` (`:1467-1485`) does `stage.innerHTML = …` and **destroys and recreates every iframe — i.e. every live terminal reloads on every room change.** Room membership is *presentational per-tile state*: patch it surgically (item 3) and call `_syncTermSig()` (`:1399-1402`) if you mutate structure, exactly as the existing surgical paths do (`addWallTiles` :1551, `dropTerminalPane` :1404, `_refreshPaneLabels` :1365).
  - 🔴 **The port must `stopPropagation` on `mousedown` or Gridstack steals the gesture** (drag handle is `.gs-grip`, `terminals.js:1495`). Precedent: `renamePane` does exactly this at `terminals.js:355`.
  - The overlay must coexist with **`pane-maximized`** (z-index 150) and with locked-drag's `overflow: hidden` on `.grid-stack` (`terminals.js:1522-1526`).
  - **Single-pane mode** (`.term-pane`, `:1146`) has a header but **no `gs-id`** and is mobile-forced-only (`:1082`) — the wire is a **wall-mode** gesture; degrade gracefully (no port), don't crash.
  - A wid can be **gone** between the `GET /api/rooms` and the render — never assume a member still has a tile.
  - Keep `uv run ruff check chela tests` green + `uv run pytest -q` + `node --test tests/*.test.mjs`.

  **Verify — ⛔ GREEN TESTS HAVE LIED 9-FOR-9 ON THIS PROJECT. DRIVE IT LIVE, IN A BROWSER, AND SAY SO IN THE PR.**
  - **Unit/node:** (a) `POST /api/rooms/join` with two wids creates the room and both memberships, and is **idempotent** (re-joining doesn't duplicate); (b) a drop resolving to the **same** wid, or to no wid, creates **nothing**; (c) `GET /api/rooms` reflects a CLI-created room (**the API and `chela room status` agree — one authority**); (d) the pure hit-test/bezier geometry, node-tested (`feedmodel.js` is the precedent for a pure, node-tested model).
  - 🔴 **(e) THE REGRESSION TEST THAT MATTERS: prove a room change does NOT reload the terminals.** Assert the iframe **elements are identity-preserved** (same node, `src` untouched) across a room-state update — i.e. `_termSig` is unchanged and `buildWall` did not run. *A test that would have caught the whole bug class if it existed.*
  - **LIVE (required, in a real browser on the real Wall with ≥2 live agents):** drag from a tile's port → the bezier follows the cursor, **every other tile shows a drop socket** → drop on a peer → both tiles wear the shared accent + non-hue cue → **`uv run chela room status` shows the room with both wids** → `chela room post <room> --kind question --from @A --to @B -- "…"` still wakes @B. **Report the actual observed sequence in the PR body, and confirm the terminals did NOT reload.**

  **Reference:** `chela/rooms.py` (the protocol — **read, do not edit**) · `chela/dashboard/app.py` (add the endpoints; SSE `/api/events` is the **delta pipe**, `/api/log` is the Feed's — don't confuse them) · `chela/dashboard/static/js/terminals.js` (**100K**: `_wallTileHTML` :1448, `paneHead` :671, `guard`/`dragMode` :1511, locked-swap `_swapTargetWid` :1751 / `_setSwapHover` :1766, `renderTerminals` :1073, `_termSig` :1110, `buildWall` :1467, `_colorTermDots` :897, `_syncTermSig` :1399, `_agentByWid` :54, `_gridItemEl` :1746, `focusPaneByWid` :2048) · `chela/dashboard/static/style.css` (:613 `.gs-dragging`, :622 `.term-dragging`, :633 `.swap-target`, :191 `.gs-head`, :513 maximized) · `knowledge.js:309-339` (SVG node-link precedent) · `chela/dashboard/static/js/feedmodel.js` (pure node-tested model precedent).

## Open — retire the bug class

- [x] **🧭 THE RUNTIME-TRUTH REGISTRY — retire the class, not the tenth instance of it.** **NINE times on 2026-07-13/14, chela checked the artifact it PRODUCED instead of the one that RUNS.** Every one was green everywhere while the feature was dead or the state was being corrupted:
  | | the artifact we wrote | the artifact that actually ran |
  |---|---|---|
  | CMX-41 | `chela plugin` rendered port **5001** | dashboard **bound 5005** → every hook POSTed into a closed socket; the whole feature silently dead |
  | CMX-51 | "this topic needs renaming" | Telegram: *already named that* — a closed loop that burned the rate limit forever |
  | CMX-53 | env file says dispatch **ON** | daemon came up **OFF** → **9-hour outage**; `/api/settings` *also* lied (it auto-discovers a `WORKFLOW.md` on disk) |
  | CMX-56 | rendered plugin, `timeout: 120` | **installed cache**, `timeout: 2` → the zero-keypress answer path silently dead all afternoon |
  | CMX-56 (test) | a test asserting a manifest | it was reading **the developer's real plugin cache** — green by luck |
  | CMX-59 | "a hermetic suite" | it **WROTE 43 fake events into the live log** |
  | CMX-60 | `run_review` resolves its `wid` | the window was **already reaped** — it never fired for the events it was written for |
  | CMX-63 | `manifest_drift` (CMX-56's *own fix*) | compared only **`entries[0]["hooks"][0]`** → a stale `SessionStart` read **green** |
  | CMX-65 | `uv run pytest -q` = 980 green | **3 JS suites executed by NOTHING**, one of them **RED on `dev`** |
  **The class, in one sentence: _a fact lives in two places across a process boundary, and the checker reads the copy it OWNS instead of the copy that GOVERNS BEHAVIOUR._** ⛔ **This is NOT a Symphony problem and NOT an ACP/inter-agent problem** (Liav asked — the answer is neither: Symphony is the *dispatcher* spec and only CMX-53 even touches it; ACP is about *reading an agent*, which hooks already solved). **It is a configuration/state-architecture problem** — and chela already solved it ONCE, correctly, for **events**: `event_log.read()` is *"the ONE authority the UI and the inbox both read."* That discipline was simply never applied to **runtime facts**. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  1. **BUILD THE REGISTRY — one entry per fact chela depends on: `(name, declared_by, owned_by, read_back)`.** `declared_by` = where *we* write it (env file, rendered manifest, a config default). `owned_by` = **who actually governs the behaviour** (the bound socket, the running daemon, Claude Code's installed plugin cache, Telegram, tmux, GitHub, the pytest collector). `read_back` = **how to ask the owner what is really in force.** Facts today's nine bugs prove must be in it: the **dashboard port**, the **daemon's effective capabilities** (dispatch/reconcile/scheduler/inbox/notify/terminals), the **installed plugin manifest** (every event × `type`/`url`/`command`/`timeout` — not just the first), the **tmux session name**, **window liveness**, the **dispatch hold**, `CHELA_DISPATCH_WORKFLOWS` + each `WORKFLOW.md` (exists? parses? tracker exists?), and **the test suites pytest actually executes**. Add the ones you find.
  2. **GENERATE `chela doctor` FROM IT — do not hand-write another check.** `chela/doctor.py` is today **seven bespoke `_check_*` functions** (`_check_env_file`, `_check_drift`, `_check_session`, `_check_dashboard_port`, `_check_plugin`, `_check_daemon`, `_check_hold`). **Every hand-written check acquires a private blind spot — that is LITERALLY how CMX-63 happened** (`manifest_drift` compared only the first hook) **and how CMX-65 happened** (a wrapper naming one `.mjs` file). An **enumerated** check structurally cannot. Doctor becomes: *for every fact in the registry → read the declared value, read back the owned value, compare, report.* ⛔ **A fact whose owner cannot be read must be reported LOUDLY as "cannot verify" — NEVER as a silent pass.** A doctor that goes green because it could not look is the bug, one level up.
  3. **CODIFY THE TWO RULES — in code, not in a comment.** **(a) For facts chela OWNS: the process that ACTS publishes what it actually DID.** Not what it was configured to do. We invented this **three separate times by accident, each after being burned** — `dashboard.port`, `daemon.json`, and the run row's `window_id` (CMX-62). Make it the *rule*: a chela process publishes its **effective** state (pid-checked) at startup, and every reader reads **that**, never the config. **(b) For facts ANOTHER SYSTEM owns — READ BACK FROM THE OWNER.** The plugin cache belongs to Claude Code; the topic name to Telegram; window liveness to tmux; the executed test list to the collector. ⛔ **You cannot infer these from your own copy. You must ask.** (That is what `installed_plugins.json` does, and it is why doctor finally caught real drift.)
  4. **PROVE EACH CHECK CAN FAIL.** A check that has never been seen to go red is not a check — CMX-65 established this and it is the acceptance bar here: for **every** fact in the registry, a test that **breaks the owned value and asserts doctor FAILS**, naming the fact. ⛔ *"The check passes"* is not evidence. *"I broke it and the gate went red"* is.
  **Landmines:** ⛔ **do not turn this into a config FRAMEWORK** — one registry, one generated doctor, done; a framework is a new artifact that will itself drift; don't regress the existing checks' behaviour (CMX-53's WARN-when-dispatch-is-off, CMX-56's ERROR-on-plugin-drift, the "I am inferring, not observing" wording when no daemon runs — that honesty is the *point*); doctor must stay **interactive-fast** (read files, don't shell out to `claude`); **a read-back can FAIL** (the daemon is down, tmux is gone, the plugin cache moved between Claude Code releases) — that is **"cannot verify" (loud)**, never green and never a crash; ⚠️ **the registry itself is an artifact that can drift from reality** — say plainly, in its docstring, how *it* is kept honest (item 4 is the answer: every entry must have a test that proves its check can go red); don't break `chela doctor`'s exit codes (0 green / 1 error — the dispatcher and the operator both read them). Keep `uv run ruff check chela tests` green + `uv run pytest -q` + `node --test tests/ chela/dashboard/static/collab/`. **Reference:** `chela/doctor.py` (**the seven bespoke checks this replaces — read them all first**), `chela/capabilities.py` (CMX-53 — the daemon's effective state, already published to `daemon.json`), `chela/hooks.py` (`installed_plugins()` / `manifest_drift` — the read-back-from-the-owner pattern that works, **and the shallow compare that failed**), `chela/config.py` (`live_dashboard_port()`, `CHELA_DIR`, `DISPATCH_WORKFLOWS`), `chela/hold.py`, `chela/dispatcher.py` (the run row's `window_id` — the third accidental instance of rule 3a), `tests/test_js_suites.py` (CMX-65 — **discovery over a hand-written list; copy this exactly**), `chela/event_log.py` (`read()` — *"the ONE authority"*, the discipline being generalised). **Verify:** unit-test that the registry enumerates every fact (a new fact added to the registry is checked with **no** new doctor code); that an unreadable owner reports **"cannot verify"**, not green; that doctor's exit codes are unchanged; **and — the acceptance bar — that for EVERY registry entry there is a test which corrupts the owned value and asserts doctor goes RED, naming the fact.** **LIVE:** with the fleet running, corrupt each owned value in turn (stale plugin manifest, daemon started with dispatch off, a `--port` the dashboard did not bind, a hold left behind) and show `chela doctor` failing loudly and specifically **every time** — then restore and show it green.

## Done — the Feed (log UI) + sidebar

- [x] **🔴 A JS TEST HAS BEEN RED ON `dev` AND `pytest` CANNOT SEE IT — the suite that runs is not the suite that exists.** Found 2026-07-14 while reviewing CMX-64 (which reported it honestly and did not cause it). `node --test tests/views.test.mjs` on **`dev`** → **10 pass, 1 FAIL**: *"the Feed reads /api/log and resumes from next_seq (never last_seq)"* (`tests/views.test.mjs:115`). Meanwhile `uv run pytest -q` reports **980 passed, all green** — because **`tests/feed.test.mjs` IS wired into pytest** (via `tests/test_feed_js.py`, the `tests/test_keys.py` pattern) **and `tests/views.test.mjs` is NOT.** A test that exists but is never executed is not a test; it is a comment that costs CI nothing to ignore. ⚠️ **This is the SAME CLASS as everything else fixed today** — CMX-41 (rendered port ≠ bound port), CMX-53 (env file ≠ running daemon), CMX-56 (rendered plugin ≠ installed cache), CMX-59 (the suite writing live state), CMX-63 (`manifest_drift` compared only the first hook): **chela keeps checking the artifact it produced instead of the one that runs.** Here the artifact is *the test suite itself.* ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **WIRE EVERY `.mjs` TEST INTO `pytest` — by DISCOVERY, not by a hand-written list.** `tests/test_feed_js.py` runs *one named file*; a second hand-maintained list is how the third `.mjs` file will go unrun too. Glob `tests/*.test.mjs` and run them all (one pytest item per file, so a failure names the file). ⛔ **Prove the wiring itself**: make a `.mjs` test fail on purpose and confirm **`uv run pytest -q` goes red**. A wiring that cannot be shown to fail is not wiring. (If `node` is absent, **skip loudly with a reason** — never silently pass; a silent skip is this bug wearing a different hat.)
  2. **Then fix the actual failure — do not delete the assertion.** The test asserts the Feed resumes from **`next_seq`, never `last_seq`**, and that contract is REAL and load-bearing: `event_log.read()`'s own docstring says *"Resume from `next_seq`, never from `last_seq`: `limit` takes the `limit` OLDEST events after the cursor, so a bounded read is resumable and can never skip an event the caller has not seen."* Resume from `last_seq` and a client that hit its `limit` **silently skips every event it did not receive.** CMX-64 notes the test greps for the string `batch.last_seq`, which CMX-60's bounded catch-up loop *legitimately* uses for a different purpose — **so decide which is wrong: the test's crude string-grep, or the code.** Read both. If the code is correct, the assertion must test the **behaviour** (a bounded read resumes without skipping) rather than grepping for an identifier; if the code is wrong, fix the resume. ⛔ **Do not make it pass by deleting it** — this is the exact assertion that stops a Feed from silently losing events.
  3. **Sweep for the same shape.** Any other test file, linter, or check that exists in the repo but is **not** on the path a bare `uv run pytest -q` (and CI) actually walks. Name what you find; wire it or delete it. **A check nobody runs is worse than no check — it buys the confidence without doing the work.**
  **Landmines:** don't slow the suite meaningfully (node startup per file is fine; a node process per *test* is not); don't make `pytest` depend on a global `node` install without a **loud** skip path; don't regress `tests/test_feed_js.py`'s existing behaviour, just generalise it; ⛔ don't "fix" the red test by relaxing the resume contract — a Feed that resumes from `last_seq` loses events **invisibly**, which is the whole failure mode the event log's `gap`/`next_seq` design exists to prevent; keep `uv run ruff check chela tests` green. **Reference:** `tests/views.test.mjs:115` (**the failing assertion — read it first**), `tests/test_feed_js.py` (the wiring that works, for `feed.test.mjs` only), `tests/test_keys.py` (the pattern both follow), `chela/event_log.py` (`read()` — the docstring that states the `next_seq` contract *and why*), `chela/dashboard/static/js/feed.js` (the catch-up loop CMX-60 added — the code the test is complaining about). **Verify:** a deliberately-broken `.mjs` test makes **`uv run pytest -q` fail** (demonstrate it, then revert); every `tests/*.test.mjs` is executed by a bare pytest run; the `next_seq` contract is asserted **behaviourally** and passes; a missing `node` skips **loudly**. **LIVE:** `uv run pytest -q` and `node --test tests/*.test.mjs` agree — both green, and the first would have caught the second.

- [x] **👻 THE FEED IS MOSTLY CORPSES — collapse GONE lanes by default.** Liav, 2026-07-14: *"i think we should filter out gone panes from the feed by default."* Measured on the live dashboard the moment he said it: **8 lanes, 2 live, 5 GONE** (`@3 @72 @74 @76 @82`) — and that is after **one day**. Every dispatched agent that finishes leaves its lane behind forever, so the three lanes you actually care about are buried under a graveyard that grows monotonically. ⚠️ **This is NOT a bug in the lane model — do not "fix" it there.** CMX-60 builds the lane list from **the log, not the live tmux table**, deliberately: key a lane on the live fleet and a dead agent's entire history evaporates the instant its window closes, which is the opposite mistake and a worse one. **The data is right; the DEFAULT VIEW is wrong.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **A gone agent's lane is COLLAPSED by default, not deleted.** It stays in the model, stays reachable, and its events remain in the flat/chronological view. Default = live lanes (needs-you → busy → idle) + the `chela itself` lane. **The gone lanes collapse into ONE row that says exactly what it is hiding** — e.g. `▸ 5 finished agents · 47 events` — with a click to expand. ⛔ **Never hide silently.** That rule is already load-bearing in this Feed (the tool-call firehose row states its own count, CMX-60) and it is the whole difference between a filter and a lie.
  2. **"Gone" is a property of the WINDOW, not of the run.** A lane is gone when its `wid` is not in the live tmux table (`agent_manager` — the **one** authority; ⛔ do not add a second). ⚠️ Do **not** infer it from the run status: a run can be `awaiting_review` (very much wanting your attention) while its window is **already reaped** — that is exactly CMX-62, and treating "window gone" as "finished, ignore me" would bury the `run_review` we just spent a PR making visible. **A gone lane holding an unanswered `run_review` must NOT be collapsed away** — decide how to surface that (hoist it? badge the collapsed row?) and say why.
  3. **A tmux `wid` is RECYCLED.** `@72` today is a different agent from `@72` next week. The collapsed group is derived per-render from the live table, so this mostly takes care of itself — but **do not persist a "gone" flag anywhere**, and do not let a recycled id resurrect a dead agent's events into a live lane. If you cache liveness, key it on something that cannot be reused.
  **Landmines:** don't regress CMX-60's lane model (lanes come from the **log**; the live fleet is *unioned in* so an agent with nothing to say still has a lane) — this task touches **presentation only**; don't regress the **flat/chronological** toggle (it must still show every event, gone or not — it is the escape hatch); the collapsed row's count must be **honest** (it says what it hides, like the firehose row); ⚠️ **Liav is red-weak (colour-blind)** — the collapsed/gone state carries a **glyph + a word**, never hue alone (Okabe-Ito, CMX-60); the lane sort is **needs-you → busy → idle → gone → chela** — keep it, this just collapses the `gone` bucket; the model is pure and node-tested (`feedmodel.js`) — keep it DOM-free and test it there. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dashboard/static/js/feedmodel.js` (the lane model — **pure, node-tested; the grouping and the sort live here**), `feed.js` (the render + the firehose row — **copy its "says what it hides" pattern exactly**), `chela/agent_manager.py` (`status_by_wid` / the live window table — the ONE liveness authority), `tests/feed.test.mjs` (the lane-model tests to extend). **Verify:** unit-test that a lane whose `wid` is absent from the live table is **collapsed, not dropped**; that the collapsed row **states the agent count and the event count**; that expanding it restores the lanes; that the flat view is **unaffected**; that a gone lane holding an **unanswered `run_review`** is still surfaced (not buried); that a recycled `wid` does not merge a dead agent's events into a live lane. **LIVE:** open the Feed with today's real log — the default view shows the **live** agents plus one collapsed row for the 5 gone ones; expand it and the graveyard is all there.

- [x] **🧠 ROOM RECAP ON `SessionStart` — a restarted agent silently forgets everything its room told it.** Stolen from Mosaic (researched 2026-07-14): on `SessionStart` their hook injects a room recap via **`additionalContext`** — *"Shared agent context recap (recent activity from linked agents)"* (`CLI/mosaic.swift:23586-23608`) — **because a restarted Claude lost whatever was injected into its predecessor.** chela now has rooms (CMX-61: a post is an event in the log, and a targeted `handoff`/`question`/`blocker` is *typed into* the peer's terminal), so we have exactly that hole: **everything a room ever told an agent lives only in the dead session's context.** Restart it — and agents restart constantly (a dispatched agent is a fresh session every run) — and the shared context is gone, silently. ⚠️ **This is the third face of a rule that has now bitten us all day: HOOKS ARE READ AT AGENT STARTUP, and an agent's context does not survive its process.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **On `SessionStart`, inject a digest of the agent's rooms** — `rooms.rooms_for(wid)` → `rooms.digest(room)` (both exist, CMX-61; the ledger is a **filter over the event log**, so there is nothing new to store). Bounded: the last N posts, newest-first, each one line (`kind`, `from`, `seq`, the summary). ⛔ **The digest must be SHORT** — it is prepended to a fresh agent's context on every single start; a recap that grows without bound is a context-window tax on every run in the fleet.
  2. **⚠️ `SessionStart` OVER `http` NEVER FIRES — this is measured, do not re-derive it** (CMX-41: *"`SessionStart` never fires over `http`; it fires as a `command` hook"*). And a `SessionStart` **command**-hook's **stdout is injected into the agent's context** — which CMX-41 treated as a hazard and deliberately declined to work around. **Here it is the FEATURE**: that is precisely how the recap gets in. So this task **requires a `command` hook** in the plugin, and its stdout **is** the payload. ⛔ It must therefore be **fast, silent on failure, and never print anything else** — a chatty or slow recap hook lands in every agent's context and stalls every startup. Decide whether it shells `chela` (a process spawn per session start — measure it) or something cheaper, and say why. Check whether `additionalContext` in a JSON response is honoured for `SessionStart` and prefer it if so; **verify, do not assume.**
  3. **An agent with no rooms gets NOTHING.** Not an empty header, not "no shared context" — **nothing at all.** Most agents are in no room; a recap hook that prints boilerplate into every session in the fleet is a tax on all of them for the benefit of none.
  **Landmines:** ⛔ **a hook runs SYNCHRONOUSLY inside the agent's process** — a slow or crashing `SessionStart` hook **delays or breaks every agent launch on this machine** (fail open, exit 0, never stray stdout beyond the recap itself); the plugin is shipped as a **PLUGIN** and its manifest is **COPIED into `~/.claude/plugins/cache/…` at install** — a new hook means the installed copy is stale until reinstalled, and **`chela doctor` now catches exactly that drift (CMX-56) — run it**; ⚠️ **bump `plugin.json`'s version** when the hook set changes, or Claude Code may not re-copy (the version is the cache key — CMX-56's "version trapdoor"); the recap is **untrusted text going into a context window** — it is other agents' words, so sanitise it the way CMX-61 sanitises a room body (`rooms.sanitize`); don't regress the observe-only contract of the *other* hooks, or the gate-answer path (`PermissionRequest`, 120s — **do not touch its timeout**); an agent's `wid` at `SessionStart` must be resolved the CMX-48 way (the **origin dir** from `transcript_path`, never `cwd`) — and if it cannot be resolved, print **nothing** rather than guess. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/rooms.py` (`rooms_for`, `digest`, `sanitize`, `RELAY_HEADER`), `chela/hooks.py` + `plugin/hooks/hooks.json` (the manifest — and CMX-41's measured note that `SessionStart` only fires as a `command` hook), `chela/doctor.py` (the installed-vs-rendered check — **use it to prove the new hook actually reached the agents**), `docs/HOOKS.md`. Mosaic: `CLI/mosaic.swift:23586-23608` (the recap injection) at `/tmp/claude-1000/-home-liavedunix/16fe5ba4-7d59-4ad6-b356-6edfaba8c4fe/scratchpad/mosaic`. **Verify:** unit-test that an agent in a room gets a bounded digest; that an agent in **no** room gets **empty output**; that the digest is sanitised; that a failure exits 0 and prints nothing. **LIVE (mandatory — hooks are read at STARTUP, so this needs a FRESH agent):** put two agents in a room, post a `question`, **restart the recipient**, and show that the restarted agent's context contains the recap — then confirm a room-less agent's startup is byte-for-byte unchanged, and that `chela doctor` is green on the installed manifest.

- [x] **🔴 THE TEST SUITE WRITES INTO THE LIVE EVENT LOG — `~/.chela/events.jsonl` is polluted with synthetic events RIGHT NOW.** Found 2026-07-14 while building the Feed's layout mock from real data. **43 of the log's 93 `hook.permission_request` events are FAKE** — every one carries `session_id="s1"` and a `transcript_path` slug of `-repo` (fixture values), and they arrive in **duplicate pairs** (e.g. seq **2174** `wid=@3` and seq **2175** `wid=None` — the *same* request, twice). They have no `tool_use_id`. They are test data, in production. ⚠️ **`tests/conftest.py` isolates `CHELA_ENV_FILE` and `CLAUDE_CONFIG_DIR` — and its docstring explains exactly why that matters — but it never isolates `CHELA_DIR` itself.** So `event_log.log_path()` resolves to `$CHELA_DIR/events.jsonl` = **the developer's real log**, and any test that appends an event writes to it. ⚠️ **This is the SEVENTH instance of one bug class today** — the suite reaching live state: CMX-33 (tests read the live runs DB — green only on an idle machine), CMX-46, CMX-56 (a doctor test passed *only* because the real plugin cache happened to agree with what the test rendered). conftest's own docstring says it: *"a suite that reads live state is how that keeps happening."* **It is now also a suite that WRITES live state — which is strictly worse: a test can corrupt production, not merely misread it.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **Isolate `CHELA_DIR` for the whole suite, at conftest import** — the same place and for the same reason `CHELA_ENV_FILE` already is (the load happens at **import** time; no fixture runs early enough). Every test gets its own scratch dir. A test that wants a state file (`events.jsonl`, `inbox.json`, `dispatch-hold.json`, `daemon.json`, `dashboard.port`, `gates/`) **puts one there itself**. ⛔ A bare `uv run pytest -q` on a machine **actively running chela** must not touch a single byte under `~/.chela` — prove it.
  2. **Sweep for every other live-state escape.** Grep the suite for the live entry points: `config.CHELA_DIR`, `event_log.log_path()`, `gateanswer.gates_dir()`, `hold`, `inbox`, `dispatcher.list_runs`, `agent_manager` (tmux + `claude agents --json`), `~/.claude/`. Each test either **isolates** it or is **handed** it. Add the reason as a comment where you isolate — conftest's existing docstring is the model, and it exists precisely because a future refactor would otherwise "simplify" the isolation away and restore the bug.
  3. **Make the escape impossible to reintroduce, not merely fixed.** A guard that **fails the run** if a test touches the real `CHELA_DIR` (e.g. point it at a path that must not exist, or assert-on-teardown that the real log's mtime/size is unchanged). A comment is a wish; a failing test is a fence. **Seven instances in one day is a mechanism problem, not seven mistakes.**
  **Landmines:** `chela/config.py` reads `CHELA_DIR` **at import** — a `monkeypatch.setenv` inside a fixture is **too late** if the module is already imported (this is exactly why `CHELA_ENV_FILE` is set at conftest *import*, not in a fixture); modules cache derived paths at import (`event_log`, `gateanswer`, `hold`) — check whether they read `CHELA_DIR` lazily or latch it, and if they latch it, that is part of the bug; don't break the **production** default (a bare `chela` with nothing exported must still resolve `~/.chela`); don't weaken the real `tick()`/`list_runs()` fallbacks — CMX-33's lesson was *the fixture was wrong, not the code*; the live log will be **wiped** by the Feed's part-1 task, so do **not** try to retro-clean it — just stop the bleeding. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `tests/conftest.py` (the isolation that exists and the one that does not — **read its docstring first, it argues this task's case**), `chela/config.py` (`CHELA_DIR` — read at import), `chela/event_log.py` (`log_path()`, `append()` — the writer that escaped), `chela/gateanswer.py` (`gates_dir()`), `chela/hold.py`, `chela/inbox.py`, `~/.chela/events.jsonl` (**seq 2174/2175 — the evidence; look before you fix**). **Verify:** unit-test that `CHELA_DIR` resolves to a per-test scratch dir; that appending an event in a test does **not** touch the real log (assert on the real path's size/mtime); that the guard from item 3 **fails** when a test is made to write the real dir. **LIVE (the whole point):** note the real `~/.chela/events.jsonl` size, run the **full suite** on this machine with chela running, and show the file is **byte-for-byte unchanged**. That is the check that would have caught this.

- [x] **🧱 THE FEED, PART 1 — the enabling refactor. A VIEW REGISTRY, the `Work` consolidation, `/api/log`, and a clean log. (NO Feed UI in this slice — that is part 2, and its layout is being decided separately.)** The dashboard has **seven** views and no way to remove one. **That is not an accident — it is the cause.** Adding or removing a view today touches **FOUR** places: the `.side-item` markup (`chela/dashboard/templates/index.html:55-78`), the per-view timer `if/else` chain in `selectView` (`static/js/nav.js:43-66`), a **second** `if/else` in `main.js:24-28`, and a **third** hardcoded list in the command palette (`nav.js:902`). A view is cheap to add and expensive to delete, so nothing was ever deleted. **Fix the mechanism before adding an eighth view**, or the Feed just deepens the problem it was meant to solve. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  1. **A VIEW REGISTRY — one declaration per view, and everything derives from it.** One place naming a view's id, label, icon, panel id, its refresh/timer hooks and its palette entry; `selectView`, `main.js`'s refresh dispatch and the palette all **read from it** instead of carrying their own hardcoded copy. **Deleting a view must become a one-line deletion.** Keep the `panel-<view>` id convention (it is the only real contract today). ⛔ Do not build a framework — one registry, done well, and prove it by using it to *remove* views in item 2.
  2. **CONSOLIDATE to FIVE views: `Feed` · `Agents` · `Wall` · `Work` · `Knowledge`** (Liav, 2026-07-14 — this is decided, do not re-litigate). **`Work` = Dispatch + Kanban + Schedules merged.** They are **not** three views over three datasets: `dispatcher.js:257` and `kanban.js:585` poll **the same `/api/dispatcher` endpoint** and merely shape it differently (`_renderWorkflowCard` vs `_kanbanFlatten`'s 7 buckets), and `nav.js:304 updateWorkBadges` is a **third** independent poller of it — so on the Kanban view that endpoint is fetched by two timers at once. One view, one poll, a board/list toggle; keep both renderings, kill the duplicate fetches (`_runDisplayId`/`_runPrCell` are already shared out of `dispatcher.js`, and the delete action is duplicated in both — dedupe it). **`Feed` is registered as a view in this slice but may render a placeholder** — its layout is part 2. `agent-detail` stays a virtual drill-in (no nav item).
  3. **`/api/log` — the read endpoint. ⛔ `/api/events` IS ALREADY TAKEN and is NOT the event log** (`chela/dashboard/app.py:2877`): it is the dashboard's SSE **delta-notification** stream, consumed by `static/js/sse.js` — a *notify-then-refetch* pipe that carries **no data**, it invalidates a cache and re-runs a render. So: (a) serve the log at a **different path** (`/api/log`), a thin wrapper over `event_log.read(after_seq=…, after_boot=…, types=…, wid=…, limit=…)` — *"the ONE authority the UI and the inbox both read"*; **pass `gap` and `next_seq` through to the client** (resume from `next_seq`, never `last_seq`) and do **not** invent a second event source; (b) **ride the EXISTING EventSource** — push a `log` delta carrying the new `seq`, and let the client fetch `/api/log?after_seq=N`. ⛔ **Do NOT open a second EventSource.** `app.py` currently imports neither `event_log` nor `inbox` — this is the first wiring.
  4. **WIPE `~/.chela/events.jsonl` to a `.bak` and start at a fresh `boot_id`** (Liav, 2026-07-14). It is ~2.2k rows of today's own debugging traffic with no historical value, and **everything before PR #61 carries the WRONG `wid`** (CMX-48: the correlation key was `cwd`, which moves the moment an agent `cd`s). A UI must never render known-wrong rows. Do it as a documented **operator step** (a `chela events --rotate`-style command, or clear instructions) — **not** a silent unlink at boot.
  **Landmines:** ⛔ **the Feed's LAYOUT is out of scope** — do not design the timeline here (part 2 is deciding it from real data with Liav); a placeholder panel is the correct deliverable; **`nav.js` is 48 KB and is not a nav** (it also renders the sidebar agent list, filter chips and work badges) — the registry governs *views*, don't get dragged into rewriting the sidebar's agent list; don't regress the SSE reconnect/poll-timer safety net (`sse.js`: *"if the stream never connects or drops, the browser auto-reconnects and the timers cover the gap"*) — the log must degrade the same way; the terminals view (`terminals.js`, **100K**) holds **live iframes** — a registry refactor that reloads them is a regression (CMX: the wall reconciles by stable `wid` precisely so a rename is a no-op); the log is **append-only with a rotation** (8 MB × 3) — a client asking for a `seq` below `first_seq` must get the **`gap`** signal, not a plausible-looking wrong continuation; keep `chela events` (the CLI) working — it and the UI must not drift into two readers. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dashboard/static/js/nav.js` (`selectView` 43-66, `updateWorkBadges` 304, the palette 902), `templates/index.html` (55-78 the nav items; the seven `.panel` divs 116-260), `static/js/main.js` (24-28 — the second dispatch chain), `static/js/sse.js` (the pipe to ride), `static/js/{dispatcher,kanban}.js` (the consolidation), `chela/dashboard/app.py` (`/api/events` **taken** 2877; `/api/dispatcher` 2004), `chela/event_log.py` (`read`/`follow`/`ring`), `chela/main.py` (`chela events` — the CLI that already proves the JSON shape a Feed wants). **Verify:** unit-test that a view can be added AND **removed** by editing the registry alone (no other file); that `Work` fetches `/api/dispatcher` **once** per refresh (not twice); that `/api/log` honours a cursor and surfaces `gap`; that the SSE `log` delta triggers a fetch. **LIVE:** open the dashboard — five nav items, `Work` shows runs *and* the board, the terminals wall still shows **live** iframes (not reloaded), and a `chela events emit` shows up in the browser without a manual refresh.

- [x] **📡 THE FEED, PART 2 — AGENT LANES. (Depends on part 1: the view registry + `/api/log` + the wiped log.)** Liav picked this from three layouts rendered against the real log (2026-07-14): *"i think #2 agents lanes is the most intuitive design."* **The fleet is the spine; the log is what each agent is saying.** Events group under the agent that produced them; **an agent that needs you sorts to the top** and its lane wears a `◆ NEEDS YOU` badge, so "who wants me" is answerable **without reading a single row**. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  **⚠️ THE ONE THING THAT DECIDES WHETHER THIS LAYOUT WORKS — attribution.** A lane view is only as good as the `wid` on a row. Measured on the real log: **`run_review` events carry `wid: null`** — but their payload already holds **`window_name: "cmx-57"` and `branch_name`** (`inbox.py::_window_payload`), so they are **attributable and simply were never attributed**. **Resolve the `wid` at write time** (the window is live when the event is queued) and those 17 events land in their agent's lane where they belong. Only genuinely ownerless events (`daemon_start`, and little else) fall to a **`chela itself`** system lane — a legitimate lane, not a dumping ground. ⛔ **Do NOT resolve a `wid` by guessing** (CMX-48: **a wrong `wid` is worse than no `wid`**) — if the payload does not name a window, the event belongs to chela. (ℹ️ The other 43 unattributed gates were **test pollution**, fixed by the sibling task — do not design around them.)
  1. **Lanes sorted by ATTENTION, not by time.** Order: agents **needing you** (a held gate / `waiting`) → **busy** → **idle** → **finished/gone** → the `chela itself` lane. Within a lane, chronological. The lane header carries the `wid`, the agent's name/project, its live status and the `◆ NEEDS YOU` badge. ⚠️ **Liav is red-weak (colour-blind):** every state MUST carry a **glyph + word**, never hue alone — `◆ gate` / `■ lifecycle` / `● run` / `✕ denied` / `▸ prompt`. Use the **Okabe-Ito** palette. Not optional.
  2. **Default filter = gates awaiting me + agent lifecycle** (Liav, decided). Prompts/answers and the **tool-call firehose behind a toggle** — measured: `pre/post_tool_use` is **86%** of the log (1,907 of 2,219). A flat render is unreadable within an hour; the collapsed row must **say how many** it is hiding, never hide silently.
  3. **A flat/chronological toggle** — the same rows, ungrouped, newest-first (that was layout "A", and it is nearly free once the rows exist). Lanes cost you global chronology; this gives it back, and it is the escape hatch when grouping is not what you want.
  4. **Live via the EXISTING SSE pipe** (part 1's `log` delta → fetch `/api/log?after_seq=N`). A new event must appear **without a manual refresh** and must not reflow the lane you are reading. ⛔ **Do NOT open a second EventSource.**
  **Landmines:** ⛔ the log **rotates** (8 MB × 3) — a cursor below `first_seq` must surface the **`gap`** signal, not a plausible-looking wrong continuation (`event_log.read` returns it — pass it through and *show* it); a **subagent's** events carry the **parent's** `session_id`, hence the parent's window — do not invent a subagent lane from them; a **dead** agent's lane must still render (74 of today's events belong to windows that no longer exist) — **the lane list comes from the LOG, not from the live tmux table**, or your history vanishes the moment a window closes; an agent's **name changes** (tmux rename is the source of truth) — key a lane on the **`wid`**, display the name; don't regress the Wall's live iframes or part 1's registry; the Feed is **READ-ONLY** in this slice (the act path already exists on the phone — CMX-50/54; do **not** build a second answer path here). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** the layout mock Liav chose (option **B · Agent lanes**): https://claude.ai/code/artifact/27ec3571-2aa1-47e3-9282-05f37ecd17af · `chela/event_log.py` (`read` — cursor + `gap` + `next_seq`), `chela/inbox.py` (`_window_payload` — **the `window_name` that is never resolved to a `wid`**; `run_events`), `chela/dashboard/static/js/` (part 1's registry; `agents.js` for the agent-row idiom; `sse.js` for the pipe), `chela/agent_manager.py` (`status_by_wid` — busy/idle/waiting, **the ONE authority**; ⛔ do not add a second). **Verify:** unit-test that a `run_review` event resolves to its agent's `wid`; that an event with no attributable window lands in the `chela itself` lane and is **not guessed**; that lanes sort needs-you → busy → idle → gone; that a **dead** agent's lane still renders from the log; that the firehose toggle states how many rows it hides; that a `gap` is surfaced, not swallowed. **LIVE:** open the dashboard with a real agent mid-task, drive it to a gate, and confirm its lane jumps to the top wearing `◆ NEEDS YOU` **with no refresh** — then answer from the phone and watch the lane settle.

- [x] **🔴 `run_review` IS STILL OWNERLESS — the window is REAPED before the event is written, so the CMX-60 attribution fix never fires for the events it was written for.** Measured live 2026-07-14, immediately after CMX-60 merged: **`seq 3070`, `type=run_review`, `window_name=cmx-61`, `wid=None`** — and `cmx-61`'s tmux window was already gone. CMX-60 resolves the id with `wid_for_window_name()`, a lookup against the **live** tmux table, and its own docstring concedes *"a run whose window is already gone stays `wid=None`"*. **That is not the edge case — it is the ONLY case:** a dispatched agent calls `chela task-finished`, which **kills its own window on purpose** (CMX-35), and only *then* does the run reconcile to `awaiting_review` and the event get queued. The daemon's log shows the order plainly — `inbox: resolved completed_gone (@43)` immediately **before** `inbox: queued run_review`. So **every `run_review` lands in the `chela itself` lane instead of its agent's** — and "your agent finished, go review the PR" is arguably the single most important row in the whole Feed. The lane layout's central premise is broken for exactly the event that matters most. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **RECORD THE `wid` AT SPAWN — in the run row.** The dispatcher **creates** the window; at that instant the id is known, unambiguous and free. The run row already carries `window_name`, `worktree_path`, `branch_name`, `task_id`, `attempt` — it must also carry **`window_id`**. Then `inbox.run_events` reads the id **from the run row**, not from a live lookup, and attribution **survives the window's death**: no race, no name collision, no guess. ⛔ This is the *only* place the id can be captured losslessly — after the reap, nothing can recover it (CMX-60's docstring says so itself, and it is right; it simply captured it in the wrong place).
  2. **Keep `wid_for_window_name()` as the FALLBACK, not the primary** — it is still correct for a run that predates this change (no `window_id` in its row) and for a window that is somehow still alive. Its refusal to guess on an ambiguous name is **load-bearing and must not be relaxed** (CMX-48: a wrong `wid` is worse than no `wid` — an unattributed event is *visibly* ownerless; a misattributed one is *invisibly false*). An old run with no recorded id stays ownerless. That is honest.
  3. **Backfill is NOT required and must not be faked.** The log was rotated (CMX-58), so there is almost nothing to backfill, and a historical `wid` that cannot be recovered **must stay `None`**. ⛔ Do not infer one from a branch name, a worktree path, or a window that has since been reused — a reused `wid` would file a dead agent's work under a *live* one, which is the worst possible version of this bug.
  **Landmines:** the run row is persisted (the dispatcher's runs DB) — an **existing** row has no `window_id` column/key, so a migration or a tolerant read is required; **do not crash the daemon on an old row** (it runs unattended under PM2 and writes `dev`); the dispatcher **retries** a failed run (`attempt`), and a retry gets a **new window** — the recorded id must be updated on respawn, not left pointing at the corpse of attempt 1; ⛔ don't regress the dispatcher's fail-closed git guards (ff-only, never force, rolls back) or the tracker strike (CMX-36) while touching the run row; don't regress CMX-60's lane model (a genuinely ownerless event must still render in the `chela itself` lane — that lane is correct and stays); `agent_manager.status_by_wid` remains **the one** status authority — do not add a second. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (`_spawn` — **where the window is created and the id is known**; the run row shape; `list_runs`), `chela/inbox.py` (`run_events` ~517 and `wid_for_window_name` ~379 — **the live lookup that fires too late**; read its docstring, it diagnoses this bug without realising it), `chela/dashboard/static/js/feedmodel.js` (the lane model that consumes the `wid`), `~/.chela/events.jsonl` (**seq 3070 — the evidence**). **Verify:** unit-test that a run row records its `window_id` at spawn; that `run_events` attributes from the row **even when the window is gone**; that a **retry** re-records the new window's id; that a run row with **no** recorded id falls back to the name lookup and, failing that, stays `None` (never guessed); that an ambiguous name still resolves to `None`. **LIVE (this is the whole point — it is the check that caught it):** dispatch a real task, let it finish and its window be reaped, and confirm the resulting `run_review` event lands with **`wid = @N`** — then confirm it renders **in that agent's lane** in the Feed, not in `chela itself`.

- [x] **🔌 AGENT ROOMS + ACTIVE DISPATCH — give `chela msg` SEMANTICS (steal the protocol, not the wire).** Researched from Mosaic (github.com/emergent-inc/mosaic, GPL-3.0) 2026-07-14 after Liav asked about their *"cross a line between two agent panes to link them"* gesture. **The gesture is REAL** — an agent-room **wire**: drag from a port on one pane's header to another, a live bezier follows the cursor at 60fps, linked panes get a shared accent border, and every pane sprouts a temporary drop socket while a drag is in flight (that last detail is what makes it discoverable). **But the wire is only a UI for a RELATIONSHIP — and chela has no such relationship to draw.** `chela msg` fires a string into a pane and vanishes: **no UI *and* no semantics.** A line between two agents today would be a picture of nothing. ⛔ **Build the relationship FIRST. The wire is a later task and is worthless without this one.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  1. **A ROOM is a shared, persisted ledger — and the substrate already exists.** Mosaic's room is an append-only store with **typed** kinds (`summary, task, decision, finding, fileChanged, testResult, blocker, question, handoff, reviewFinding, status, message`). **chela has `chela/event_log.py`** (append-only, `seq`+`boot_id`, cursor-readable), so a room is **membership + a filter over the log**, *not a second store*. ⛔ **Do NOT add a second event source** — the log's own docstring: *"the ONE authority the UI and the inbox both read."* Membership needs a home (`$CHELA_DIR/rooms.json`, or a room id on the event) — decide, and say why.
  2. **ACTIVE DISPATCH is the whole point.** A *targeted* `handoff` / `question` / `blocker` is **delivered into the peer's terminal**, waking an idle agent **with no human in the loop** — and the injected prompt carries a **reply instruction** so the answer routes back to the asker, attributed. Two agents complete a question→answer round trip with nobody in the middle. **chela already has the wake mechanism**: `inbox.deliver()` pastes into the orchestrator's tmux window, gated on **idle**. Generalise it — an inbound `question` to *any* agent is the same wake. ⛔ **Only `handoff|question|blocker` may interrupt.** `status`/`finding`/`summary` are **recorded, never injected**, or a busy fleet becomes an interrupt storm.
  3. **THE LOOP GUARD IS NOT OPTIONAL.** A messages B; B's reply is injected into A; A's hook sees it and relays it back… Mosaic guards with a fixed **non-localized prefix list** the publish hook skips (`"Linked context message|handoff|question|blocker"`). **Design ours explicitly and test it ADVERSARIALLY** — two agents wired to each other, one message, prove it terminates. **An echo loop between two live agents burns real money and a real machine.**
  4. **Respect the inbox's existing safety rails.** ⛔ **Never paste into a `waiting` agent** — it would answer a permission prompt with the message text. That rule already exists in `inbox.py` and is load-bearing; **reuse it, don't reinvent it.** A dead/unbound recipient must fail **loudly** (CMX-47: `chela msg` once reported a *busy* agent as "offline" and silently dropped a real message — do not regress that).
  **Landmines:** ⛔ **do not build the wire/UI here** — it is a follow-up, and it needs the Wall, where the tiles are **`ttyd` iframes that EAT pointer events**, so a drag layer must sit *above* them; a room's ledger must survive an agent **restart** (Mosaic re-seeds a restarted agent on `SessionStart` via `additionalContext` — worth doing, but as its own task); don't regress `chela msg`'s **self-notify refusal** (an orchestrator messaging its own window is a loop); the recipient is resolved against the **live window table** (`messenger.resolve_window`, id-or-name — CMX-47), never guessed; ⚠️ **message text is UNTRUSTED INPUT being typed into a terminal** — decide what a `handoff` body containing control characters, an `Escape`, or a leading `/` slash-command does, and **neutralise it** (we already inject keystrokes; this widens who can). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** Mosaic clone at `/tmp/claude-1000/-home-liavedunix/16fe5ba4-7d59-4ad6-b356-6edfaba8c4fe/scratchpad/mosaic` — `Packages/Shared/MosaicCollaboration/…/ClaudeRoomStore.swift`, `ClaudeRoomEventKind.swift`, `ClaudeRoomDeliveryPolicy.swift`, **`AgentRoomActiveDispatchPromptBuilder.swift`** (the prompt protocol + `followUpInstruction` + `relayPromptHeaderPrefixes` — **read this one first**), `CLI/mosaic.swift:3668-3856` (`agent-room status|create|connect|post|digest`). **In chela:** `chela/messenger.py` (`send_tmux`, `resolve_window`), `chela/main.py` (`cmd_msg`/`cmd_broadcast`), `chela/inbox.py` (`deliver()` — **the idle-gated wake we already have**), `chela/event_log.py`. **Verify:** unit-test that a `question` to an idle agent is injected and a `status` is **not**; that a reply routes back to the asker, attributed; that an echo loop **terminates** (two agents, adversarial); that a `waiting` agent is never pasted into; that an unknown recipient fails loudly with **exit 1**; that a hostile body (control chars / a leading `/`) cannot drive the recipient's TUI. **LIVE:** wire two real agents, have one ask the other a question, and show the answer arriving back **with no human touching either terminal**.

## Done — interactive UI (phone approvals)

- [x] **📱 ONE MESSAGE PER GATE — the mirror is the only thing posted; the cards become a `📖` expansion.** Liav, from his phone 2026-07-14, after driving the CMX-54 mirror live (cursor moved, previews followed it, multiSelect answerable): *"i think i prefer the bottom variant with this emoji 🎛️ … it's also the single view where i can do multi select"* and — on the mirror showing only the focused option's preview — *"because it's like in claude terminal - it only shows the preview if the current ❯ is on the option"*. **That is the mirror doing its job, not a defect.** But today a 2-question gate posts **THREE** messages to the topic — `❓ Question 1/2`, `❓ Question 2/2`, and `🎛️` the mirror — so the surface he actually uses is buried under two he does not, and he has to scroll past them to reach it. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  **What the cards uniquely carry — the ONLY reason not to just delete them:** the mirror renders the pane, and the TUI draws **one preview at a time**, for the option under the `❯`. The cards render **every** option's `description` and `preview` **together**, so you can *compare* them. That is what previews are FOR — the sidebar question that started this whole thread put three before/after trees side by side precisely so they could be read against each other. Comparing by arrowing back and forth and holding the other option in your head is not comparing. ⛔ So do **not** simply delete the card content: **relocate** it.
  1. **The mirror is the ONLY message posted for a gate.** No `❓` cards alongside it. One tracked message per window per gate, edited in place, poofed on resolve — the CMX-52/54 lifecycle, unchanged.
  2. **Add a `📖` button to the mirror that expands the full option list** — every option's `label` + `description` + `preview`, all questions, from the **hook payload** (CMX-49's `hookgate` content authority — full fidelity, never the truncated pane; note the TUI *also* clips a long preview with `✂ N lines hidden`, so the payload is strictly better than what the pane can show). Decide the mechanism and say why: an **edit-in-place toggle** on the same message (📖 expand ⇄ 🎛️ collapse — keeps ONE message, which is the whole point of this task) is the strongest candidate; a separate posted message re-creates the clutter being removed. **The expansion must not lose the D-pad or the answer buttons** — a human who expands to compare must be able to answer without collapsing first.
  3. **Nothing about ANSWERING changes.** The `qa:h:`/`qa:hs:` option buttons (zero keystrokes, CMX-50/54), the `focused_question` proof-guard, the D-pad, the bounded wait, the fail-open — all stand exactly as they are. **This task moves CONTENT between surfaces. It does not touch the answer path.** If you are editing `gateanswer.py`, you are out of scope.
  **Landmines:** ⛔ **do not regress CMX-49's content fidelity** — the previews and descriptions Liav verified legible on his phone must still be reachable, just behind the `📖` instead of beside it (a task that ends with the previews *unreachable* has failed); a `multiSelect` question's ticks (`☑`) must stay in sync across collapse/expand — both surfaces already read the same `Drafts.selected`, keep it that way; the expansion can be **long** — respect the >4096 splitter (PR #50, entity-safe) and remember an `editMessageText` has the same cap as a send, so an expansion that cannot fit must degrade **visibly**, never silently truncate; an unchanged render must still cost **zero** API calls (CMX-52's de-dup) and a **tap must still bypass the throttle** (a tap that doesn't visibly move the cursor is the bug CMX-52 fixed); the **pre-plugin fleet** has no hook payload at all → no `📖` to expand (there is nothing to show) — degrade to the mirror alone, and do not crash; don't regress the ephemeral status line (CMX-43) or the permission/plan cards for dialogs that are **not** `AskUserQuestion` (they are a different shape — decide whether they keep their own card and say why). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (`format_hook_askuq_cards` — the cards being relocated; `format_mirror_card`, `mirror_suppressed`, `focused_question`, the `_MIRROR`/`_uq_msg` trackers), `chela/telegram/interactive.py` (`MIRROR_KEYS`, the `qa:h:`/`qa:hs:` buttons — add the `📖` here), `chela/telegram/inbound.py` (`_on_mirror`, `_on_qa` — the tap handlers), `chela/telegram/hookgate.py` (the payload = the content authority), `chela/telegram/relay.py` (`split_message` — the 4096 cap applies to an edit too). **Verify:** unit-test that a gate posts **exactly one** message (not three); that `📖` expands every question's options with descriptions **and** previews from the payload; that the expansion keeps the D-pad *and* the answer buttons; that collapse returns to the pane render; that a `multiSelect` tick survives an expand/collapse round-trip; that a pre-plugin window (no payload) still gets a drivable mirror and no `📖`. **LIVE (mandatory — Liav has overturned this design twice from his phone):** fire the 2-question preview+multiSelect gate at a **fresh** agent (hooks load at STARTUP — and check `chela doctor` first: the installed plugin manifest must be the 120s one, or no gate is ever HELD and no answer buttons appear), and confirm from the phone: **ONE** message; the cursor still moves on every tap; `📖` shows both Apple and Banana previews **together**; a tap still answers with no keystrokes. Screenshot it.

- [x] **🔴 CLAUDE CODE READS A *COPY* OF THE PLUGIN, NOT THE ONE WE RENDER — so the entire zero-keypress answer path was SILENTLY DEAD while every check was green.** Found live 2026-07-14, and it is the reason the mirror's option buttons never appeared on Liav's phone. `chela plugin` renders `~/.chela/plugin/hooks/hooks.json`. **Agents do not read that file.** `/plugin install` **copies** the plugin into a cache, and *that* copy is what every agent loads at startup:
  ```
  ~/.chela/plugin/hooks/hooks.json                         PermissionRequest timeout = 120   <- what we render
  ~/.claude/plugins/cache/chela/chela/0.1.0/hooks/hooks.json   PermissionRequest timeout = 2   <- what AGENTS actually read
  ```
  CMX-50 raised the `PermissionRequest` timeout to 120s so a human has time to tap. The **installed cache still said 2s**, so every hook was killed after two seconds, the daemon's hold was torn down, **no gate file was ever written**, `held` was always `None` — and the mirror (correctly, per CMX-54) refused to draw option buttons it could not back. **The code was right; the manifest the agents actually read was stale.** Nothing anywhere said so: `chela doctor` was **all-green** the whole time, because it validates *the manifest we render* rather than *the manifest that runs*. ⚠️ **This is the THIRD instance of one bug class in one day** — CMX-41 (`chela plugin` rendered port 5001 while the dashboard bound 5005 → every hook POSTed into a closed socket, feature silently dead), CMX-53 (doctor compared the env file to the running env; both agreed, both were wrong), and now this. **chela keeps verifying the config it WRITES instead of the config that RUNS.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **`chela doctor` must read the INSTALLED manifest — the one an agent would actually load — and compare it to what we render.** A drift is an **ERROR**, not a warning: it means the hook feature is dead. Report *which* file it read, both values, and the fix. Find the install location honestly (`~/.claude/plugins/cache/<marketplace>/<plugin>/<version>/…` today — **verify the layout empirically, do not hardcode a guess**, and if it cannot be located, say **that** rather than passing silently). ⛔ **A doctor that cannot find the installed copy must NOT report green.**
  2. **Close the loop so a re-render cannot be forgotten.** `chela plugin` writing a manifest nobody reads is a trap. Either it **refreshes the installed copy** (and says so), or it **tells the operator, loudly and specifically, that a `/plugin` reinstall is required for the change to take effect** — naming the cache path. ⚠️ **The cache is Claude Code's, not ours** — decide carefully whether writing into it is legitimate (a reinstall may overwrite it) or whether the honest move is to *detect and instruct*. Say which, and why. **Also state, in the output, that hooks are read at agent STARTUP** — a running fleet will not pick up a corrected manifest, which bit us three times today.
  3. **The version is a trapdoor.** The cache path contains the plugin **version** (`0.1.0`). Bumping `plugin.json`'s version changes the cache directory, so a doctor that hardcodes a path will silently check a stale one — or nothing. Resolve the installed copy by *discovery*, and prove it survives a version bump.
  **Landmines:** ⛔ **do not write `~/.claude/settings.json`** (the whole reason chela ships a plugin — CMX-41's rule stands); the cache is a Claude Code implementation detail and **may change shape between releases** — the failure mode when it does must be a **loud "I cannot verify this"**, never a silent green (that is the exact bug being fixed, reintroduced one level up); don't slow `chela doctor` down (it is interactive — read a file, don't shell out to `claude`); **the 2s timeout on every OTHER hook is deliberate and load-bearing** (`PreToolUse`/`PostToolUse` are ~78% of the log's volume and must stay fast) — a fix that raises them all is a regression; do not regress the fail-open contract (a dead daemon must never wedge an agent). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/hooks.py` + `chela plugin` (`chela/main.py` — the renderer), `plugin/hooks/hooks.json` (the source of truth we render), `~/.claude/plugins/cache/chela/chela/0.1.0/hooks/hooks.json` (**the copy that actually runs — read it**), `chela/doctor.py` (CMX-53's capability checks — this is the same lens, applied to the plugin), `chela/gateanswer.py` (`gates_dir()` — a gate file that is never written is the symptom), `docs/HOOKS.md`. **Verify:** unit-test that doctor ERRORs when the installed manifest disagrees with the rendered one; that it ERRORs (never passes) when the installed copy cannot be found; that a version bump still resolves the right cache dir. **LIVE (the whole point):** corrupt the installed manifest back to `timeout: 2`, run `chela doctor`, and confirm it **fails loudly** and names the file — the state that hid all day must now be impossible to miss. Then restore it and confirm green.

- [x] **🔴 THE DISPATCHER WINS EVERY RACE FOR THE QUEUE — the orchestrator cannot reprioritise, because a slot is always claimed before the reorder lands.** Hit **twice** on 2026-07-14 (cmx-51 and cmx-53 were both claimed while a higher-priority item was being written), and Liav: *"i think the order matters, and in general i think we should fix this race."* **This is not a narrow timing window — the dispatcher wins essentially always**, and the mechanism is structural: (1) the orchestrator merges a PR; (2) the run reconciles and **frees the slot**; (3) the orchestrator starts *writing* the next task — which takes **minutes**, because it is reviewing what just merged; (4) the dispatcher's tick fires long before that and claims whatever is top of the **old** queue. With `concurrency.max: 1`, a wrong claim does not merely reorder work — it **occupies the only slot for a full agent run (20–40 min)**. That is a priority inversion with a real cost, and it deepens with the queue. ⚠️ **This repo has already met this exact shape once:** `TODO.md` is a **shared mutable resource with two writers**, and CMX-36 fixed the *write* axis by making the dispatcher the sole owner of the strike. This is the **same two-writer problem on the ORDERING axis**, and it is unfixed. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **FETCH-THEN-CLAIM — the tracker must be re-read from `origin/<base_branch>` at the INSTANT of claiming**, not from a parse made earlier in the tick (or an earlier tick). Cheap, strictly-better, and it closes the tail of the window for free. ⛔ But **do not stop here and call the race fixed** — it does not help the case above at all, because the orchestrator's push genuinely lands *after* the slot freed. Fetch-then-claim is necessary and **not sufficient**; say so in the code.
  2. **A QUEUE HOLD — the orchestrator must be able to say "claim nothing, I am rewriting the queue."** This is the actual fix: an explicit, *visible* pause that the orchestrator takes before it edits the tracker and releases after it pushes, so ordering is decided by intent instead of by who is faster. Design it: a CLI (`chela dispatch --pause` / `--resume`, or a hold file under `CHELA_DIR`), **honoured by the running daemon** (which is a *different process* — the CMX-42 trap; a hold that only exists in one process's memory is not a hold), **surfaced** wherever a human can see it (`/api/settings`, `chela doctor`, the startup capability line from CMX-53 — a paused dispatcher is a *disabled subsystem*, and CMX-53's rule now applies: it MUST announce itself, never be silently off). ⛔ **A hold MUST NOT be able to strand the fleet forever** — an orchestrator that crashes mid-rewrite must not leave dispatch paused for eternity: give the hold an **expiry** (and/or a pid-check, like `dashboard.port`/`daemon.json` already do), and make an expired hold loud. Reconciliation must **keep running** while dispatch is held — the CMX-53 lesson is that these two are coupled, and pausing *dispatch* must not silently also stop runs from closing out and freeing their slots.
  3. **DECIDE, EXPLICITLY: can a just-claimed run be PREEMPTED?** If a higher-priority task lands seconds after a claim and the agent has done nothing yet, is it killed and re-dispatched? **My instinct is NO** — killing a live agent is its own hazard class (this repo has killed its own fleet three times, and `tmux -L <socket>` isolation exists precisely because of it) — but the task must not leave this unanswered. **Write the answer down** in a docstring: either preemption is out of scope and the hold is the remedy, or it is in scope and you must define exactly when it is safe (has the agent written anything? opened a PR? touched the worktree?) and how the run is cleanly abandoned. **Do not implement a half-preemption that leaves an orphaned worktree, branch, or tmux window.**
  **Landmines:** the dispatcher now **pushes to `dev` unattended** (the CMX-36 tracker strike) — do not loosen its fail-closed guards (ff-only, never force, rolls back a rejected push) while touching the claim path; don't regress `chela dispatch --dry-run` / `--once`; a hold must not deadlock a **tick already in progress** (take it at a tick boundary, exactly as the CMX-39 hot-reload applies config at a boundary and never mid-`_spawn`); the daemon runs under PM2 **from disk** and loads Python at start — a hold implemented purely in module state dies with a restart, so **persist it** and re-read it (the same reason `daemon.json`/`dashboard.port` exist); ⛔ do NOT "fix" the race by slowing the dispatcher's tick — that hides the inversion behind latency and makes the fleet worse at its job; the task-id hashing is how a task is matched to a run (CMX-36) — a reorder must not change a task's identity, so **verify that moving a line in `TODO.md` does not re-key an in-flight run** (if it does, that is a second bug and it is worse than this one). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (the tick: tracker parse → claim → `_spawn`; `list_runs`; `_strike_merged_tasks` — the unattended `dev` writer; the task-id hashing), `chela/main.py::cmd_run` (the daemon loop + the per-workflow tick cadence; the CMX-53 capability announcement — a hold belongs in it), `chela/capabilities.py` (CMX-53 — "a disabled subsystem announces itself"; a *held* dispatcher is exactly that), `chela/config.py` (`CHELA_DIR`, `DISPATCH_WORKFLOWS`), `chela/doctor.py` (must report a hold — and an **expired** one), `chela/dashboard/app.py` (`/api/settings`), `WORKFLOW.md` (`concurrency.max: 1` — why a wrong claim is so expensive). **Verify:** unit-test that a tracker edit pushed to `origin/dev` **after** the tick began is honoured at claim time (fetch-then-claim); that a hold blocks a claim and **releases** cleanly; that a hold does **not** stop reconciliation; that an **expired** hold resumes dispatch and says so loudly; that a hold survives a daemon restart; that reordering a `TODO.md` line does **not** re-key an in-flight run. **LIVE (this is the whole point — reproduce the actual failure):** with a run in flight, take the hold; merge the PR so the slot frees; confirm the daemon claims **nothing** while held; rewrite the queue putting a NEW item on top; push; release the hold; confirm the daemon claims **the new top item**, not the one that was top before. That sequence is exactly what failed twice today.

- [x] **🔴 THE MIRROR IS NOW THE PRIMARY JOURNEY — but the code is programmed to HIDE it exactly when the answer path works. Invert the composition rule: the D-pad and the one-tap buttons belong on the SAME message.** Liav drove the CMX-52 mirror from his phone on 2026-07-14 — *"the cursor moved, and it was pretty nice, i like this surface more"* — and **answered a `multiSelect` from a phone for the first time** (there was no `Space` key before CMX-52). That decides the open question from CMX-52's brief: **the mirror is the primary surface, not a fallback.** ⚠️ **But he only saw it because his session's hooks were latched at the OLD 2-second manifest, so no hook could HOLD the gate.** `gatewatch.mirror_suppressed` (~704) opens with `if held is not None: return True` — so the instant an agent starts with the new `PermissionRequest timeout: 120` manifest (CMX-50), the hook holds the gate, **the mirror is suppressed, and Liav is handed the card-only surface he just said he likes less.** The better the zero-keypress path works, the more reliably it hides the surface he chose. **This is a live regression on a turning-over fleet, not a hypothetical** — every newly-started agent already has the 120s manifest. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **One message: the mirrored pane as the BODY, the hook's option buttons ABOVE the D-pad.** Stop treating "every option is one tap away" as a reason to *withhold* the pane. The two are complementary and the user has now told us so from the phone: the pane is the only thing that shows him **where he is** (no hook carries a cursor — a cursor is a render, not an input), and the hook buttons are the only thing that answers **with zero keystrokes** (no race, `multiSelect` as a real set, every question). He should get **both**: watch the cursor, *or* tap the answer — his choice, same message. Invert `mirror_suppressed` accordingly. Suppression should become the rare case, and you must **state in a docstring what (if anything) still justifies it** — "the dialog has no pane region to show" is a reason; "the answer is already easy" is not.
  2. **Keep every safety property of CMX-50 exactly as it is.** The bounded wait, the fail-open-never-deny, the `tool_use_id` stale-answer refusal, the `CHELA_GATE_MAX_WAITS` bound, the refusal to submit a **partial** `answers` map (Claude Code accepts one and *silently drops the unanswered question* — measured, CMX-50). **This task changes only what is DRAWN, not how an answer is delivered.** If you find yourself editing `gateanswer.py`'s wait/refusal logic, stop — you are out of scope.
  3. **The two answer paths must not collide.** A held gate can now be answered **two ways at once**: a tap on a semantic option button (→ the hook returns the answer) *or* the D-pad + `⏎` (→ keystrokes into the pane). Both are legitimate and both must stay, but they must not **double-answer** — decide explicitly what happens when a human drives the TUI to a choice *while* the hook is holding, and when the hook answer lands *while* the pane has already been driven. The gate resolving by either route must cleanly release the other (the `post_tool_use` for that `tool_use_id` is the resolution signal — it fires whichever way it was answered). Write the interleaving down; do not leave it to luck.
  **Landmines:** ⛔ **do NOT delete the CMX-49 cards** — they carry the `description`s and `preview`s the pane truncates or mangles, and Liav verified those legible on his phone; the *content* is still the hook's job (a mirror of a side-by-side preview layout is not a substitute for the preview text); a keystroke `⏎` on a **held** gate answers the agent through the TUI while the hook is still blocking — make sure the hook then returns promptly and does not sit out its full 90s budget (a resolved gate must release its wait slot immediately, or `CHELA_GATE_MAX_WAITS` fills with corpses); don't regress CMX-52's de-dup (an unchanged pane must still cost **zero** API calls) or the tap-bypasses-the-throttle rule (a tap that doesn't visibly move the cursor is the bug it fixed); don't regress the `_answerable` proof-guard (CMX-32: a button whose ordinal mapping cannot be proven is worse than no button) — note it applies to *keystroke* buttons only, since a **hook** button needs no ordinal at all; the mirror must keep working for the **pre-plugin fleet** (no hooks at all → no buttons, D-pad only). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (`mirror_suppressed` ~704 — **the whole bug**; `_MIRROR` tracker), `chela/telegram/interactive.py` (`MIRROR_KEYS`/`MIRROR_ACTIONS`, the hook-answer buttons from CMX-50), `chela/telegram/gateanswers.py` + `chela/gateanswer.py` (the answer path — **read, do not modify**), `chela/telegram/inbound.py` (`_on_mirror`, the tap handlers), `chela/telegram/hookgate.py` (the content authority). **Verify:** unit-test that a **held** gate now renders the mirror **and** the option buttons on one message; that tapping an option answers via the hook with **zero** tmux writes (CMX-50's booby-trap fixture already asserts this — keep it); that driving the D-pad to a choice and pressing `⏎` on a held gate resolves it **once**, releases the hook's wait slot promptly, and does not also submit a hook answer; that a pre-plugin window still gets a drivable mirror with no option buttons. **LIVE (mandatory — Liav decides, and the last live check overturned the design):** start a **fresh** agent (so it picks up the 120s manifest — hooks are read at STARTUP), fire a multi-question `AskUserQuestion` with previews, and confirm from the phone that the mirror **is still there**, the cursor still moves, **and** the option buttons answer with no keystrokes. Screenshot it.

- [x] **🔴 A GATE IS RENDERED FROM THE PANE, SO A SHAPE THE SCRAPER CANNOT PARSE ARRIVES ON THE PHONE AS AN UNANSWERABLE STUB — while the hook already holds the full payload.** Hit live 2026-07-14: the orchestrator asked a 3-question `AskUserQuestion` (single-select, each option carrying a `preview`). Telegram showed **the question text and a bare nav row** (`↑ ↓ ↺ ⏎ 🔄`) — **zero options, zero descriptions, zero previews, nothing to tap.** Meanwhile `~/.chela/events.jsonl` **seq 184** (`hook.pre_tool_use`) and **seq 185** (`hook.permission_request`) carried the *complete* `tool_input`: all 3 questions, every option's `label`, `description` **and** `preview`, `header`, `multiSelect`, `permission_mode=auto`. **The data was in the log before the message ever reached the phone. The relay threw it away.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  **Root cause — this is a designed fallback firing, not a crash.** `panescan.AskUQ.multi` is documented as *"True for a multi-tab / multi-select selector **(or an otherwise unparseable one)**: the relay then offers the nav row only"* (`chela/telegram/panescan.py` ~289-300). The option regexes were measured against *"a single-select, single-question selector"* (~249-282): `_RE_OPTION` expects `❯ 1. Label` to **start** a line, and `_RE_MULTI_TAB` (`^\s*←\s+[☐✔☒]`) treats a tab strip as unparseable. A **multi-question** selector draws that tab strip, and a selector whose options carry a **`preview`** switches the TUI to a **side-by-side layout** (option list left, preview box right) — so the option rows no longer start their line. Either alone trips the fallback. **The scraper will keep meeting shapes it was not measured against; the hook will not.** ⛔ Do **not** fix this by extending the regexes to cover previews/tab-strips — that is re-deriving, from a lossy render, data you are **already being handed structurally**. Fix the *source*, not the parser.
  1. **The HOOK payload becomes the CONTENT authority for a gate; the pane stays only for liveness/fallback.** When a gate is detected for a bridged window, look up that window's most recent **unresolved** `hook.pre_tool_use` for an interactive tool (`AskUserQuestion` / `ExitPlanMode`) in the event log and render **from its `tool_input`** — full fidelity, every question, every option's `label` + `description` + `preview`, `header`, `multiSelect`. **Use `PreToolUse`, not `PermissionRequest`, as the content source: `PreToolUse` carries a `tool_use_id` and `PermissionRequest` does NOT** (already recorded in this file, item "EVENT LOG — part B"), so `PreToolUse` is the only one you can pair with its `PostToolUse` to know the gate is **resolved**. Define "pending" as *a `pre_tool_use` for an interactive tool with no `post_tool_use` bearing the same `tool_use_id`* — and say so in a docstring.
  2. **One Telegram message PER QUESTION — not one per gate.** Three questions × three options × a dozen preview lines is a wall of text on a phone, and the TUI itself walks questions **one at a time**. Each message carries that question's options numbered in full, their descriptions, and their previews, with its own selector row. This is also what makes the multi-question case *answerable* at all.
  3. **Previews render, and are NEVER silently dropped.** Put a `preview` in a Telegram `<pre>` block — those **scroll horizontally rather than wrapping** on mobile, so box-drawing ASCII survives (**VERIFY THIS LIVE on a real phone before building on it** — do not take it on faith; this repo's "green tests lied" count is 4-for-4). The >4096 splitter is already entity-safe (PR #50) and will carry them. ⛔ **If a preview exists and the card does not show it, the card MUST say so.** A gate that renders as though it were complete when it is not is the same silent-degrade class as the wrong `wid` (CMX-48) and the silent mis-answer (CMX-32): the human answers with less information than the asker intended, and nothing tells them.
  4. **Be HONEST about what you cannot answer yet.** Answering still goes through keystroke injection in this slice. Where the scraper *can* still locate the cursor (single-question, single-select), keep the existing `1 2 3` selector path — it works. Where it **cannot** (multi-question, `multiSelect`), render the full content, offer the nav row, and **state plainly in the message that this shape must be answered in the terminal for now.** ⛔ Do **not** synthesize a selector whose ordinal mapping you cannot prove — a button that silently picks the wrong option is exactly CMX-32, and it is worse than no button. The zero-keypress answer path is the **next** task; do not build it here.
  **Landmines:** **hooks are read at agent STARTUP** — an agent launched before the plugin was installed emits **no** hook events, so the pane-scraped path **must remain the fallback** and must not regress (do not delete `panescan`/`gatewatch`'s detectors); the log is the content source but the **pane is still what tells you a gate is on screen right now** — a `pre_tool_use` with no `post_tool_use` could also mean the agent died, so **corroborate against the pane before posting** (cf. the false-`DIED` bug, CMX-35); a **subagent's** events carry the **parent's** `session_id` — a parent-window gate and a subagent's tool call must not be confused; events **before PR #61** carry **wrong `wid`s** — do not resolve a gate against them (scope your lookup to the current `boot_id`, or to `seq` above the #61 boundary, and say which); `preview` is **optional** and often absent — an empty preview must render nothing, not an empty `<pre>`; don't regress the tracked-message lifecycle (`_uq_msg` post/edit/poof) or the ephemeral status line (CMX-43), which shares the same tick. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `~/.chela/events.jsonl` **seq 184/185 — the real payload this bug is about; read it first**; `chela/telegram/panescan.py` (`AskUQ` ~285, `_RE_OPTION`/`_RE_MULTI_TAB` ~271-282 — the fallback that fires), `chela/telegram/gatewatch.py` (`format_askuq_message`, `poll` ~354, the post/edit/poof tracker ~393/~436), `chela/telegram/interactive.py` (`scraped_reply_markup`, `NAV_KEYS` ~84, the `qa:*` callback scheme), `chela/event_log.py` (`read(types=…, wid=…)` — "the ONE authority the UI and the inbox both read"), `chela/hooks.py` (the receiver + the CMX-48 origin-dir correlation), `docs/HOOKS.md`. **Verify:** unit-test that a gate whose window has a pending `pre_tool_use` renders **from the payload** (all questions, all options, descriptions, previews) and not from the scrape; that a `post_tool_use` with the same `tool_use_id` marks it resolved; that a window with **no** hook events still falls back to the pane-scraped render (the pre-plugin fleet); that a preview-bearing option whose preview is omitted produces an explicit notice. **LIVE (mandatory — a green suite has lied 4× on this substrate):** from a real agent, fire the **exact shape that broke**: a 3-question `AskUserQuestion` with previews. Every question must arrive as its own message with its options, descriptions and previews **legible on the phone**. Screenshot it.

- [x] **THE RELAY BURNS ITS TELEGRAM RATE LIMIT REWRITING TOPIC NAMES THAT DID NOT CHANGE — and the 429s it earns are now delaying real messages.** Observed live 2026-07-14 in `pm2 logs chela-telegram`: every ~16s, for **every** bound topic, `editForumTopic(976 -> chelamux) failed: Bad Request: TOPIC_NOT_MODIFIED` / `editForumTopic(1001 -> nautilus) failed: …` — i.e. the reconcile loop calls `editForumTopic` with **the name the topic already has**, unconditionally, forever. Telegram answers `TOPIC_NOT_MODIFIED`, and the wasted calls earn genuine **`429 Too Many Requests: retry after 10`** — which then land on the *relay*: `telegram editMessageText failed: Too Many Requests: retry after 3`. So decoration is stealing the budget from **real** traffic. This is a prerequisite, not a nicety: the next task's whole UX is **edit-in-place responsiveness**, and it cannot compete for a rate limit that is being spent on no-op renames. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Only call `editForumTopic` when the name actually CHANGED.** Reconcile already knows the desired name; it must also know the **current** one and compare before writing (cache the last-applied name per topic; a `TOPIC_NOT_MODIFIED` response is itself proof the write was pointless — treat it as a signal to cache, not a warning to log forever). Edge-trigger it, exactly as the run-state toasts and the status line already do (both learned this lesson: `_runStateToasts` is edge-triggered on purpose, and the status line **skips the edit entirely when the text is unchanged**). **The same audit applies to every other unconditional Telegram write in the reconcile loop** — grep for them; a no-op write is a rate-limit leak wherever it lives.
  2. **A `TOPIC_NOT_MODIFIED` must never be logged as a WARNING.** It is not an error and it is not actionable — it is chela telling on itself. Once item 1 lands it should be unreachable; if it is still reachable, it is a *bug signal*, so log it at DEBUG (or count it and surface the count), and keep genuine failures loud. A log that cries wolf every 16s is a log nobody reads — which is exactly how the silently-disabled dispatcher hid for nine hours.
  **Landmines:** don't fix this by simply widening the reconcile interval — that hides the leak instead of closing it (the call is *wrong*, not merely *frequent*); the 429 `retry_after` loop is load-bearing and must stay (a flood-wait is not a rejection — do not "simplify" it away while touching this code); topic renames must still WORK (a genuine rename — an agent's window is renamed — has to propagate; prove it); the status line deliberately opts **out** of the 429 retry loop (CMX-43 — decoration must never stall real messages) — keep that, and consider whether topic reconcile deserves the same treatment; don't regress `chela-telegram`'s binding reconcile (`~/.chela/telegram-bindings.json`). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/reconcile.py` (the `editForumTopic` caller — the whole bug), `chela/telegram/relay.py` (the 429 `retry_after` loop, `edit` ~492), `chela/telegram/gatewatch.py` (the edge-triggered post/edit/poof tracker — the pattern to copy), `pm2 logs chela-telegram` (reproduce it in 30 seconds — it is firing right now). **Verify:** unit-test that an unchanged topic name produces **zero** API calls; that a genuine rename produces exactly one; that a 429 is still retried. **LIVE:** watch `pm2 logs chela-telegram` for 2 minutes and see **no** `TOPIC_NOT_MODIFIED` and **no** 429s at idle — then rename an agent's window and see the topic follow.

- [x] **🎛️ PORT ccbot's PANE-MIRROR DRIVER — one message, edited in place after every key, with a real D-pad. Liav: it may become the PRIMARY journey, not a fallback.** Liav used the new hook-rendered cards (CMX-49) on his phone 2026-07-14 and said: *"i saw all the questions on telegram but the interface is still not so convenient… we might want to use the ccbot ux as the main/primary journey and not just as a fallback."* **ccbot never parsed a dialog in order to ANSWER it** — it mirrored the pane verbatim into ONE message (box drawing, the `❯` cursor, `☐/✔` checkboxes, descriptions, the tab bar), put a fixed 9-key D-pad under it, and after **every** keypress re-captured the pane and **edited that same message in place**. You watch the cursor move, in the chat. One message; deleted when the dialog resolves. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  **The three things chela lacks, measured against ccbot (2026-07-14):**
  (a) **NO FEEDBACK.** chela's card is a static render — tapping ↑/↓ changes **nothing on screen**, because `hook_askuq_signature` (`gatewatch.py` ~465) does not include the cursor, so the edit-in-place path is a no-op. In the nav-only fallback **you are driving a selector you cannot see.** This is the likeliest single cause of "not convenient".
  (b) **NOT UNIVERSAL.** ccbot detects **8** dialog shapes and renders them all identically, because mirroring a pane needs no per-shape parser (`ccbot/src/ccbot/terminal_parser.py:49-118` — `ExitPlanMode`, `AskUserQuestion` single **and** multi-tab, 2× `PermissionPrompt`, `BashApproval`, `RestoreCheckpoint`, `Settings`). chela has **three** hand-built detectors; a Bash approval, `RestoreCheckpoint`, `/model`, Settings, or any reworded dialog gets **nothing at all**.
  (c) **A `multiSelect` IS UN-ANSWERABLE.** chela's nav row has no `Space`, no `Tab`, no `←`/`→` — so a multi-select question literally cannot be toggled from the phone. ccbot has all of them.
  1. **Port the mirror as a FIRST-CLASS surface, not a fallback.** One tracked message per window: the sliced pane region, **edited in place** after every key, **deleted** when the dialog leaves the pane. Port the D-pad in full — `␣ Space` `↑` `⇥ Tab` / `←` `↓` `→` / `⎋ Esc` `🔄` `⏎ Enter` — and make `🔄` **re-render the same message**, not post a new photo below it (today's `🔄` posts a separate screenshot you must scroll to and mentally align — `inbound.py` ~668). **The cursor MUST be in the render signature** or the edit never fires and the whole feature is dead on arrival — that is bug (a), and it is the one that makes this feel broken.
  2. **Do NOT throw away the hook payload — it is the one thing ccbot could never have.** The pane truncates and mangles; the hook carries every option's `label`, `description` and **`preview`** in full (that is CMX-49, and it is why Liav can now *read* the question at all). The endgame is **both**: the rich, full-fidelity content from the hook **plus** a driver you can see and steer. Decide explicitly how they compose — the strongest candidate is *the mirror is the message body, the hook-derived semantic buttons sit above the D-pad when the mapping is provable* — and write your reasoning down. ⛔ Do **not** delete the CMX-49 cards to make room; a regression to pane-only content would take Liav's previews away and undo the task he just verified live.
  3. **Formatting is load-bearing — VERIFY IT, don't assume.** Box-drawing and a `❯` cursor only line up in a **monospace** context; check exactly how ccbot posts the pane (`handlers/interactive_ui.py:144-250`) and preserve whatever made it legible on a real phone. Get this wrong and the mirror is confetti.
  4. **THROTTLE, and never let decoration stall a real message.** ccbot does key → `sleep(0.5)` → re-capture → edit (`ccbot/src/ccbot/bot.py:1594-1697`). Every tap is a tmux capture **and** a Telegram edit, so a fast-tapping thumb is a rate-limit gun: dedup unchanged renders (skip the edit entirely), throttle the edits, and swallow `message is not modified`. **The rate-limit prerequisite is the task above** — land it first or this will be measured against a budget already being wasted.
  **Landmines:** ⛔ **do not regress CMX-49** (the hook cards, the previews, the `_answerable` proof-guard — a button whose ordinal mapping cannot be proven is CMX-32, tap 3 → answer 2, and it must stay refused); **the next task makes most of this D-pad unnecessary for hook-covered gates** (a zero-keypress answer needs no cursor to steer) — the mirror's enduring value is the shapes hooks **don't** cover and the **pre-plugin fleet**, so build it to coexist, not to compete; a failed edit (429 / deleted message / not-modified) must be **swallowed**, never propagated — it must never wedge the relay (CMX-43's rule); **poof only this tracker** — the gate/plan/status trackers each have their own lifecycle; a `RestoreCheckpoint` dialog collapses the arrow row to `↓` only in ccbot (`interactive_ui.py:81-141`) — understand why before flattening it; the target window is re-resolved from the message's **topic**, never trusted from the callback payload — keep that; **`_KEY_ACTIONS` is derived from the key table so a button and the key it fires cannot drift** (CMX-45) — keep that derivation. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (port from):** `ccbot/src/ccbot/handlers/interactive_ui.py` (`handle_interactive_ui` 144-250 — the slice + the verbatim render; the keyboard 81-141), `ccbot/src/ccbot/bot.py` (1594-1697 — key → sleep → re-capture → **edit in place**; the collapse-on-resolve at 1652 and 1793-1795; the free-text answer path at 998-1032), `ccbot/src/ccbot/terminal_parser.py` (49-118 — the 8 UI patterns; 196-240 — the status line, already ported), `ccbot/src/ccbot/handlers/status_polling.py` (86-102 — the poller that catches UIs the transcript never emits). **In chela:** `chela/telegram/gatewatch.py` (`poll` ~354, the post/edit/poof tracker, `hook_askuq_signature` ~465 — **the missing cursor**), `chela/telegram/interactive.py` (`nav_only_markup` ~273 — what this replaces; `NAV_KEYS` ~84), `chela/telegram/inbound.py` (`SCREENSHOT_KEYS` ~134, `_KEY_ACTIONS`, `_on_qa` ~640-693 — the tap handler that does **not** re-render), `chela/telegram/panescan.py` (the detectors), `chela/telegram/hookgate.py` (the content authority — keep it). **Verify:** unit-test that a keypress re-renders the tracked message **in place** (one message, not two); that an **unchanged** pane produces **no** API call; that the cursor position is part of the signature (move the cursor → the render changes); that the message is **deleted** when the dialog leaves the pane; that a failed edit is swallowed; that a dialog chela has **no detector for** still mirrors and is still drivable. **LIVE (mandatory — this whole task exists because a green suite cannot tell you an interface feels dead):** from the phone, drive a real **multi-question, multi-select** `AskUserQuestion` end to end — move between questions with `←`/`→`, toggle with `Space`, submit with `⏎` — **watching the cursor move in the message after every tap**. Then drive a **Bash approval** (a dialog chela has no detector for today). Screenshot both. Liav decides from that whether this becomes the primary journey.

- [x] **ANSWER A GATE FROM THE PHONE WITH ZERO KEYPRESSES — the hook can return the answer, and for multi-question / multi-select it is the ONLY safe way. (Depends on the task above.)** Keystroke injection cannot reliably answer the shapes we actually send: CMX-32 already fixed a **silent mis-answer** (tapping option 3 selected option 2 — Enter raced the selector's arrow moves), and a multi-question / `multiSelect` picker multiplies that race by every question. **PROVEN (measured 2026-07-13, Claude Code 2.1.207 — do not re-derive, do not trust the docs over this):** a `PermissionRequest` hook can **ANSWER** an `AskUserQuestion` by returning `{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow","updatedInput":{"questions":[…],"answers":{"<question string>":"<chosen label>"}}}}}` — **zero keypresses, in every permission mode including `auto`.** This retires the keystroke substrate for gates. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **🔴 A BOUNDED WAIT IS MANDATORY — this is the whole risk of the feature.** The hook runs **synchronously inside the agent's process**: the moment the endpoint blocks waiting for a human, **a live agent is frozen**. Symphony `SPEC.md` is explicit: *"A run MUST NOT stall indefinitely waiting for user input."* So the `/hooks/permission_request` handler must wait **at most** a configured budget (`CHELA_GATE_WAIT_S`, default it conservatively) for an answer to arrive from Telegram, then **give up and return `{}`**. ⛔ **The fallback on timeout is FAIL-OPEN to the existing pane path — NOT auto-deny.** Returning `{}` leaves the TUI picker exactly as it is today: still on screen, still answerable in tmux or by the current nav-row keystrokes, and the run is no worse off than before this feature existed. An auto-deny would *destroy* work on a slow human. Say this in a docstring so nobody "hardens" it into a deny later.
  2. **The wait budget must be SHORTER than the hook's own timeout, and that ceiling is a MEASURED number, not a guess.** The plugin manifest declares `{"type":"http", …, "timeout": N}`; if the daemon answers later than `N`, Claude Code kills the hook and fails open anyway — so a wait longer than the timeout is a wait that can never deliver an answer. **Empirically determine the maximum `timeout` Claude Code actually honours for an `http` hook** (raise it for `PermissionRequest` **only** — `PreToolUse`/`PostToolUse` fire at high volume and must stay fast; measured: 78% of the log is tool-call chatter). Then set the wait budget strictly below it. **Do not hardcode a number from memory or from the docs — measure it, and put the measurement in the PR.**
  3. **The answer must be matched to the RIGHT gate, and a stale answer must be refused.** The `answers` map is keyed by the **question string**, and a gate is identified by its `tool_use_id` (from `PreToolUse` — `PermissionRequest` has none, so you must correlate). A tap that arrives after the gate resolved, after the agent died, or for a *different* gate in the same topic **must be dropped and reported**, never applied to whatever is on screen now. `multiSelect` questions take a **list** of labels; single-select takes one. Answer **every** question in the payload — a partial `answers` map is a shape you must decide about explicitly (does Claude Code accept it? **test it**) and document.
  **Landmines:** ⛔ **do NOT flip the dispatcher to `acceptEdits` in this task** — that would extend blocking-on-a-human to every out-of-sandbox Bash command, and the handoff gates it on exactly this bounded-wait work landing first; **an in-cwd Bash command is sandbox-approved and does NOT gate even under `acceptEdits`** — only an **out-of-sandbox** path is a valid gate test, and a naive test gives a false negative; the daemon being **down or slow must never wedge an agent** (fail open, always — this is already the CMX-41 contract, do not weaken it); a blocked hook holds a **request thread** — N simultaneous gates must not exhaust the dashboard's threadpool (bound it, and say what happens at the bound); **`PermissionDenied` does not fire on an interactive deny** (measured, CMX-41) — do not build the resolution path on it; do not answer a gate for an agent the human never saw (an unbound topic must not silently auto-answer anything); keep the pane path working for pre-plugin agents. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/hooks.py` + `chela/dashboard/app.py` (`POST /hooks/<event>` — today returns `{}` and a test asserts the **absence** of `permissionDecision`/`hookSpecificOutput`; **that test now has to change, deliberately and visibly**), `chela/telegram/interactive.py` (the callback that will now deliver an answer instead of keystrokes), `chela/event_log.py`, `docs/HOOKS.md`, the previous task (the render this answers). **Verify:** unit-test that an answer delivered inside the budget returns a well-formed `updatedInput`; that the budget expiring returns `{}` (fail-open, **not** a deny); that a stale/mismatched `tool_use_id` is refused and logged; that a `multiSelect` question round-trips a list; that N concurrent blocked gates do not exhaust the server. **LIVE (mandatory):** drive a real 3-question `AskUserQuestion` with previews from a real agent, answer **all three from the phone**, and confirm from the agent's transcript that it received exactly the chosen labels — **with no keystrokes sent to the pane at all** (prove it: the answer path must not call `send_tmux`). Then let one gate **time out** and confirm the agent is unharmed and still answerable in the terminal.

- [x] **🔴 THE DISPATCHER WAS SILENTLY OFF FOR ~9 HOURS AND EVERYTHING REPORTED HEALTHY — `chela doctor` was ALL-GREEN, the daemon logged nothing, and reconcile stopped too.** Found live 2026-07-14. `CHELA_DISPATCH_WORKFLOWS` was **missing from `~/.chela/chela.env`** — dropped by the **CMX-42 env migration** (the PR whose entire purpose was to kill config drift). `config.py:257` read `""` → `DISPATCH_WORKFLOWS = []` → `main.py:131`'s `if DISPATCH_WORKFLOWS:` skipped the dispatcher **and the reconcile loop that lives in the same tick**. Consequences, all silent: no task was ever claimed from `TODO.md`; PR #61 merged and its run sat in `awaiting_review` forever; that stale run held the **only** concurrency slot (`concurrency.max: 1`), so even a correctly-configured dispatcher would have been blocked behind it. **The one and only tell was the ABSENCE of a log line** (`Dispatcher enabled for N workflow(s)` never printed). **Nobody reads an absence.** This is the same failure class as the CMX-42 port drift (hooks POSTing into a closed socket, feature silently dead) and it slipped through the very tooling built to catch that. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **A DISABLED SUBSYSTEM MUST ANNOUNCE ITSELF — loudly, at startup, every time.** `cmd_run` logs `Dispatcher enabled for N workflow(s)` when on and says **nothing** when off. Invert that: when `DISPATCH_WORKFLOWS` is empty the daemon must emit a **WARNING** naming the var, what is consequently not happening (**dispatch AND reconcile — say both**, the coupling is the non-obvious part), and how to fix it. Sweep `cmd_run` for **every** other capability behind an `if <config>:` guard (scheduler, inbox, notifications, terminals) and give each the same treatment: a startup line that states **on or off**, never silence-means-off. ⛔ An empty config may remain a *valid* state — it must not remain an *invisible* one.
  2. **`chela doctor` must verify the daemon's JOB, not just its env file.** `doctor.py:46` already lists `CHELA_DISPATCH_WORKFLOWS` among the vars it knows — and doctor still printed **all-green** while the dispatcher was dead. Checking "the env file parses and the running env agrees with it" is checking the wrong thing: **both agreed, and both were wrong.** Doctor must assert the *effective capability*: the dispatcher is enabled, each configured `WORKFLOW.md` **exists and parses**, its tracker file exists, and the daemon that is actually running has it on. A `WARN` (not a silent pass) when the dispatcher is off. Apply the same "assert the capability, not the config" lens to doctor's other checks.
  3. **Ship the var in the repo's env template so a fresh install cannot lose it again.** CMX-42 added `examples/`/`scripts/chela-env.sh`; whatever the canonical template is, `CHELA_DISPATCH_WORKFLOWS` must be **in it, with a comment stating that empty disables dispatch *and* reconcile**. The live `~/.chela/chela.env` was hand-repaired on 2026-07-14 (with that comment) — the repo must not still be able to generate the broken file.
  **Landmines:** ⛔ **do NOT "fix" this by defaulting `DISPATCH_WORKFLOWS` to `./WORKFLOW.md`** — a daemon that silently starts dispatching against whatever repo it happens to be `cwd`'d into is far worse than one that is off; the *empty* state is fine, the *silence* is the bug; don't turn the startup warning into a per-tick log line (it will bury the log within a day — announce at startup, and surface the state in `/api/settings` / the dashboard where a human can actually see it); doctor must not shell out to anything slow (it is run interactively); **doctor must be honest about what it cannot see** — it runs in a *different process* from the daemon (exactly the CMX-42 trap), so "the config says enabled" is **not** "the running daemon has it enabled": read the daemon's real state (the port-file pattern CMX-42 established for the dashboard is the precedent — consider having the daemon publish its effective capabilities the same way) or say plainly that it is inferring. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/config.py` (~250-261 — `DISPATCH_WORKFLOWS`, and the comment *"Empty = dispatcher off in the daemon"* that was accurate and useless), `chela/main.py::cmd_run` (~119-180 — the `if DISPATCH_WORKFLOWS:` guard and the log line that only fires on the happy path), `chela/doctor.py` (~46 — the var it already knows about and does not check), `~/.chela/chela.env` (the live file, hand-repaired), `scripts/chela-env.sh` + the repo's env template (the thing that must ship it), `chela/dashboard/app.py` (`/api/settings` — where an operator-visible "dispatcher: OFF" belongs). **Verify:** unit-test that an empty `CHELA_DISPATCH_WORKFLOWS` produces a **WARNING at startup** naming both dispatch and reconcile; that doctor **WARNs** (not passes) when the dispatcher is off; that doctor FAILs when a configured `WORKFLOW.md` is missing or unparseable; that the shipped env template contains the var. **LIVE (this is the whole point):** start the daemon with the var **empty** and confirm (a) the log says loudly that dispatch and reconcile are OFF, and (b) `chela doctor` does **not** report all-green — the exact scenario that hid for 9 hours must now be impossible to miss.

- [x] **🔴 HOOK EVENTS ARE FILED AGAINST THE WRONG WINDOW — the correlation key (`cwd`) is not stable.** Found the moment the plugin went live, 2026-07-14. The orchestrator's own session (`@0`, cwd `/home/liavedunix`) had been `cd`-ing into `~/projects/chelamux` to work, so **every one of its hook events was filed against `@1` (chelamux)** — a different agent:
  ```
  seq 20  hook.user_prompt_submit  wid=@1  cwd=/home/liavedunix/projects/chelamux  session=cf19ca61   <- actually @0
  seq 21  hook.pre_tool_use        wid=@1  cwd=/home/liavedunix/projects/chelamux  session=cf19ca61   <- actually @0
  ```
  `chela/hooks.py` correlates by matching the payload's **`cwd`** against tmux `#{pane_current_path}`. But **`cwd` is the session's *current* directory, not its identity** — it changes the moment an agent `cd`s anywhere, which is completely normal (any agent inspecting another repo, running a tool from elsewhere, or working across projects). The pane's `pane_current_path` is *also* mutable. So the key is mutable on **both** sides. CMX-41 deliberately chose "ambiguity resolves to `None`, never a guess" — but this is worse than ambiguity: it is a **confident wrong answer**, and an event filed against the wrong agent is exactly the failure that rule existed to prevent. **The whole timeline/inbox UI is per-window — building it on this key would make the UI lie.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **Correlate on `session_id` — it is stable, unique, and ALREADY IN EVERY PAYLOAD.** (Confirmed: `session=cf19ca61` is the orchestrator's real Claude Code session id.) You need a **`session_id` → window** map. The robust source: a window's pane → the `claude` **pid** (`agent_manager.claude_pid`) → the transcript that process holds **open** (`/proc/<pid>/fd/*`, or `lsof`), whose filename **is** `<session_id>.jsonl` (Claude Code writes `~/.claude/projects/<slug>/<session_id>.jsonl`). That gives an identity that survives any `cd`. **Verify this empirically before building on it** — do not assume the fd is held open; check.
  2. **Cache it, and hold no lock across the lookup.** A hook runs **synchronously inside a live agent** — walking `/proc` per event would be a disaster at `PreToolUse` volume. Resolve lazily, cache `session_id → wid`, and refresh on a miss (a new session appears; a window is renamed; a window dies). A cache miss must fall back to **`None`, never to a `cwd` guess.**
  3. **Keep `cwd` only as a last-resort HINT, and only when it is unambiguous** — or drop it entirely and say why. If two windows share a cwd, or the cwd matches a window that is *not* the session's, `None` is the correct answer. **A wrong `wid` is worse than no `wid`.**
  **Landmines:** the existing `hook.*` events already in `events.jsonl` have **wrong `wid`s** — decide whether to backfill, drop, or leave-and-document (they are already in the log; a UI built later will render them); don't regress the ~5ms budget (CMX-41 rejected the `pgrep` + `claude agents --json` path precisely because it takes *seconds*, and the agent is BLOCKED while a `PermissionRequest` hook runs); a subagent's events carry the **parent's** `session_id` — decide what that means for attribution and say so; the dispatcher's worktree agents live in `~/.chela/worktrees/...` and their cwd genuinely *is* their identity — don't break that case while fixing this one. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/hooks.py` (the correlation — the whole bug), `chela/agent_manager.py` (`claude_pid`, the window↔pid authority), `chela/telegram/monitor.py` (it already resolves a window's **transcript** — that is the same session→window link, solved once already: see CMX-9 "resolve transcript by newest record"), `docs/HOOKS.md`. **Verify:** unit-test that a payload whose `cwd` points at a *different* agent's directory still resolves to the **session's own** window; that an unknown session resolves to `None` (not a cwd guess); that the lookup is cached (one `/proc` walk, not one per event). **LIVE (this is the whole point):** from the orchestrator's own session (`@0`, home dir, but `cd`-ed into `~/projects/chelamux`), fire a real tool call and confirm the event lands with **`wid=@0`** — not `@1`. Then confirm a dispatched worktree agent still correlates correctly.

- [x] **FLAKY TEST — `test_wait_sleeps_normally_when_no_signal_arrives` asserts on the real wall clock and fails ~1 run in 5 under load.** Caught 2026-07-14 while verifying CMX-43 (it is **not** that PR's fault — it predates it, from `d82729e`, and is on `dev`). `tests/test_graceful_shutdown.py`:
  ```python
  started = time.monotonic()
  assert stop.wait(0.2) is False        # no signal → a plain sleep
  assert time.monotonic() - started >= 0.15
  ```
  Observed failure: `assert (28437.504260218 - 28437.388405753) >= 0.15` — the 0.2s wait measured **0.116s** on a busy machine. **A flaky test is worse than a failing one**: it teaches everyone to re-run until green, and this repo has *already* shipped four bugs that were green in CI and broken live — a suite nobody trusts is how the fifth gets through. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Verify.**
  1. **Stop asserting on the real clock.** The test's *intent* is "with no signal, `wait()` does not return early — it sleeps." Assert that **behaviourally**, not chronometrically: inject/patch the clock or the underlying `Event`, or assert on what `wait()` **returned** (`False` = timed out, which is already the first assertion and is the real contract) and drop the duration check entirely. If a duration assertion is genuinely wanted, it must tolerate a starved scheduler (a wide margin is a smell — prefer a fake clock, as `tests/test_telegram_status.py` correctly does for its throttle).
  2. **Sweep for the same shape.** Grep the suite for other real-clock assertions (`time.monotonic()`, `time.time()`, `time.sleep` + a duration assert) and fix them the same way. A CI box under load is exactly where these fire.
  **Landmines:** don't delete the test — the graceful-shutdown contract it guards is real (a daemon that returns early from `wait()` would spin); don't "fix" it by widening the margin to something like `>= 0.05` (that is the same bug with a longer fuse); keep the other tests in the file (a second signal must not raise). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `tests/test_graceful_shutdown.py`, `chela/` (`GracefulShutdown` — the thing under test), `tests/test_telegram_status.py` (the **fake-clock** pattern to copy). **Verify:** the decisive check is **statistical, not a single green run** — run the full suite **20×** under concurrent load (e.g. with other pytest processes running) and show **20/20 green**. State that in the PR. One green run proves nothing about a flake.

- [x] **`/screenshot` keyboard: drop the arrow WORD-labels, and add the missing 🔄 refresh.** Liav, 2026-07-13: *"in the screenshot keys, no need to write the arrows labels, and i'm missing the refresh button."* Two defects in `SCREENSHOT_KEYS` (`chela/telegram/inbound.py` ~134):
  ```
  ␣ Space   ↑        ⇥ Tab        <- ↑ is bare, but...
  ← Left    ↓ Down   → Right      <- ...these three carry redundant words
  ⎋ Esc     ^C       ⏎ Enter
  ```
  (a) the arrow captions are **inconsistent and redundant** — `↑` is already a bare glyph while `← Left` / `↓ Down` / `→ Right` spell it out; the glyph *is* the label, and the words only cost width on a phone. (b) **There is no refresh button on this keyboard at all** — `🔄` (`ref`) exists only on the *AskUserQuestion* nav row (`chela/telegram/interactive.py:84`, `qa:nav:ref`), so after driving the terminal with these keys there is **no way to see the result** without sending `/screenshot` again by hand. The re-capture code already exists and is already shared (`inbound.py` ~463, "Shared by the `/screenshot` command and the AskUserQuestion 🔄 refresh") — it is simply not wired to a button here. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Arrows become glyph-only:** `←` `↑` `↓` `→`. Leave the non-arrow keys' words (`␣ Space`, `⇥ Tab`, `⎋ Esc`, `^C`, `⏎ Enter`) — those glyphs are **not** self-evident and the words earn their width. Re-flow the rows so the four arrows sit together (they are now narrow), e.g. a dedicated arrow row; keep it to 3–4 buttons per row so nothing truncates on a phone.
  2. **Add `🔄` refresh to the screenshot keyboard**, firing the existing re-capture (**edit the screenshot message in place** — do not post a new one each tap, or a few taps bury the topic). Reuse the shared refresh path rather than writing a second one.
  **Landmines:** `_KEY_ACTIONS` is derived from `SCREENSHOT_KEYS` precisely so a button and the key it fires **can never drift apart** — keep that derivation; **`ref` has no tmux key** (it is `None` in the nav row — a refresh is not a keypress), so the `(label, key_id, tmux_key)` tuple shape must tolerate that or the flattening will break; don't change the `k:` callback prefix or the `qa:nav:*` scheme (both are live); the target window is re-resolved from the message's **topic**, never trusted from the payload — keep it that way; the toast must still echo what was sent. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/inbound.py` (`SCREENSHOT_KEYS` ~134, `_KEY_ACTIONS` ~146, `_KEY_CB_PREFIX`, the shared snapshot/refresh ~463, the key handler ~540), `chela/telegram/interactive.py` (`NAV_KEYS` ~84 — how `🔄`/`ref` is modelled there; `_snapshot`/refresh handling ~339-351). **Verify:** unit-test that every button in `SCREENSHOT_KEYS` resolves through `_KEY_ACTIONS` (no drift), that `ref` maps to a refresh and not a keypress, and that a refresh **edits** the existing message rather than posting a new one. **Live:** `/screenshot` from the phone → tap an arrow → tap 🔄 → the same message updates in place showing the moved cursor.

- [x] **A passthrough slash command (`/clear`, …) sends SILENTLY — no confirmation, so from a phone it is indistinguishable from being swallowed.** Liav, 2026-07-13: *"when we send escape from telegram i see 'sent escape' but not for /clear — can we add send confirmation as well?"* `/esc` is a **BRIDGE** command, handled in-process, so it replies `⎋ Sent Escape` (`chela/telegram/inbound.py:515`). `/clear` and every other Claude Code command in `PASSTHROUGH_COMMANDS` is forwarded raw to tmux and the handler returns **silently**. Given that this exact path was broken until today (`/clear@chelamuxbot` was forwarded verbatim and Claude Code never recognised it — the session was never cleared, and *nothing said so*), silence is the worst possible feedback. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Confirm the SEND, and claim nothing more.** After the passthrough `send_tmux` returns, reply with what is actually known — e.g. `⏎ Sent /clear` on success, `❌ Couldn't send /clear` on failure — mirroring the Escape reply's shape and tone. ⛔ **Do NOT say "session cleared"** or otherwise assert the command's *effect*: all we know is that the keystrokes were delivered. Over-claiming here is how a silent failure becomes a lying success.
  2. **Scope it to commands — do NOT confirm every plain message.** A `✓` on every chat message would turn a topic into acknowledgement spam. Passthrough **slash commands** only (the `PASSTHROUGH_COMMANDS` path). Consider whether a Telegram **reaction** on the user's own message (`setMessageReaction`, e.g. 👍) is less noisy than a new message — if you choose the reply, keep it terse and consistent with `⎋ Sent Escape`; if you choose the reaction, say why and make sure a *failure* is still loud (a missing reaction is not a visible error).
  **Landmines:** slash commands go through the Escape-first path (`send_tmux` sends Escape, settles, then `/cmd` + Enter) — the confirmation must reflect the **whole** sequence succeeding, not just the first keystroke; don't regress the `@botname` strip (`resolve_command_for_window` — a group delivers `/clear@chelamuxbot`); don't confirm a command that was **dropped** (an unbound topic, a chat-gate rejection) — that must stay a rejection, not a cheerful "sent"; keep the reply out of the transcript-relay path so it can't be mistaken for an agent message. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/inbound.py` (`PASSTHROUGH_COMMANDS` ~75, `resolve_command_for_window` ~86, the Escape reply ~515, the passthrough forward path), `chela/messenger.py::send_tmux` (the return value that tells you it landed). **Verify:** unit-test that a passthrough command replies on success and on failure, that a *dropped* command does not reply "sent", and that a plain message still gets no confirmation. **Live:** send `/clear` from the phone and see the confirmation — then confirm the session actually cleared.

- [x] **`chela msg` says an agent is OFFLINE while `/api/agents` says it is BUSY — two authorities disagreeing about the same window.** Hit live 2026-07-13: `chela msg @32 "..."` → `@32 offline — message not delivered`, while `curl /api/agents` reported `@32 cmx-43 busy` **at the same moment** and the window was plainly alive and working (a `messenger.send_tmux("@32", …)` straight from Python delivered fine). So a real message to a real, working agent was **silently dropped** — the orchestrator's own agent-to-agent channel failed closed with a wrong reason. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Find the second source of truth and kill it.** `/api/agents` (`agent_manager.session_status_map` / `claude_pid` — the `claude agents --json` authority) is the documented one place agent liveness lives. `chela msg` is evidently deciding liveness some other way (a stale cache, a pid check, a window-name lookup, a `liveness` helper). Make `msg` / `broadcast` use **the same authority** as `/api/agents`, or explain in the code why they legitimately cannot and what the fallback is. **Do not add a third.**
  2. **A message must never be silently lost.** If liveness genuinely cannot be established, the honest behaviours are (a) **send anyway** — a paste into a live pane is harmless and the agent picks it up — or (b) fail **loudly** with the real reason and a non-zero exit. Silently reporting "offline" about a busy agent is the worst of both. Pick one, and say why.
  **Landmines:** don't regress the `chela msg` refusal to self-notify (an orchestrator messaging its own window is a loop, and that guard is deliberate); a *genuinely* dead window must still be reported as such (don't invert the bug); `session_status_map` is **cached** — use `force=True` for a fresh read where correctness matters (the dispatcher already learned this); a busy agent's pane shows an empty `❯` **below** its spinner, so "the prompt looks idle" is NOT evidence of death (that misread is what produced the false-`DIED` bug, CMX-35). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/main.py` (`cmd_msg` / `cmd_broadcast`), `chela/messenger.py` (`send_tmux` — the path that *worked*), `chela/agent_manager.py` (`session_status_map`, `claude_pid`, `liveness` — the real authority), `chela/dashboard/app.py::api_agents` (what `/api/agents` actually reports). **Verify:** unit-test that a busy agent is messageable; that a dead window fails loudly with the real reason; that self-notify is still refused. **Live:** dispatch an agent, and while it is **busy**, `chela msg` it — the message must arrive (this is the exact case that failed).

- [x] **Port ccbot's EPHEMERAL STATUS LINE — the live "Claude is working" verb (+ shell count), as a self-deleting message.** Liav, 2026-07-13: *"ccbot showed the 'claude working verb' and 'x shells running' in a 'disappearing' text, can we port this?"* On a phone you currently cannot tell a **thinking** agent from a **dead** one — chela relays only finished turns. ccbot showed Claude Code's own status line (`✻ Boondoggling… (2m 45s · ↓ 12.0k tokens)`) as a single message that **edits in place** while the agent works and **deletes itself** when the turn ends. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–4 + Landmines + Verify.**
  ℹ️ **Hooks cannot provide this and never will** — the working verb is a **TUI render**, not an event (there is no `Boondoggling` hook). Unlike the interactive gates, **the pane is the correct source here permanently.** Do not "modernize" this onto the event log.
  1. **Port the parser** (`chela/telegram/panescan.py`, next to the gate detectors). ccbot's `parse_status_line` — `~/projects/ccbot/src/ccbot/terminal_parser.py:202` — is the reference and its **anchoring is the whole trick**: find the **chrome separator** (the run of `─` in the last ~10 lines) and read the line(s) just **above** it, returning the text after a leading spinner glyph (`· ✻ ✽ ✶ ✳ ✢`). ⛔ Do **not** grep the pane for a spinner char — Claude's normal output is full of `·` bullets, and that is exactly the false-positive this anchoring exists to avoid. **Also: verify empirically what "x shells running" actually is on Claude Code 2.1.207** — capture a live pane with a background shell and look. If it is part of the same status line, it comes along free; if it is separate chrome, decide whether it is worth a second parse and say so. Don't guess.
  2. **Ride the EXISTING per-tick pane capture — add no tmux calls.** `gatewatch.poll()` (`chela/telegram/gatewatch.py:354`) already captures every bridged window's pane once per tick for the three gate detectors. The status line is a fourth read of the **same** text. A separate poller would double the tmux load on the whole fleet for nothing.
  3. **Reuse the tracked-message machinery — the status is a new "kind".** `gatewatch` already posts / **edits in place** / **poofs** a tracked message per window per prompt kind (the `_uq_msg` / `_plan_msg` pattern, `~393` post-or-edit, `~436` clear-and-poof). A status message is the same shape with a different lifecycle: **post on first sight → edit while the verb changes → DELETE when the status line leaves the pane** (the turn ended). That deletion is the "disappearing" behavior and is the point — a topic must not accumulate dead status messages.
  4. **THROTTLE, or Telegram will rate-limit the topic.** The status line ticks roughly every second. ccbot caps edits with a `STATUS_EDIT_MIN_INTERVAL` per chat and **skips the edit entirely when the text is unchanged** — port both. A dropped update is **fine** (the next poll re-sends the latest); a 429 that wedges the relay is not. Also port the **typing indicator**: `sendChatAction(TYPING)` while the pane says `esc to interrupt` — that is what makes a phone feel live.
  **Landmines:** a status edit that fails (429 / message deleted / "not modified") must be **swallowed**, never propagated — this is decoration, and it must never wedge the relay or delay a real message; the existing 429 `retry_after` loop must not be bypassed *or* allowed to block on a status edit (decide which, and say why); **poof only the status message** — do not touch the gate/question/plan trackers, which have their own lifecycle; if the agent's window changes under a topic, delete the old status and post fresh (ccbot does this — `message_queue.py:477`); the status must not be the *last* thing a user sees in a topic after it self-deletes (make sure a real assistant message still lands normally); an **idle** agent has no status line — that is the delete trigger, not an error. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (port from):** ccbot `src/ccbot/terminal_parser.py:196-240` (`parse_status_line`, `STATUS_SPINNERS`) and `src/ccbot/handlers/message_queue.py:470-575` (the tracked status message: edit-in-place, throttle, dedup, typing indicator, delete-on-clear, window-change). **In chela:** `chela/telegram/panescan.py` (put the parser here), `gatewatch.py` (`poll` ~354, the post/edit/poof tracker ~393/~436), `relay.py` (`edit` ~492, and the delete path). **Verify:** unit-test the parser against real captured panes — a working pane (verb extracted), an **idle** pane (None), a pane whose output contains `·` bullets (**None** — the false positive that motivates the anchoring), a pane with no chrome (None); the throttle (no edit inside the interval), the dedup (unchanged text → no API call), delete-on-idle, and that a failed edit is swallowed. **LIVE (mandatory):** watch a real agent work from the phone — the verb appears, **updates as it changes**, and **vanishes when the turn ends**, with no leftover message in the topic and no rate-limit errors in `pm2 logs chela-telegram`.

- [x] **ENV IS THE SINGLE SOURCE OF TRUTH — config must not also live in the PM2 file or in CLI args.** Liav, 2026-07-13: *"env should be single source of truth and not also in pm2 config file."* Today config is scattered across three places and they have already drifted: (a) `~/.chela/ecosystem.config.js` duplicates `CHELA_TMUX_SESSION` / `CHELA_DIR` into **three separate `env:` blocks** — and **all three still said `'ccbot'`** after the tmux session was renamed to `chela` (a clean `pm2 start ecosystem.config.js` would have brought the whole fleet up against a session that no longer exists); (b) the dashboard port is a **CLI arg** (`--port 5005`) which `main.py` writes into `os.environ` **inside that process only**; (c) `run-telegram.sh` already does it right (`set -a; source ~/.chela/telegram.env; set +a`). **This is not cosmetic — it is why CMX-41 is broken:** `config.dashboard_port()` (the "single authority" for the port the hook plugin POSTs to) is a **different process** from the dashboard, so it reads no `CHELA_DASHBOARD_PORT`, falls back to `5001`, and `chela plugin` renders `http://127.0.0.1:5001/hooks/…` while the dashboard actually serves **5005**. Every hook then POSTs into a **closed socket**, fails open, and **the whole hook feature silently does nothing**. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **One env file, sourced by everything.** A single `CHELA_DIR/chela.env` (keep `telegram.env` separate for the **secret**, or fold it in and document the permissions — decide and say why) holding `CHELA_TMUX_SESSION`, `CHELA_DIR`, `CHELA_DASHBOARD_PORT`, `CHELA_TERMINALS_*`, `CHELA_IGNORE_WINDOWS`. Every PM2 app sources it via a thin launcher (`run-telegram.sh` is the pattern that already works) instead of carrying its own `env:` block. **Ship the ecosystem template + the launcher in the repo** so a fresh install gets it right; `chela` must still run correctly from a plain shell with nothing exported (documented defaults).
  2. **The port must be discoverable ACROSS processes — that is the actual bug.** `CHELA_DASHBOARD_PORT` in the env file becomes the authority; the dashboard **binds what the env says** (a `--port` flag may still override for a one-off, but then it is NOT the source of truth and `chela plugin` must not be fooled by it). Belt-and-braces: have the dashboard **publish the port it actually bound** to a small state file under `CHELA_DIR` at startup, and have `config.dashboard_port()` prefer that over the default — so the manifest is right even when someone passes `--port` by hand. Whatever you choose, **`chela plugin` must render the port the dashboard is really listening on**, and a mismatch must be **loud**, not silent.
  3. **Make the drift impossible to repeat.** Add a check (a `chela doctor`-style command, or a startup warning) that fails loudly when the config a service is *running* with disagrees with the env file — that is the exact failure that let `CHELA_TMUX_SESSION: 'ccbot'` sit in the restart path for a day while the live processes said `chela`.
  **Landmines:** ⚠️ **`CHELA_TMUX_SESSION` is LOAD-BEARING** — `config.current_session()` resolves `$CHELA_TMUX_SESSION` → **auto-derive from `$TMUX_PANE`** → `"chela"`, and a stale `TMUX_PANE` leaking through PM2 makes a service silently target a **`webterm_*` mirror** session. Do not "simplify" the override away; start services with `env -u TMUX -u TMUX_PANE`. **`pm2 restart --update-env` MERGES env, it does not delete a removed var** — clearing one truly needs `pm2 delete` + a fresh `pm2 start` from a non-tmux shell, so document the migration. Don't break `chela-telegram` (it is **missing from `ecosystem.config.js` entirely** today, and the daemon is named `chela` there but `chela-daemon` in the live process list — both are drift). The secret (`TELEGRAM_BOT_TOKEN`) must not land in a world-readable file or in git. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/config.py` (`CHELA_DIR`, `current_session`, `dashboard_port`), `chela/main.py` (~911 — where `--port` writes `os.environ` in-process only; ~1096), `chela/dashboard/app.py` (~2893 — the other `5001` default), `chela/hooks.py` + `chela plugin` (the consumer that is broken by this), `~/.chela/run-telegram.sh` (the pattern that already works), `~/.chela/ecosystem.config.js` (the drift — a backup is at `ecosystem.config.js.bak-2026-07-13`; the live file's session pins were hand-fixed to `chela` on 2026-07-13). **Verify:** unit-test that `dashboard_port()` returns the port the dashboard actually bound, from a **separate process**; that a `--port` override still renders correctly in the manifest; that a missing env file falls back to documented defaults without crashing. **LIVE (this is the whole point):** with the real dashboard on **5005**, run `chela plugin` and confirm the rendered manifest says **5005**, not 5001 — then POST a hook to it and see the event land in `chela events`.

- [x] **EVENT LOG — part A: the store, the read API, the CLI. (No hooks, no plugin, no UI — those are part B and later.)** chela has nowhere for an event to *live*: `chela msg` / `broadcast` fire into a pane and vanish, watches exist only as keys in `inbox.json`, the queue is transient. That is the root cause behind every inbox bug fixed today — the false-`DIED` (state inferred, not observed), the stale `awaiting_review` (a snapshot with no expiry), the essay-as-notification (no title/payload split). **You cannot render, filter, thread, or reconcile prose.** PR #51 already made an inbox event a **record** (`kind` + `summary` + `payload`) — that is the shape this log stores. Build the log as a standalone subsystem with a **programmatic append**, so it is fully testable with zero Claude Code involvement; part B then feeds it from hooks. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **The store: append-only JSONL + monotonic `seq` + `boot_id`.** Append to `CHELA_DIR/events.jsonl`, one JSON object per line (the audit trail). Each record: **`seq`** (monotonic, process-local), **`boot_id`** (changes on restart — so a stale cursor is *detectable* rather than silently wrong), `ts`, **`type`**, **`wid`** (the chela window, when known), `session_id`, a one-line **`summary`**, and a structured **`payload`**. Mirror PR #51's record shape — do NOT invent a second event schema. Keep a bounded in-memory ring (~2000) for live reads; older events stay in the JSONL. **Rotate the file** — an unbounded append on a busy fleet is a disk-filler.
  2. **The read API: cursor-based, filterable, resumable.** `chela events [--after-seq N] [--type X] [--wid @N] [--follow]` — replay from a cursor, filter, tail live. On a **`boot_id` change, report a resume gap** rather than pretending the cursor is valid. This is the one authority the dashboard timeline and the inbox will both consume — do **not** add a second event source alongside it.
  3. **A concurrency-safe append.** Multiple writers (daemon, dashboard, CLI, and later N hook calls) append at once. Use an O_APPEND write of a single line (atomic on POSIX for small writes) or a short-held lock — but **never hold a lock across other I/O**, and never let a writer block. Decide and document which, and prove it: hammer it from N concurrent processes and assert every line is intact and `seq` has no duplicates/gaps.
  **Landmines:** don't build the UI, don't build hook ingestion, don't touch `~/.claude/`; the record shape must match PR #51's (`chela/inbox.py`) or the log and the inbox will drift apart; a corrupt/truncated final line (a crash mid-append) must be **skipped on read, not fatal**; `seq` must survive a restart (derive it from the file's last line, or scope it per `boot_id` and say so); don't regress `chela watching` / the inbox. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/inbox.py` (the event record from PR #51 — `kind`/`summary`/`payload`; and the inbox is the log's first consumer), `chela/config.py` (`CHELA_DIR`), `chela/agent_manager.py` (window ↔ cwd ↔ pid correlation). Prior art: Mosaic `docs/events.md` (`seq` / `boot_id` / `after_seq` / categories / heartbeats + a JSONL audit) — same shape, independently arrived at. **Verify:** unit-test append → read → cursor → filter; a `boot_id` change surfaces a **resume gap**; a truncated final line is skipped, not fatal; `seq` is unbroken across a restart; the concurrency hammer above. **Live:** append real events from two processes at once, then `chela events --follow` in a third and show them arriving in order.

- [x] **EVENT LOG — part B: feed it from Claude Code HOOKS, shipped as a PLUGIN. (Depends on part A.)** Hooks deliver *typed, structured* events **before** the fact — unlike the transcript, which only lands interactive records at resolution (the reason the current gates are pane-scraped). ⚠️ **Scope: ingestion only. This slice is OBSERVE-ONLY — it must NOT answer any gate.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  **PROVEN GROUND (measured 2026-07-13, Claude Code 2.1.207 — do not re-derive, do not doubt, and do NOT trust the docs over this):** `AskUserQuestion` / `ExitPlanMode` are **not** hook events — they are **TOOLS**, and `PreToolUse` / `PermissionRequest` fire for every tool carrying `tool_name` + the full `tool_input`. **`PermissionRequest` fires for `AskUserQuestion` in EVERY permission mode, including `--permission-mode auto`** (what the dispatcher uses) — auto does *not* auto-answer a question, it genuinely blocks on the picker. Payloads carry `session_id`, `transcript_path`, `cwd`, `prompt_id`, `permission_mode` (+ `tool_use_id` on `PreToolUse`, **not** on `PermissionRequest`). Hook events include: `PreToolUse`, `PostToolUse`, `PermissionRequest`, `PermissionDenied`, `UserPromptSubmit`, `SessionStart`, `SessionEnd`, `Stop`, `SubagentStart`, `SubagentStop`, `Notification`, `PreCompact`, `PostCompact`, `Elicitation`.
  1. **🔌 SHIP A CLAUDE CODE PLUGIN — do NOT edit the user's `settings.json`.** A plugin can carry hooks: declare them in `hooks/hooks.json` (or inline in `.claude-plugin/plugin.json`), with paths via **`${CLAUDE_PLUGIN_ROOT}`**. **Hooks MERGE additively across all scopes** — plugin hooks fire *alongside* the user's own rather than overriding them — so chela never opens, backs up, or risks corrupting a settings file holding hundreds of the user's permission entries. Install: `/plugin marketplace add <repo>` + `/plugin install chela`. Build the plugin skeleton + document the install. (Plugin hooks have the **lowest precedence** and fire after user/project hooks — fine for observe-only.)
  2. **Prefer the `http` hook type — the daemon is already listening.** `{"type": "http", "url": "http://127.0.0.1:<dashboard port>/hooks/<event>", "timeout": 2}` POSTs the event JSON straight to chela's running process: **no shell script, no process spawn per event, no PATH assumptions.** Add the receiving endpoint; it appends via part A's API and returns immediately.
  3. **Correlate the event to a window WITHOUT the pane.** Use `cwd` / `session_id` against `agent_manager` (the `/api/agents` authority). If you find yourself scraping a pane to identify an event, you have reinvented the thing this replaces.
  **Landmines:** 🔒 **do NOT write `~/.claude/settings.json` at all** — the plugin mechanism exists precisely so you don't have to; if you find yourself opening that file, stop. **A hook runs SYNCHRONOUSLY inside the agent's process** — a slow or crashing hook **stalls or breaks a live agent**: short explicit `timeout` (~2s), append-and-return, no lock held across I/O, **fail OPEN if the daemon is down** (agent proceeds, event lost — never wedge the agent). ⚠️ **OBSERVE-ONLY:** return **no** `hookSpecificOutput` and **no** `permissionDecision` — printing a decision would silently start auto-answering the user's permission prompts. Only exit code `2` blocks a tool call; a command-hook with stray stdout (a chatty `.bashrc`) corrupts the JSON contract — another reason to prefer `http`. **Hooks are read at agent STARTUP**, so an already-running fleet has none → **keep the pane-scraped gates as the fallback**; do not delete them. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** part A's store/API; `chela/dashboard/app.py` (where the endpoint goes); `chela/agent_manager.py` (correlation); `chela/telegram/{panescan,gatewatch,interactive}.py` (the substrate this will eventually replace — **not in this slice**). **Verify:** unit-test the endpoint appends a well-formed record and returns fast; that a malformed payload is dropped without raising; that the daemon being **down** does not wedge the caller. **LIVE (mandatory):** install the plugin into an **ISOLATED** settings/config (⛔ NOT Liav's real `~/.claude/` — he runs `/plugin install` himself), run a real agent, drive a real `AskUserQuestion` + a real Bash gate + a session start/stop, and show the resulting `events.jsonl` lines — **with the AskUserQuestion payload's per-option `label` and `description` present in the log**. Then confirm a normal agent still runs **unaffected** with the hooks installed (no stall, no error).

- [x] **`WORKFLOW.md` is read once at startup — an edit needs a restart, and a BAD edit takes the daemon down.** Symphony (the spec chela's workflow format is derived from — `github.com/openai/symphony`, `SPEC.md` §6.2) makes this a **MUST**: *"The software MUST detect `WORKFLOW.md` changes. On change, it MUST re-read and re-apply workflow config and prompt template without restart"* and *"invalid reloads MUST NOT crash the service; keep operating with the last known good effective configuration and emit an operator-visible error."* chela does neither: `chela run` (PM2 `chela-daemon`) parses the workflow at boot, so changing `polling.interval_ms`, `concurrency.max`, `agent.cmd` or the prompt body means a restart — and a YAML typo means a **broken daemon**, not a skipped tick. This matters more now that the dispatcher runs unattended and writes `dev`. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Detect and re-apply on change, without restart.** Re-read `WORKFLOW.md` when its mtime/hash changes and re-apply the config + prompt template to the *next* tick. Per the spec, an in-flight agent is **not** restarted on a config change (`"Implementations are not REQUIRED to restart in-flight agent sessions automatically"`) — the new config governs the next dispatch. The effective poll interval should be updated when the config is re-applied.
  2. **Keep last-known-good on a bad reload — never crash the loop.** A YAML/parse error must leave the daemon running on the last valid config, emit an **operator-visible** error (log + surface it on `/api/settings` or the dashboard, not just a log line nobody reads), and **block new dispatches until fixed** while **reconciliation keeps running** (`"Workflow file read/YAML errors block new dispatches until fixed. Template errors fail only the affected run attempt."` — note the distinction: a bad *template* fails one run, a bad *config* pauses dispatch).
  **Landmines:** don't reload mid-dispatch (apply at a tick boundary, not inside `_spawn`); a config change must not orphan or re-key in-flight runs; don't re-read the file on every tick if nothing changed (hash/mtime gate — this runs every 30s forever); the daemon must survive a **partially-written** file (an editor saving) — treat a parse failure as "keep last good", not "the workflow is now empty"; don't regress `--dry-run` or `--once`. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (workflow load + the tick loop), `chela/main.py::cmd_run` (the daemon loop — where a reload check belongs), `WORKFLOW.md` (the front-matter schema). Prior art: Symphony `SPEC.md` §6.2 (dynamic reload) + §6.3 (startup and per-tick validation). **Verify:** unit-test that a changed workflow is re-applied without a restart; that a **broken** workflow leaves the last-good config in force, blocks dispatch, keeps reconciling, and surfaces the error; that an unchanged file is not re-parsed every tick. **Live:** with the daemon running, edit `concurrency.max` and confirm the next tick honors it with no restart; then introduce a YAML syntax error and confirm the daemon **stays up**, keeps reconciling, refuses to dispatch, and says why.

- [x] **Any assistant message over ~4096 chars renders as RAW MARKDOWN on Telegram — the 4096 split cuts an inline entity in half.** CONFIRMED live 2026-07-13 (Liav's screenshot, `nautilus` topic: literal `**zero funding**` and `## 3. The funding "tailwind"` on screen). Bridge logs give the exact cause: `Bad Request: can't parse entities: Can't find end of Bold entity at byte offset 4316` and `… Can't find end of Code entity at byte offset 4135`. **Mechanism:** `relay.split_message` chunks at `MAX_LEN = 4096`, each chunk is posted with `parse_mode=MarkdownV2`, and a chunk whose boundary lands inside `*bold*` / `` `code` `` ends with an **unterminated entity** → Telegram rejects it → the relay's fallback re-sends the **whole message** as plain text (`parse_mode=None`), so the reader sees raw `**` and `##`. The splitter **already** handles UTF-16 lengths (`_utf16_len`) and code **FENCES** (`_FENCE`/`_FENCE_RESERVE` — it appends a closer and reopens on the next chunk) — it just never does the same for **inline** entities. Short messages format fine, which is why this looked random: **the trigger is length**. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **Split on entity-safe boundaries.** In `chela/telegram/relay.py::split_message`, prefer to break at a **blank line, then a newline, then a space** at/below the limit, rather than mid-token — a paragraph break can't sit inside an inline entity. This alone fixes the common case and is the cheapest correct move. Keep the existing UTF-16 accounting (`_utf16_len`) — the limit is UTF-16 code units, not Python chars, and the observed byte offsets (4135, 4316) are past 4096 precisely because of multi-byte characters.
  2. **Balance inline entities across a boundary, the way fences already are.** If a chunk still ends inside an open inline entity (`*bold*`, `_italic_`, `__underline__`, `~strike~`, `||spoiler||`, `` `code` ``), **close it at the end of the chunk and reopen it at the start of the next** — the exact trick `_FENCE`/`_FENCE_RESERVE` already performs for ``` fences; generalize that, don't special-case bold. Reserve room for the closer so the reopened chunk still fits (mirror `_FENCE_RESERVE`). Remember `render_markdown`/`telegramify-markdown` escapes text first, so an entity marker may be **escaped** (`\*`) and must NOT be treated as an opener — parse what is actually emitted, not the source markdown.
  3. **Make the fallback per-CHUNK, not whole-message.** Today one rejected chunk downgrades the entire message to plain text (`BotSender.send` returns False on the first failure and the caller re-sends everything unformatted). A single bad chunk should not strip formatting from the other five. Fall back only for the chunk that failed. (Keep the existing 429 / `retry_after` behavior intact — a flood-control 429 is NOT a rejection and must still sleep-and-resend the same payload.)
  **Landmines:** don't regress the fence handling, the 429 retry loop, or the `reply_markup`-on-first-chunk rule (`markup_json and i == 0`); the limit is **UTF-16 code units** — do not "simplify" to `len(text)`; an entity closer you inject must itself be counted against the limit; don't break `skills/telegram-send/send.py`, which shares the same split approach (check whether it should be fixed too or left alone); plain-text fallback must remain byte-safe. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/relay.py` (`MAX_LEN` ~35, `_FENCE`/`_FENCE_RESERVE` ~43-47, `split_message`, `BotSender.send` ~282-306 — the `if not resp.get("ok")` early-return is the whole-message fallback), `chela/telegram/format.py` (`to_markdown_v2` ~199, `render_markdown`, `escape_markdown_v2`). **Verify:** unit-test that a >4096-unit body containing bold/inline-code spanning the boundary produces chunks that are each **individually valid MarkdownV2** (every entity opened in a chunk is closed in it); that a fence spanning the boundary still works; that one rejected chunk no longer downgrades the others. **LIVE (mandatory — this bug was invisible to a green suite):** relay a real >4096-char assistant message containing `**bold**`, `` `code` `` and a `##` heading through the actual bridge and confirm on Telegram that it renders **formatted**, not as raw `**` — reproduce with the nautilus-style long report that triggered it.

- [x] **Inbox events are a PROSE SNAPSHOT, not a record: they go stale before delivery, and the notification text is the entire TODO item.** Two defects, one root cause — an event is a formatted string built at queue time, so it can be neither re-validated nor rendered. Both observed live 2026-07-13. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Re-validate an event at DELIVERY time — a queued event is a claim about the past.** Observed: `📥 run aeaae296a725 (…) is awaiting review — <PR #47 url>` was delivered to the orchestrator **after that PR had already been reviewed and merged**. The event was queued when the PR opened (true then), but delivery is deliberately deferred until the orchestrator is `idle` — and by then the world had moved. So the orchestrator is handed a stale instruction to do work that is already done. **Fix:** before pushing a queued event, re-check the fact it asserts (run state / PR state) and **drop or supersede it if it no longer holds** (a merged PR is not awaiting review). This is distinct from the false-`DIED` bug (state *inferred* rather than observed) — here the state was right when queued and rotted in the queue. Note the inbox already re-resolves the orchestrator every tick; the *events* get no such treatment.
  2. **Give an event a TYPE, a SUMMARY and a PAYLOAD — stop shipping the whole task body as the notification.** Observed: the `awaiting_review` event's text is the **entire multi-paragraph TODO item** (the full "Make the dispatcher's default agent launch mode editable…" brief, landmines and all) pasted into the orchestrator's window as a notification. A notification needs one line; the essay belongs in a payload the reader can open. Today `queue` holds `{"kind", "text", "wid", "clear_watch"}` where `text` is a pre-rendered string — so nothing downstream (a UI, a filter, a de-dup) can do anything with it but print it. Restructure the queued event into a record: **`kind`** (already there), a short **`summary`** (≤1 line — e.g. `cmx-34 awaiting review — PR #47`), and a **`payload`** (run id, wid, PR url, task title, timestamps). Render the one-liner for the tmux push; keep the payload for the log/UI that will consume it next. **This is the first brick of the event log** — do not build the log here, but do not build a shape the log will have to throw away either.
  **Landmines:** don't break the `idle`-only push gate (never push into a `waiting` agent — it would answer a permission prompt with the notification text); a re-validation must not become a slow network call inside the store lock (`tick()` already keeps the critical section short — do the check OUTSIDE it, like the status probes and the runs query); dropping a stale event must be logged, not silent, or a real event lost to a bug becomes undebuggable; keep the existing `runs_seen` de-dup working. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/inbox.py` (`tick` ~328, `run_events` ~263, the queue records + `_line()` formatter, `runs_seen`), `chela/dispatcher.py` (`list_runs` — run state + PR url). **Verify:** unit-test that an `awaiting_review` event whose PR is merged before delivery is **dropped (and logged)**, not pushed; that a still-open PR is pushed normally; that a queued event carries `kind` + a one-line `summary` + a structured `payload`, and the tmux push renders only the summary. **Live:** dispatch a task, merge its PR *before* going idle, and confirm the orchestrator is NOT handed a stale "awaiting review" for it.

- [x] **EVERY dispatched PR conflicts on `TODO.md` — the agent and the orchestrator are two writers on one file.** Hand-resolved **three times** on 2026-07-13 (cmx-33, cmx-34, and again after). `WORKFLOW.md`'s done-criteria tell the agent to strike its own `- [ ] → - [x]` line **in its branch**, while the orchestrator keeps appending new items to `dev` behind it. Both edits land at the top of the same file, so the merge conflicts every single time — a guaranteed, self-inflicted tax on every dispatch, and it will get worse as the queue deepens. The tracker is also the *only* reason most PRs touch `TODO.md` at all. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Make the DISPATCHER own the strike — the agent must not touch `TODO.md`.** Remove the "strike the TODO line in your branch" step from `WORKFLOW.md`'s done-criteria (and the `{{task_file_relative}}` plumbing that supports it). Instead, the dispatcher marks the item `- [x]` **on `dev`** when it reconciles the run to `done` (the PR merged) — it already tracks that transition (`reconciled_done` in the tick counters). One writer, no conflict, and the checkbox now reflects *merged*, not *the agent believed it finished*, which is strictly more truthful. Match the task by its stable **task id** (the dispatcher already hashes it), not by fuzzy title text — titles get edited.
  2. **Decide the failure mode explicitly and document it.** If the dispatcher writes `dev` directly it must handle: a dirty/ahead `dev` (fetch+rebase or skip and retry next tick — do NOT force); a task whose line was edited or removed by a human meanwhile (skip, log, don't guess); and two runs reconciling in the same tick (batch into one commit). It must **never push to `dev` if the strike is the only change and it conflicts** — log and leave it for a human. A missed checkbox is cosmetic; a mangled `dev` is not.
  **Landmines:** don't regress `chela dispatch --dry-run`; don't let the dispatcher's `dev` write race the orchestrator's own commits (it runs under PM2, unattended — pull before write, never force-push, never touch anything but the one checkbox line); the tracker abstraction (`tracker.kind: markdown`) should stay pluggable — do not hardcode `TODO.md` paths into the dispatcher; an agent that still edits `TODO.md` out of habit must not break the merge (the strike being idempotent is fine). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `WORKFLOW.md` (done-criteria step 4 — the strike instruction + `{{task_file_relative}}`), `chela/dispatcher.py` (the reconcile path that sets a run `done` / `reconciled_done`, the markdown tracker + task-id hashing). **Verify:** unit-test that reconciling a merged run strikes the right line by task id on `dev`, that an already-struck line is a no-op, that a missing/edited line is skipped-and-logged rather than guessed, and that two runs reconciling together produce one commit. **Live (the whole point):** dispatch a task, merge its PR, and confirm (a) the PR's diff does **not** touch `TODO.md`, (b) the item is struck on `dev` afterwards, and (c) **the merge had no conflict** — while a *second* new item was added to `dev` during the run.

- [x] **The inbox reports a SUCCESSFUL agent as `DIED mid-task` — it infers death from "window gone" and cannot tell completion from a crash.** Fired **twice** on 2026-07-13, both times about work that had already succeeded and been merged: `📥 @6 (@6) DIED mid-task (window gone) — the work was not finished.` (CMX-32 → PR #45, merged) and the same for `@11` (CMX-33 → PR #46, merged). Each time the inbox **contradicted itself in its own queue** — it also queued the correct `… is awaiting review — <PR url>` event for the same run. A dispatched agent that finishes normally *exits*, and its tmux window goes away; the watcher reads that disappearance as death. So the one message a human is most likely to act on ("your agent died, the work is unfinished") is **false precisely when everything went right**. It also leaves a stale watch behind. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**

- [x] **Make the dispatcher's default agent launch mode editable in Settings (the FIRST editable setting — there is no write path yet).** Today the launch command is hard-wired in `WORKFLOW.md` (`agent.cmd: claude --permission-mode auto`, `chela/dispatcher.py` ~835), so changing how dispatched agents are launched means editing a YAML front-matter file by hand. Liav wants it editable from Settings. ⚠️ **Two constraints are load-bearing — read them before designing.** ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **🔒 EXPOSE A MODE ENUM, NOT A FREE-TEXT COMMAND.** `agent.cmd` is a shell command that gets executed to spawn an agent. The dashboard is served over the tailnet. **Making that string editable in a web UI is remote code execution by design** — do NOT add a text input for it. Expose only the **permission mode**, as a constrained enum, with the rest of the command line fixed and built in code. Enumerate the *actually valid* modes from the installed CLI (`claude --help`; do not guess or hardcode from memory — the set includes at least `default`, `acceptEdits`, `auto`, `bypassPermissions`, `plan`, but VERIFY) and reject anything not in that set **server-side**, not just in the UI. A rejected value must fail closed (keep the current mode), never fall through to a shell.
  2. **Settings is READ-ONLY today — build the write path.** `/api/settings` (`chela/dashboard/app.py` ~1437) is GET-only and there is no settings UI module in `chela/dashboard/static/js/` (the drawer is rendered elsewhere — find it). Add a persisted store (a small JSON under `CHELA_DIR`, mirroring how `~/.chela/inbox.json` / `telegram-bindings.json` are handled — **not** a new database) plus a write endpoint. Keep the store generic enough that the *next* editable setting doesn't need a second mechanism, but do not build a settings framework — one key, done well.
  3. **Define PRECEDENCE explicitly and document it in the code.** `WORKFLOW.md` currently sets `agent.cmd` outright, so a Settings value would be silently ignored. Proposed order (state it in a docstring): **explicit `agent.cmd` in WORKFLOW.md wins** (a per-workflow override stays authoritative) **→ else the Settings permission mode → else the built-in default**. That means `WORKFLOW.md`'s `cmd:` line should be **removed** (or reduced to no `--permission-mode` flag) so the Settings value is actually reachable — otherwise the feature is dead on arrival and the UI lies. Whichever precedence you choose, **prove it with a test** and make the UI show which source is currently winning.
  **Landmines:** the RCE point in item 1 is the whole reason this task is worded this way — a free-text `agent.cmd` field would be a serious vulnerability, do not add one "for flexibility"; a mode change must only affect **newly dispatched** agents (a running agent's mode is fixed at startup — do not try to mutate a live agent); don't regress `chela dispatch --dry-run`; the dispatcher is run by PM2 **from disk** (`chela-daemon`), so a stale/invalid settings file must not crash the daemon loop — read defensively, fail to the built-in default. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (`agent_cmd` ~828-835 + the comment block explaining the auto default), `WORKFLOW.md` (`agent:` block ~19), `chela/dashboard/app.py` (`/api/settings` ~1437), `chela/config.py` (`CHELA_DIR`), `chela/dashboard/static/js/` (find the Settings drawer — it is not its own module yet). **Verify:** unit-test that an unknown/malicious mode is rejected server-side and the default is kept; that precedence resolves as documented; that a changed setting persists across a daemon restart. **Live:** change the mode in Settings, dispatch a real task, and confirm from the spawned agent's actual command line (`ps`/pane) that it launched in the new mode — and that an already-running agent was untouched.

- [x] **`tests/test_inbox.py` reads the LIVE runs DB — 9 tests fail whenever a real agent is mid-flight.** Found 2026-07-13 while verifying CMX-32: the suite reported `9 failed, 482 passed` on a machine with one dispatched run in `awaiting_review`, and the SAME 9 fail on `dev` (so it is not CMX-32's doing). Root cause: `inbox.tick(prev, runs=None)` falls back to `from chela import dispatcher; runs = dispatcher.list_runs()` (`chela/inbox.py` ~341), and the tests call `inbox.tick({AGENT: BUSY})` **without passing `runs`** — so they query the **real** dispatcher runs database. The `store_file` fixture isolates the inbox JSON (`CHELA_INBOX_FILE`), the session statuses, and `send_tmux` — but **not** the runs DB. With a live run awaiting review, `run_events()` synthesizes a real `awaiting_review` event into the test's tmp store and every count assertion is off by one (`assert 2 == 1`). **These tests are GREEN BY LUCK: they pass only when the machine is idle and fail precisely when chela is being used** — the third instance of this repo's signature bug (cf. the C1 permission gate and the A2 mid-render double-relay, both green while broken live). ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Landmines + Verify.**
  1. **Isolate the runs DB in the fixture.** In `tests/test_inbox.py`, make the `store_file` fixture (or a new autouse fixture in that module) monkeypatch the live query so it returns nothing by default — e.g. `monkeypatch.setattr(dispatcher, "list_runs", lambda: [])` — so `tick()` can never reach the real database. Tests that WANT run events should pass them explicitly via `tick(prev, runs=[...])` (the parameter already exists — that is the seam; use it). Proven diagnosis: with `CHELA_DIR` pointed at an empty scratch dir, the same 9 tests pass (23/23 on `dev`, 491/491 on cmx-32) — but do NOT fix it by requiring an env var, fix it in the fixture so a bare `uv run pytest -q` is hermetic.
  2. **Then sweep for the same leak elsewhere.** Any other test that calls into `inbox`, `dispatcher`, or `agent_manager` without isolating the live state (`~/.chela/*.db`, `~/.chela/inbox.json`, `claude agents --json`, tmux) has the same failure mode. Grep for the live-state entry points and confirm each test either isolates them or passes them in. Add a short comment in the fixture recording WHY the isolation exists (a future refactor will otherwise "simplify" it away and restore the bug).
  **Landmines:** don't just delete or xfail the failing assertions — the assertions are RIGHT, the fixture is wrong; don't make hermeticity depend on the caller exporting `CHELA_DIR` (a bare `uv run pytest -q` on a busy machine must pass); don't weaken the real `tick()` fallback to `dispatcher.list_runs()` — that default is correct in production, it is only the TEST that must not reach it. Keep `uv run ruff check chela tests` green. **Reference:** `chela/inbox.py` (`tick` ~328-343, `run_events` ~263), `tests/test_inbox.py` (the `store_file` / `sends` / `windows` fixtures ~29-45), `chela/dispatcher.py` (`list_runs`). **Verify:** the decisive test is environmental, not a unit test — **dispatch a real run (or hand-insert a row so a run sits in `awaiting_review`) and confirm `uv run pytest -q` is fully green with it in flight**, then confirm it is still green with an idle machine. State both results in the PR. A suite that only passes on an idle machine has not been fixed.

- [x] **AskUserQuestion is UNANSWERABLE on a phone — the option text exists ONLY as a truncated button caption.** CONFIRMED live 2026-07-13 (screenshot): a 4-option question relayed as `❓ <question>` + four buttons that Telegram truncated to one line each — *"Rotate the group, skip the …"*, *"filter-repo + ask GitHub …"*, *"Fresh public repo,"*. The human got four half-sentences and **none** of the reasoning, on a decision that hinged entirely on the reasoning. Root cause is structural, not cosmetic: `gatewatch.format_askuq_message` deliberately puts **only the question** in the body ("the answer buttons carry the options"), and `interactive.scraped_reply_markup` renders each option label as a **button caption** — the one place in Telegram that hard-truncates to a single line. So the labels are simultaneously the only carrier of the options and the one surface that can't display them. This lands directly on the "decisions inbox = killer UX" thesis: the orchestrator asks the human to decide while structurally withholding the basis for the decision. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–3 + Landmines + Verify.**
  1. **Move the options into the message BODY, numbered, in full.** Rewrite `format_askuq_message(uq)` (`chela/telegram/gatewatch.py` ~172) to emit the question followed by a numbered list of the scraped option labels at **full length** — e.g. `❓ <question>` then `1. <label>` / `2. <label>` … The body is the only surface that wraps, so this is what actually fixes readability. Keep it **plain text** (the send path does not escape MarkdownV2 for scraped content).
  2. **Reduce the buttons to compact selectors.** In `interactive.scraped_reply_markup` (`chela/telegram/interactive.py` ~219), stop using the label as the caption. Emit compact numbered buttons (`1`, `2`, `3`, …) that fit a phone's width without truncation, packed a few per row rather than one full-width row each, keyed to the SAME `qa:<i>` callbacks in the SAME order. Keep the `⎋` escape row (`_esc_row`). The body now carries the meaning; the buttons are just the selector.
  3. **Investigate — do per-option descriptions exist in the pane at all?** The `description`/`preview` fields never reach the bridge (the `tool_use` record only lands in the JSONL at answer-time — the reason this path is pane-scraped in the first place). **Establish empirically** whether the Claude Code TUI renders a description for every option or only for the cursor-focused one: capture a live pane with a real multi-option AskUserQuestion (`tmux capture-pane -p`) and look. **If** every option's description is on-screen, scrape it into `AskUQ` (`chela/telegram/panescan.py` ~274, alongside `options`) and indent it under its option in the body. **If only the focused option's description renders, STOP — do not fake, infer, or synthesize descriptions.** Ship items 1–2 (which already fix the reported bug) and write what you found into the PR description. Labels-in-the-body is the fix; descriptions are a bonus that may be impossible.
  **Landmines:** the `qa:<i>` **index mapping is load-bearing** — a tap injects `i - cursor` Down/Up presses to move the caret (`chela/telegram/inbound.py` ~589), so button *i* MUST still mean option *i*, in display order, with no reordering, no filtering, and no gaps; don't regress the **multi/multi-tab fallback** (`uq.multi or not uq.options` → `nav_only_markup()` — unchanged); don't regress **edit-in-place** or **poof-on-resolve** (`_uq_msg`) — note the body text now changes as options render mid-scrape, which is exactly what edit-in-place is for; respect Telegram's **4096-char** message cap (truncate a pathological option, don't drop it); `_askuq_signature` must still change when the scrape changes. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (`format_askuq_message`, `_askuq_markup`, `_askuq_signature`), `chela/telegram/interactive.py` (`scraped_reply_markup`, `_truncate`, `_esc_row`, `QA_CB_PREFIX`), `chela/telegram/panescan.py` (`AskUQ`, `detect_askuserquestion`), `chela/telegram/inbound.py` (`_tapped_label` ~562, the `qa:` tap handler + cursor-relative injection ~589–637). **Verify:** unit-test that the body contains every option label **in full** and numbered; that the keyboard is compact selectors whose `qa:<i>` callbacks preserve option order 1:1; that the multi fallback is untouched; that an over-long body is truncated, not dropped. **LIVE (mandatory — green tests have lied twice on this exact substrate: the C1 permission bug and the A2 mid-render double-relay both passed unit tests while broken in production):** trigger a real AskUserQuestion with 4 long option labels from a bridged agent and confirm on an actual phone that (a) all four option texts are fully readable, and (b) tapping selector `3` really selects option 3 — not option 2 or 4.

- [x] **Dispatcher seed-delivery robustness — CONFIRM delivery via busy status, don't hope.** The dispatcher spawns an agent, waits for `❯`, then `send_tmux(seed)` — but a late boot-splash redraw (the `⚠ MCP servers need authentication` line landing *after* `❯`) can swallow the paste, leaving the agent idle with **no task** (stuck CMX-26 for 22 min). A fixed sleep is exactly what the code already tried and warns against (*"send-keys lands mid-splash and is silently dropped"*). Use the **busy status** to verify the seed landed. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Verify.**
  1. **Verify-at-spawn via busy status:** right after `send_tmux(target_id, prompt)` in `_spawn` (`chela/dispatcher.py` ~772), poll the agent's native session status — `chela.agent_manager.session_status_map(force=True)` keyed by `agent_manager.claude_pid(window)` (the `claude agents --json` authority `/api/agents` uses: `"busy"` while working, `"idle"` at the prompt). If the agent flips **idle → busy** within a short window (~8s, polling every ~1s), the seed landed — done. If it stays **idle** past the window, the paste was dropped → **re-send the seed** (retry 2–3×, short settle-sleep between). Confirms delivery instead of hoping a delay was enough.
  2. **Tie in the existing watchdog:** the dispatcher already re-nudges a `running` row stuck at an idle empty prompt (`_pane_idle_empty_prompt` + `idle_nudged_at` + the `watchdog_renudged` counter) — but only on a reconcile *tick*, so between ticks a dropped seed sits idle (why CMX-26 waited 22 min). The verify-at-spawn loop catches it in seconds; optionally switch that watchdog's idle check to the same busy-status signal for consistency.
  **Landmines:** a freshly-booted agent reads "idle" until the seed makes it busy, so check for the idle→**busy** transition within a timeout (NOT just "is idle now"); use `session_status_map(force=True)` for a fresh read (it's cached); the seed is a long multi-line paste (send_tmux's paste-buffer path — the re-send goes through the same); **cap retries** so a genuinely-broken agent fails cleanly (no infinite loop). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dispatcher.py` (`_spawn` seed send ~772, `_pane_idle_empty_prompt`, the reconcile watchdog + `idle_nudged_at`), `chela/agent_manager.py` (`session_status_map`, `claude_pid`, `liveness`), `chela/messenger.py` (`send_tmux`). **Verify:** unit-test the verify-resend loop (mock status: stays idle → re-sends up to the cap then gives up; flips busy → stops). Live: spawn an agent; if the seed is dropped it re-sends and the agent starts within seconds instead of sitting idle.

- [x] **Slice C2 — permission Allow/Deny buttons + FIX C1's detection gating (it never fires while a gate is pending) + A2/B2 UX polish.** LIVE-TESTED 2026-07-12: C1's permission relay is **effectively broken**. `detect_permission_gate` works (confirmed on a live `ls /tmp` gate → "Do you want to proceed? 1.Yes/2.Yes-allow/3.No"), but `gatewatch` reads the pane for permission **only when the transcript has an unpaired `tool_use`** (docstring lines 10-12, 190-191) — and the gated command's `tool_use`, exactly like AskUserQuestion, is **only written to the JSONL at approval-time**. So while the gate is pending there is NO unpaired tool_use → the pane is never read → the `❓ Permission` message never posts. (C1's unit tests passed because they mocked that precondition.) Same class of bug A2 fixed. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–3 + Landmines + Verify.**
  1. **Fix the gating — make permission detection UNGATED (pane-triggered), like A2/B2.** In `chela/telegram/gatewatch.py`, run `detect_permission_gate` on the per-tick pane capture for **every** bridged window (remove the "only when an unpaired `tool_use`" precondition — it's false during a live gate). Enrich the command from the **pane gate region** (`detect_permission_gate` already returns the "Bash command / <cmd>" text), NOT the transcript's unpaired `tool_use` (absent while pending). Keep edge-triggered/de-dup + edit-in-place + clear-on-resolve (gate leaves the pane, or its `tool_result` lands).
  2. **Add the Allow/Deny keyboard** (the risk scope chosen earlier): `✅ Allow once` → Enter (`qa:nav:ent`, the default-highlighted option 1 "Yes"), `❌ Deny` → Escape (`qa:nav:esc`), showing the actual command in the message. **No one-tap "allow always"** — the human can arrow to option 2 via nav if wanted. **Validate empirically** (like B2) that Enter approves option 1 and Esc cancels/denies.
  3. **Apply the A2/B2 UX polish, consistently across AskUserQuestion + ExitPlanMode + permission** (one shared helper): (a) **trim the nav row to just ⎋ Esc** when semantic buttons are present — there's no visible caret on Telegram, so ↑↓⏎🔄 are redundant (keep the full nav only in the no-semantic-buttons fallback); (b) **"poof" on resolve** — delete the gate/question/plan message once answered (the `tool_result` is the record), mirroring ccbot `handlers/interactive_ui.py::clear_interactive_msg`.
  **Landmines:** ungated pane-read for permission adds a detector to the per-tick capture (fine — fleet is small, one capture already happens); edit-in-place tolerate "not modified" + fall back to fresh post; poof deletes ONLY the tracked interactive message; don't regress A2 (AskUserQuestion), B2 (ExitPlanMode), C1 detection, or the `k:` keyboard; plain-text send. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (remove the permission gating; mirror the AskUQ/ExitPlan ungated pattern + `_uq_msg`/`_plan_msg` edit-in-place + clear), `panescan.py` (`detect_permission_gate` — the gate text has the command), `interactive.py` (`_nav_row`/`NAV_KEYS` to trim, `qa:nav` scheme), ccbot `src/ccbot/handlers/interactive_ui.py` (`clear_interactive_msg` = poof). **Verify:** unit-test that a permission gate is detected + relayed with **NO unpaired `tool_use` present** (the live-broken case — the core fix); the Allow/Deny keyboard; nav trimmed to Esc when semantic buttons present; poof deletes the message on resolve for all three prompt types. **Live:** trigger a Bash gate on a fresh agent → `❓ Permission — Bash: … [✅ Allow once] [❌ Deny]` appears **while pending**; Allow → runs, Deny → cancels; the message poofs after.

- [x] **Slice B2 — ExitPlanMode PANE-TRIGGERED (same fix A2 got for AskUserQuestion).** ExitPlanMode's approval buttons (Slice B / CMX-20) are still **transcript-triggered**, so — exactly like AskUserQuestion before A2 — the `ExitPlanMode` `tool_use` record only lands in the JSONL **at answer-time**, so the Approve/Keep-planning buttons show up **after the plan is already resolved**, useless. The plan-approval selector ("Would you like to proceed?" with Yes-auto / Yes-manual / No) is a **live pane** UI, so detect it from the **pane** and reuse A2's `panescan`/`gatewatch`/edit-in-place substrate. ⚠️ **BEFORE implementing, open `TODO.md` and read items 1–4 + Landmines + Verify. Mirror how A2 did AskUserQuestion — this is the ExitPlanMode analog.**
  1. **Extend `chela/telegram/panescan.py`** with ccbot's ExitPlanMode `UIPattern`s (`~/projects/ccbot/src/ccbot/terminal_parser.py:50-61`: top `^\s*Would you like to proceed\?` / `^\s*Claude has written up a plan`, bottom `^\s*ctrl-g to edit in ` / `^\s*Esc to (cancel|exit)`). Add `detect_exitplanmode(pane_text) -> ExitPlan | None` returning the **scraped plan region** (the plan text shown above the options) — used as the message body. Keep all regex in `panescan.py`.
  2. **Pane-trigger via `gatewatch`** alongside AskUserQuestion + permission (same per-tick `capture_pane`, ungated — the `tool_use` is post-answer). **Edit-in-place + de-dup** using the A2 machinery (track a per-window message id like `_uq_msg` → a sibling `_plan_msg`; clear when the selector leaves the pane / on the `ExitPlanMode` `tool_result`).
  3. **Message + buttons:** relay the scraped plan (plain text; if long, truncate with a "… (/screenshot for full)" note) + the approval keyboard. Reuse Slice B's option-count-independent mapping: **`✅ Approve (auto mode)` → `qa:nav:ent` (Enter)**, **`📝 Keep planning` → `qa:nav:esc` (Escape)**, plus the glyph-only nav row (so the human can arrow to "Yes, manually approve edits"). Build via the existing `_plan_rows` + `_nav_row` in `chela/telegram/interactive.py`. (Enter/Esc are option-count-independent, so **no cursor-relative injection needed here** — unlike AskUserQuestion.)
  4. **Suppress the transcript double-relay:** in `chela/telegram/relay.py`, drop the transcript-triggered `ExitPlanMode` `tool_use` keyboard the same way A2 dropped AskUserQuestion's (it now arrives post-answer); keep its `tool_result` as the "approved/kept" confirmation + a de-dup clear signal. Remove/blank the ExitPlanMode branch in `ask_reply_markup` (transcript path) so it's pane-only now.
  **Landmines:** edit-in-place tolerate "not modified" + fall back to fresh post; clear on resolve; don't double-relay (pane + transcript); don't regress A2 (AskUserQuestion), C1 (permission), or the `qa:`/`qa:nav:`/`k:` keyboards; plain-text send (no MarkdownV2 escaping of scraped plan). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (A2 is the template):** `chela/telegram/panescan.py` (`detect_askuserquestion`), `gatewatch.py` (AskUQ relay + `_uq_msg` edit-in-place + clear), `interactive.py` (`_plan_rows`, `scraped_reply_markup`, `_nav_row`), `relay.py` (AskUserQuestion `tool_use` suppression), ccbot `terminal_parser.py:50-61`. **Verify:** unit-test `detect_exitplanmode` on a sample plan-approval pane (region scraped; normal/permission/AskUQ panes → None), edit-in-place + clear-on-resolve, transcript ExitPlanMode `tool_use` no longer builds a keyboard. **Live:** trigger a plan approval on a fresh bridged agent → the plan + Approve/Keep-planning buttons appear **while pending**; tapping Approve proceeds, Keep-planning stays in plan mode.

- [x] **Render markdown tables as a card-style list (port ccbot `convert_markdown_tables`).** Telegram has no table rendering. chela passes tables straight to `telegramify-markdown`, which doesn't produce the readable card-style list ccbot does (Liav: "ccbot renders the table as a list") — ccbot **pre-processes** tables before telegramify; chela's markdown port (CMX-18) skipped that. ⚠️ **BEFORE implementing, open `TODO.md`, read items 1–2 + Verify.**
  1. **Port `convert_markdown_tables`** from ccbot `~/projects/ccbot/src/ccbot/markdown_v2.py` (+ its `_split_table_row` helper) into chela (`chela/telegram/format.py` or a small helper). It turns each markdown table into card-style `**Header**: value` pairs separated by horizontal rules (how Claude Code renders tables in a narrow terminal), and **skips tables inside code blocks**.
  2. **Hook it into the body path:** in `to_markdown_v2` (`chela/telegram/format.py`), run the assistant/text **body** through `convert_markdown_tables` **before** `telegramify-markdown`, so telegramify renders the card text as normal markdown instead of a raw table. Keep the existing fallback: if telegramify is absent, still apply `convert_markdown_tables` then the blind escape. Don't touch the emoji-header escaping or `to_code_block`.
  **Landmines:** skip code blocks (don't convert `|`-lines inside a ``` fence); a malformed/no-body table must pass through gracefully (no crash); keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** ccbot `src/ccbot/markdown_v2.py` (`convert_markdown_tables`, `_split_table_row`), `chela/telegram/format.py` (`to_markdown_v2` + the telegramify path). **Verify:** unit-test a markdown table → card-style `**Header**: value` output (not raw pipes); a table inside a code fence is left untouched; a normal paragraph unchanged. **Live:** an agent message containing a table renders as a readable list on Telegram.

- [x] **BUGFIX — long single-line Telegram messages STRAND on the agent's input row instead of submitting ("hit send → break row").** CONFIRMED live 2026-07-12: `chela/messenger.py::send_tmux` single-line `else` branch (~92-96) does `tmux send-keys -t <t> <text> Enter` in **one** call. Claude Code's TUI treats a long injected blob as fast/paste input and the rapid trailing Enter is absorbed as a **newline**, so the message sits wrapped on the `❯` input line, unsubmitted. Reproduced with a 262-char message (a SHORT message submits fine — only long ones strand). ⚠️ **BEFORE implementing, open `TODO.md` and read items 1–2 + Verify.**
  1. **Fix (empirically proven):** in the single-line `else` branch, send the **text and Enter as SEPARATE tmux calls with a ~0.5s gap** — mirroring ccbot (`~/projects/ccbot/src/ccbot/tmux_manager.py:420-424`: "a 500ms gap lets the TUI process the text before receiving Enter") and chela's OWN multi-line path (paste-buffer → sleep → separate Enter, which works). Send the text with `tmux send-keys -l <text>` (**literal**) so a message containing a tmux key-name (e.g. "Up", "Enter", "C-c") isn't misinterpreted as a keypress — the current combined call omits `-l`. **Verified:** the same 262-char message submitted cleanly as `-l <text>` → `sleep 0.5` → `Enter`.
  2. **Keep robustness:** leave the multi-line paste-buffer path + its `_pane_has_unsubmitted_paste` 2nd-Enter chip-guard intact (that's the collapsed-`[Pasted text]` case; the single-line strand shows inline-wrapped text with NO chip, so that guard doesn't apply — the gap fix is what submits it). Don't change the slash-command Escape-first behavior.
  **Landmines:** `-l` for literal text (don't reintroduce key-name misinterpretation); keep the ~0.5s gap (a rapid Enter IS the bug); don't regress the multi-line/paste path or slash-commands. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/messenger.py` (`send_tmux` — the multi-line path is the template), ccbot `src/ccbot/tmux_manager.py:420-456`. **Verify:** unit-test (mock subprocess) that single-line `send_tmux` issues literal text (`-l`, no Enter) then a SEPARATE Enter (two calls, with the gap); multi-line still uses paste-buffer; slash-command still sends Escape first. Report a live check: a long single-line message submits (doesn't strand).

- [x] **A2 polish — AskUserQuestion double-relays (partial→full) + nav-row label truncation.** From a live screenshot: (a) the question posted **TWICE** — first with only the first option ("Mango"), then again complete ("Mango"/"Peach"/"Kiwi") — because the pane watcher scraped the selector **mid-render** (only option 1 had drawn), relayed it, then re-scraped the full selector (different content signature) and relayed AGAIN; (b) the **nav row** crams 5 buttons (↑ ↓ ⏱Esc ⏎Enter 🔄) into one row so "⏎ Enter" truncates to "⏎ E…" on mobile (the semantic option buttons render full/fine, one per row — leave them). ⚠️ **BEFORE implementing, open `TODO.md` and read items 1–2 + Verify.**
  1. **Fix the double-relay via EDIT-IN-PLACE (ccbot pattern):** when a detected AskUserQuestion's scraped content CHANGES for the same window (more options render / cursor moves), **edit the existing topic message** (text + `reply_markup`) instead of posting a new one. Track the message_id per window (mirror ccbot `src/ccbot/handlers/interactive_ui.py` `_interactive_msgs` + `edit_message_text`, tolerate "Message is not modified"). This collapses the mid-render partial + the full into ONE message that updates as the selector settles. **Clear** the tracked message_id when the selector leaves the pane (answered). Supersedes the current "new message per changed content signature" in `gatewatch`.
  2. **Reflow the nav row so labels fit:** shorten to glyph-only (e.g. `↑ ↓ ⎋ ⏎ 🔄`, drop the words) or split across two rows, so nothing truncates on a phone. Keep the same `qa:nav:*` callbacks/actions (`NAV_KEYS`).
  **Landmines:** edit-in-place must tolerate "Message is not modified" (no-op) and fall back to a fresh message if the edit fails (message deleted); don't double-post; clear tracking on resolve; don't regress the option buttons, the permission-gate relay, or `qa:nav` behavior. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/gatewatch.py` (the AskUQ relay + de-dup marker), `chela/telegram/interactive.py` (`scraped_reply_markup`, `_nav_row`, `NAV_KEYS`), ccbot `src/ccbot/handlers/interactive_ui.py` (edit-in-place message tracking). **Verify:** unit-test that a changed scrape for the same window EDITS (not re-posts) and clears on resolve; the nav row fits (glyph-only or 2 rows); permission-gate path unaffected. **Live:** trigger a single-select question, confirm ONE message that fills in as options render + no truncated nav labels.

- [x] **Inbound image + file sharing (Telegram → agent) — port ccbot's media handling.** Today the bridge only registers `MessageHandler(filters.TEXT, _on_message)` (`chela/telegram/inbound.py:541`), so a **photo or document pasted into a topic is silently ignored** (Liav: "can't paste a screenshot from mobile"). Port ccbot's approach so a shared image/file reaches the agent. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify.** **Reference (port from):** ccbot `~/projects/ccbot/src/ccbot/handlers/document.py` (`document_handler` — download to a dir + forward the path; 20 MB `getFile` cap handling) and `src/ccbot/handlers/message_sender.py::send_photo`.
  1. **Photo handler:** register `MessageHandler(filters.PHOTO, _on_photo)` in `build_application` (`chela/telegram/inbound.py`, **before** the `filters.TEXT` catch-all). On a photo in a bound topic: resolve the window from the message's **own topic** via `router.resolve(chat_id, thread_id)` (chat-gate fail-closed — same as `_on_message`), pick the **largest** `PhotoSize`, `await photo.get_file()` → `download_to_drive()` into `CHELA_DIR/documents/` (create it; use a unique name, `.jpg`), then `messenger.send_tmux(window_id, <abs_path>)` so Claude Code can Read the image. Prefix a short line, e.g. `📎 image: <path>` (a bare path also works — Claude Code reads images by path).
  2. **Document handler:** `MessageHandler(filters.Document.ALL, _on_document)` — same flow; preserve the original filename where possible; download to `CHELA_DIR/documents/`, forward the path. **Reject > 20 MB** before downloading (Telegram caps bot `getFile` at 20 MB) with a friendly reply, mirroring ccbot.
  3. **Wire it:** `CHELA_DIR` is `~/.chela` (`chela/config.py`). Both handlers reuse the existing `router` + `messenger.send_tmux` that `_on_message` uses. Keep handler ORDER correct: PHOTO and Document handlers before `filters.TEXT` (PTB runs one handler per group).
  4. **Scope:** inbound photos + documents only. **Voice transcription is OUT** (ccbot's `transcribe.py` pulls faster-whisper — defer). Outbound agent→Telegram images already exist via `/screenshot`.
  **Landmines:** resolve window from the topic, never trust a payload; chat-gate fail-closed; create `CHELA_DIR/documents/` if missing; unique filenames (don't clobber); reject > 20 MB before download; a caption on the photo/doc (Telegram sends caption separately) should also reach the agent if present. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Verify:** unit-test the handlers with a fake `update`/`bot` (largest PhotoSize chosen; download path under `CHELA_DIR/documents/`; `send_tmux` called with the path + window resolved from topic; oversized document rejected without download; non-bound topic gated out). Note that real photo/doc round-trip needs a live check (report it as manual).

- [x] **Interactive UI Slice A2 — AskUserQuestion PANE-TRIGGERED (fixes "buttons show the selection after it's already answered").** ROOT CAUSE settled 2026-07-12 by hard measurement: Claude Code writes the `AskUserQuestion` `tool_use` record to the JSONL **only when the question is ANSWERED** (measured live: 0 records while the selector was on the pane pending → 1 the instant it was answered; embedded `timestamp` says question-time but the FILE APPEND is at answer-time). So Slice A's transcript-triggered relay can **never** show the buttons while the question is answerable — the record isn't there yet. The **pane** shows the selector live, so detection must come from the **pane** (exactly like C1's permission gates). This EXTENDS C1's `panescan`/`gatewatch` — not a rebuild. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–5 + Landmines + Verify beneath this line.**
  1. **Extend `chela/telegram/panescan.py` with the AskUserQuestion selector.** Port ccbot's AskUserQuestion `UIPattern`s (`~/projects/ccbot/src/ccbot/terminal_parser.py`: single-tab `^\s*[☐✔☒]` + bottom `^\s*Enter to select`; multi-tab `^\s*←\s+[☐✔☒]`). Add `detect_askuserquestion(pane_text) -> AskUQ | None` that returns the **question text**, the **ordered option labels** (parse lines like `❯ 1. Apple` / `  2. Banana` — strip the cursor/number/description; ignore the trailing `Type something.` / `Chat about this` meta-rows), and the **current `❯` cursor index** among the options. Keep ALL regex in `panescan.py` (the signatures table). **MVP = single question, single-select:** if it's multi-tab (multiple questions) or multi-select, relay the scraped **question text + the nav-row only** (no semantic option buttons) — never a broken keyboard.
  2. **Pane-trigger it — WITHOUT the unpaired-tool_use gate.** C1's `gatewatch` reads a pane only when the transcript has an **unpaired `tool_use`** — but a pending AskUserQuestion has **nothing in the transcript yet**, so that gate would never fire. So AskUserQuestion detection must `capture_pane` bridged windows **directly** (run `detect_askuserquestion` on the pane each tick). Since we're already capturing the pane, run it alongside `detect_permission_gate` in the same `gatewatch` poll (one `capture_pane` per window, two detectors). The fleet is small; note the per-tick pane-read cost. **Edge-triggered + de-dup per window** (like the permission gate): relay ONE message per question; clear the marker when the selector disappears from the pane (answered).
  3. **Message + buttons from the SCRAPED question** (transcript is not available): one `qa:<i>` button per scraped option (reuse Slice A's callback scheme + `_on_qa`, `chela/telegram/interactive.py` / `inbound.py`), plus the nav-row. Text is the scraped question.
  4. **Cursor-relative injection (re-read at tap time).** On a `qa:<i>` tap, `_on_qa` must `capture_pane` the target window, run `detect_askuserquestion` to get the **current** `❯` index + options, then send `Down`×(target − current) + `Enter` (NOT blind `Down×i` — the cursor may have moved). Add the cursor-relative variant; keep Slice B's `qa:nav` Enter/Esc working.
  5. **Formatting + no double-relay.** Build the message ourselves; send **plain text** (no MarkdownV2 escaping of scraped text — same choice C1 made for gates; cf. https://core.telegram.org/bots/api#formatting-options). And **suppress Slice A's transcript-triggered AskUserQuestion keyboard** (`ask_reply_markup` on the transcript `tool_use`) — that record now arrives post-answer, so attaching buttons there is useless and would double-post; drop that message (or keep it as a plain "answered: X" confirmation). Use the transcript `tool_result` (which DOES land, at answer) as a belt-and-suspenders signal to clear the pane de-dup marker.
  **Landmines:** edge-triggered/de-dup (one msg per question; clear when the selector leaves the pane); cursor-relative injection (read `❯`, never assume 0); plain-text send; NO double-relay (pane + transcript); don't regress C1 permission detection, Slice B ExitPlanMode buttons, or the `k:`/`qa:` keyboards. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/telegram/panescan.py` + `gatewatch.py` (C1 — the substrate to extend), `chela/main.py` `_outbound_loop` (wiring), `chela/telegram/interactive.py` (`ask_reply_markup`, `select_keystrokes`, `qa:` scheme) + `inbound.py` (`_on_qa`), `chela/messenger.py` (`capture_pane`, `send_key`), ccbot `terminal_parser.py` (AskUserQuestion `UIPattern`s). **Verify:** unit-test `detect_askuserquestion` on sample selector panes (question + ordered options + cursor index; multi-tab/multi-select → the fallback), and a normal pane → None; edge-trigger/de-dup; cursor-relative keystroke calc (current=1, target=0 → one `Up`… or `Down`×… as implemented); plain-text formatting; no-double-relay. **Live (report):** spawn a FRESH bridged agent, trigger a single-select AskUserQuestion, confirm the question + option buttons appear on the topic **while it is still pending**, and that tapping an option selects the right one end-to-end.

- [x] **BUGFIX — monitor skips a freshly-appeared / rotated transcript (blocked agents show NOTHING on Telegram, or only the post-answer selection).** ROOT-CAUSED live 2026-07-12: `chela/telegram/monitor.py::_poll_window` (~106-112) seeks to **EOF** on first-sight of a window's transcript to skip old backlog — but that also skips records **already in the file when it first APPEARS** (a fresh agent's first turn) or **ROTATES** (a `/clear` → new session file). Effect: an agent blocked on an **AskUserQuestion / permission prompt** relays **nothing** (fresh window: its whole first turn, question included, is skipped while still pending) or **only the post-answer `tool_result`** (rotation caught between the question's `tool_use` and its `tool_result`) → "the buttons show the selection after it's already answered." This defeats the A/B interactive keyboards for exactly these cases (the keyboards are fine; the message never reaches them). ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–3 + Landmines + Verify beneath this line.**
  1. **The fix — EOF-skip ONLY on cold-start attach to PRE-EXISTING history.** A transcript that **appears or rotates while the monitor is running** must be read from **offset 0** (its content is all new — the agent produced it this session). In `_poll_window`: when `path` is None/absent, remember it (`self._seen_empty: set[str]`, init in `__init__`). When a transcript first resolves for a window that was **seen empty** (`window_id in self._seen_empty`), OR the path **rotates** (`tracked is not None and tracked.path != path`), start at **0** — not `path.stat().st_size`. Keep the EOF seek ONLY for the genuine cold-start case: `tracked is None`, window **never** seen empty, transcript **already existed** on the very first poll (binding an agent that has pre-existing history — must NOT be replayed). `discard(window_id)` from `_seen_empty` once it has a file.
  2. **Guard against replaying a large backlog.** From-0 is safe for a fresh/`/clear` file (starts empty). But if an appeared/rotated file is already **large** at first sight (e.g. a `--resume` that repopulated history into a new file), from-0 would dump backlog — add a size/record guard: if the newly-resolved file already exceeds a sane threshold at first sight, fall back to EOF (and `log`). A fresh first-turn file is small; pick the threshold conservatively.
  3. **Don't regress.** Keep `_read_new`'s partial-write / truncation (`offset > size` → reset) handling; keep the cold-start "skip pre-existing history" behavior for agents bound WITH an existing transcript (binding an existing agent must NOT replay its whole history); `start_at_eof=False` (tests/backfill) still means from-0 always.
  **Landmines:** the whole fix is one distinction — *"did we poll this window and find NO transcript before it got one?"* (→ fresh, from 0) vs *"transcript already there on the first poll"* (→ backlog, EOF). Get it wrong and you either miss fresh turns (the current bug) or replay months of history (spam). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference / repro (confirmed):** bind a window before its transcript exists, then have its first turn contain an AskUserQuestion → today the topic stays empty though the `"name":"AskUserQuestion"` record IS in the JSONL. Files: `chela/telegram/monitor.py` (`_poll_window`, `_Tracked`, `_read_new`, the `start_at_eof` docstring), `chela/telegram/parser.py` (emits the events). **Verify (unit, fake resolver + tmp files):** (a) poll a window whose resolver returns None, then returns a file WITH content → the content **IS** emitted (was skipped — the core fix); (b) cold-start: first poll resolves a pre-existing file with content → **NOT** emitted (EOF); (c) rotation: tracked to file A, resolver switches to file B with content → B emitted from 0; (d) the large-backlog guard falls back to EOF; (e) truncation-reset still works. Report before/after on the repro.

- [x] **Interactive UI Slice C1 — permission-gate DETECTION + enriched relay (pane-scrape; NO buttons yet).** Unlike AskUserQuestion/ExitPlanMode, a tool/permission gate ("Do you want to proceed?" for a Bash/Edit) is **NOT in the transcript** — only the live pane shows it. This slice adds the bridge's **first pane read** so a blocked agent is surfaced to its topic as `❓ Permission — Bash: rm -rf build/`. **Buttons come in C2** — this is detection + enriched relay + the poll loop + de-dup only. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify beneath this line — they carry the load-bearing constraints (two-channel correlation, gated polling, edge-trigger, the one-module TUI-regex rule). Do not implement from this summary line alone.**
  1. **New pane detector (PORT from ccbot; isolate ALL TUI regex here):** create a small pure module (e.g. `chela/telegram/panescan.py`) — port ONLY ccbot's PermissionPrompt + BashApproval `UIPattern`s (`~/projects/ccbot/src/ccbot/terminal_parser.py:74-99`) + its `extract_interactive_content` helper. Expose `detect_permission_gate(pane_text) -> Gate | None` returning the gate's region text + a type tag. Keep **every** Claude-Code-TUI regex in this one module (the "signatures table") so a Claude Code version bump is a one-file edit. Do NOT port the whole `terminal_parser` — just the permission/bash patterns + the extractor.
  2. **Gated pane-poll in the EXISTING loop (no new thread):** in the bridge's outbound cycle (`chela/main.py` `_outbound_loop` ~354 → `monitor.poll`), for each bridged window **gate on the transcript**: only when the window's latest `tool_use` is **unpaired** (no `tool_result` yet — the parser already tracks this in its `pending` map, `chela/telegram/parser.py`) do a `messenger.capture_pane(wid)` (`chela/messenger.py:~103`) and run `detect_permission_gate`. This correlates the two channels (transcript says a call is pending → only then read the pane) and keeps pane-capture cheap — never blind-scrape every window every tick.
  3. **Enriched, EDGE-TRIGGERED relay:** on a NEWLY-detected gate for a window (not already relayed), post ONE message to the window's topic: `❓ Permission — <tool>: <command/args>`, where **tool name + args come from the transcript's unpaired `tool_use`** (Bash `command`, Edit `file_path`, etc.) — that's the two-channel win (you see the real command, not blind pane text). If the transcript identity is unavailable, fall back to the scraped gate-region text. **Edge-triggered:** track the relayed gate per window; **clear** it when the `tool_result` appears OR the pane no longer shows a gate — so a still-open gate is not re-posted every ~2s.
  4. **NO keyboard in this slice.** The `✅ Allow once` / `❌ Deny` + nav-row keyboard is **Slice C2** (reuses the `qa:nav` Enter/Esc plumbing). Keep C1 to detection + enriched relay + poll + de-dup so the new pane-scrape mechanism is reviewed on its own.
  **Landmines:** edge-triggered (ONE message per gate, never per poll); gate the pane-capture on the transcript's unpaired `tool_use` (don't blind-scrape); isolate ALL TUI regex in the one detector module; correlate identity from the transcript, fall back to pane text; don't regress the transcript relay or the A/B (`qa:`) + `/screenshot` (`k:`) keyboards. Attribution: the ccbot port is covered by the top-level `NOTICE` (`chela/telegram/` adapted from ccbot). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference — ccbot:** `src/ccbot/terminal_parser.py` (PermissionPrompt/BashApproval `UIPattern`s:74-99, `extract_interactive_content`), `src/ccbot/handlers/status_polling.py` (its "detect permission UI not in JSONL" poll — model, don't copy wholesale). **chela:** `chela/messenger.py` (`capture_pane`), `chela/telegram/monitor.py` (poll loop) + `parser.py` (tool_use↔tool_result pairing / `pending` unpaired map), `chela/main.py` (`_outbound_loop`), `chela/telegram/relay.py` (posting to a topic). **Design principle:** transcript = identity, pane = liveness, correlated; gated polling; one signatures module (this is the PTY-native answer to ACP). **Verify:** unit-test `detect_permission_gate` on sample permission/bash-approval pane snippets (→ detected + type) and a normal pane (→ None); unit-test the edge-trigger/de-dup (same gate not re-relayed; cleared on resolve; a new gate relayed); unit-test the transcript-identity extraction (unpaired `tool_use` → tool + args). Live: trigger a real Bash permission prompt in a test agent and confirm the bridge relays EXACTLY ONE enriched message naming the command. **Report the exact pane snippets you keyed on** (for the version-canary later).

- [x] **Interactive UI Slice B — ExitPlanMode plan-approval buttons (reuses Slice A plumbing).** When an agent calls `ExitPlanMode`, the bridge already relays the plan text to its topic (`ExitPlanMode` ∈ `INTERACTIVE_TOOL_NAMES`, `chela/telegram/relay.py:47`). Attach an **inline keyboard** so the human can approve / keep-planning from the phone. Detection = transcript (structured `tool_use`; plan in `input.plan`). ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read the numbered items 1–6 + Landmines + Verify beneath this line — they carry the load-bearing constraints (the KEY difference from Slice A, the option-count-independent keystrokes, the VALIDATE-empirically step, and what to report). Do not implement from this summary line alone.**
  1. **KEY difference from Slice A (read first):** unlike AskUserQuestion, ExitPlanMode's approval CHOICES are **NOT in the transcript** — `input` carries only `plan`, not the "Yes / Yes-auto / No, keep planning" options (those are harness-rendered TUI). So do **NOT** enumerate choices from the transcript, and do **NOT** introduce general pane-scraping (that's deferred to Slice C). Use **option-count-independent** keystrokes.
  2. **Keyboard (two semantic buttons + the always-present nav row):** `✅ Approve plan` → sends **`Enter`** (accept the default-highlighted proceed option); `📝 Keep planning` → sends **`Escape`** (dismiss the plan-exit, stay in plan mode). Both are single keystrokes, independent of how many Yes-variants exist. **Reuse Slice A's existing `qa:nav:*` plumbing:** `Approve` = callback `qa:nav:ent`, `Keep planning` = `qa:nav:esc` — both already route through `_on_qa` → `NAV_ACTIONS` → `send_key`, so **no inbound handler change is needed**. Keep the full nav-row (`_nav_row()`) too, so the human can arrow to a *specific* variant (e.g. "Yes, manually approve edits") when the default isn't what they want.
  3. **Where to build it:** extend `ask_reply_markup(msg)` in `chela/telegram/interactive.py:119` to return the ExitPlanMode keyboard when `msg.tool_name == "ExitPlanMode"` (dispatch on `tool_name`; the AskUserQuestion path stays unchanged). The relay already calls `ask_reply_markup(msg)` at both attach sites (`relay.py:163,206`) and threads `reply_markup` through `BotSender.send` — so **no relay change is needed** beyond what A shipped. Rename to `interactive_reply_markup` only if it reads cleaner (then update the 2 call sites + import); otherwise just branch inside.
  4. **VALIDATE EMPIRICALLY (load-bearing):** trigger a real ExitPlanMode in a live test agent (present a plan), `capture-pane`, and confirm: (a) **`Enter` proceeds** — and **REPORT which option it selects** (does the default silently enable *auto-accept edits*, or is it manual-approve?); (b) **`Escape` keeps planning** — stays in plan mode, does NOT cancel the whole task. If `Enter`'s default is auto-accept-edits (a real behaviour change the user may not want), either **label the button accurately** (e.g. `✅ Approve (auto-accept edits)`) or make the **nav-row primary** — report which you shipped and the exact behaviour of Enter/Esc.
  5. **Callback / handler:** reusing `qa:nav:ent`/`qa:nav:esc` (recommended) means **nothing new in `inbound.py`**. If instead you add a dedicated `pa:` (plan-approve) scheme for nicer toasts, mirror `_on_qa` (`inbound.py:453`): resolve the window from the callback's **own topic** via `router.resolve` (never trust a window id off the wire — CMX-8), chat-gate fail-closed, `send_key`, `query.answer(<label>)`, register its `CallbackQueryHandler(pattern=…)` **before** the catch-all, and ensure `^qa:` and the new pattern don't overlap.
  6. **Keep it additive:** the ExitPlanMode keyboard attaches the same additive way as A's — if building it fails, still relay the plan text (never crash the relay). The AskUserQuestion (`qa:<i>`) and `/screenshot` (`k:`) keyboards must be untouched and keep working.
  **Landmines:** don't regress Slice A (AskUserQuestion buttons) or the `k:` screenshot keyboard — both must coexist. No general pane-scraping (defer to C). callback_data ≤64 bytes. Never crash the relay. Attribution already covered by the top-level `NOTICE` (ccbot). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference — chela (Slice A, now on `dev`):** `chela/telegram/interactive.py` (`ask_reply_markup:119`, `_nav_row:85`, `NAV_KEYS:51`, `NAV_ACTIONS:61`, `select_keystrokes:68`, `decode_callback:144`, `QA_NAV_PREFIX`), `relay.py:47,163,206`, `inbound.py:453,514`, `parser.py` (`Message.tool_name`/`tool_input`). **ccbot** (context only — do NOT port the scraper): `src/ccbot/terminal_parser.py` ExitPlanMode patterns. **Verify:** unit-test (a) an ExitPlanMode `tool_use` message → keyboard with Approve(`qa:nav:ent`)/Keep-planning(`qa:nav:esc`) + nav-row; (b) AskUserQuestion still returns its `qa:<i>` semantic buttons (no regression); (c) a plain/text message → no keyboard. Plus the live validation; report Enter/Esc behaviour in the PR.

- [x] **Interactive UI Slice A — AskUserQuestion inline-keyboard answering (single-select MVP).** When an agent calls `AskUserQuestion`, the bridge already relays the question text to its Telegram topic (it's special-cased in `INTERACTIVE_TOOL_NAMES`, `chela/telegram/relay.py:45`). Add an **inline keyboard** so the human can answer from the phone instead of typing the answer back. **Detection = transcript (structured), NOT pane-scrape.** The `AskUserQuestion` `tool_use` block carries `input.questions[]` with `{question, header, multiSelect, options:[{label, description, preview}]}` — parse this where the relay builds the message. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read the FULL spec in the numbered items 1–6 + Landmines + Verify directly beneath this line — they carry the load-bearing constraints (MVP boundary, the VALIDATE-keystrokes-empirically-first step, the resolve-window-from-topic security rule, and the ccbot files to port from). Do not implement from this summary line alone.**
  1. **MVP scope (HARD boundary):** handle ONLY a **single question** with **`multiSelect == false`** and **no "Other"/free-text**. If the tool_use has `len(questions) > 1`, OR `multiSelect == true`, OR a free-text option — do NOT attach semantic option buttons; attach only the navigation-fallback row (below) so the human drives manually. Never present a broken keyboard.
  2. **Semantic option buttons (primary UX):** one button per `options[i].label` (truncate to Telegram's button-text limit), `callback_data = f"qa:{i}"` — **index only; NEVER put the label in callback_data (64-byte limit).** Tapping option *i* selects it.
  3. **Navigation-fallback row (PORT from ccbot, don't reinvent):** mirror ccbot's `_build_interactive_keyboard` (`~/projects/ccbot/src/ccbot/handlers/interactive_ui.py:81`) — a compact `↑ / ↓ / ⎋ Esc / ⏎ Enter / 🔄` row driving chela's existing `messenger.send_key` (the same primitive the `/screenshot` control-key keyboard already uses, `chela/messenger.py:121`). Always present, so the human can navigate manually if a semantic pick misfires or in the fallback cases above.
  4. **Answer-injection contract — VALIDATE EMPIRICALLY FIRST (load-bearing unknown):** Claude Code's AskUserQuestion selector highlights option 0 on render; sending `Down`×i then `Enter` should select option i and (single question) submit. **Before wiring the semantic buttons, trigger a real single-select AskUserQuestion in a live test agent, `capture-pane`, and confirm `Down`×i + `Enter` lands on and submits option i** (watch for cursor-start offset or a leading header/"Other" row). If unreliable, make the **navigation keyboard primary** and downgrade the semantic buttons — **report which you shipped and the exact keystroke sequence that worked** (Slice B / ExitPlanMode reuses it).
  5. **Callback handler (mirror `_on_key`, `chela/telegram/inbound.py:371`):** add a `CallbackQueryHandler` (or branch inside `_on_key`, which early-returns on non-`k:` data at `inbound.py:385`). On `qa:{i}`: **resolve the window from the callback message's OWN topic via `router.resolve(chat_id, thread_id)` — NEVER trust a window id from the payload** (CMX-8 isolation boundary), chat-gate fail-closed, fire the keystrokes via `send_key`, and `query.answer(<label>)` for instant feedback. Register it before the catch-all `MessageHandler` (`inbound.py:434`).
  6. **Wiring:** pass the injection fn (index → `send_key` sequence) into `build_application` the way `send_key`/`send_escape`/`capture` already are (`chela/telegram/inbound.py:200`, `chela/main.py:533`). Attach the keyboard to the AskUserQuestion message emitted in `RegistryRelay.on_message` (`chela/telegram/relay.py:182`); `BotSender.send` (`relay.py:112`) must accept an optional `reply_markup` — add it, keep the existing MarkdownV2→plain fallback intact.
  **Landmines:** keyboard is ADDITIVE — if building it fails, still relay the text (never crash the relay). callback_data ≤64 bytes, index only. Resolve window from topic, not payload. Don't regress the `k:` screenshot keyboard (`_on_key` must still handle `k:`; the new handler ignores non-`qa:` data). Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference (port mechanics): ccbot** `src/ccbot/handlers/interactive_ui.py` (`_build_interactive_keyboard`, edit-in-place tracking), `src/ccbot/handlers/callback_data.py` (`CB_ASK_*` keystroke callbacks), `src/ccbot/bot.py` (callback_handler round-trip). **chela:** `relay.py:45,112,182`, `inbound.py:200,273,371,427-450`, `messenger.py:121`, `bindings.py:96,103`, `main.py:492,533`, `parser.py:101-170`. **Verify:** unit-test (a) single-question single-select → reply_markup with one `qa:{i}` button per option; (b) multiSelect==true OR >1 question OR Other → NO semantic buttons (nav-only); (c) callback for `qa:2` resolves window from topic + fires index-2 injection, ignores non-`qa:` data. Plus the live validation; report the working keystroke sequence in the PR.

- [x] **Render assistant markdown properly in Telegram (add `telegramify-markdown`).** `chela/telegram/format.py::to_markdown_v2` currently **blind-escapes** the body via `escape_markdown_v2` (every MarkdownV2 special char backslashed), so Claude's markdown renders LITERALLY on Telegram — ` ``` code blocks ``` ` show as escaped backticks, `*bold*` shows the asterisks, tables show raw pipes. Port ccbot's approach (it deliberately was NOT ported to stay dep-free — now we want the fidelity):
  1. Add **`telegramify-markdown`** to the `[telegram]` extra in `pyproject.toml` (what ccbot used; pulls `mistletoe`).
  2. In `to_markdown_v2`, render the **assistant/text body** through `telegramify-markdown` → valid MarkdownV2 (real code-block + bold + inline-code entities), replacing the blind `escape_markdown_v2` for that body. Optionally port ccbot's `convert_markdown_tables` (tables → card-style key/value, since Telegram has no tables) — `src/ccbot/markdown_v2.py`.
  3. **Keep the fallback:** import `telegramify-markdown` **lazily**; if it's absent (import fails) or raises on a message, fall back to the current `escape_markdown_v2` blind-escape so the core still works without the extra. The relay's existing MarkdownV2→plain-text-on-reject path stays as the outer safety net.
  4. The emoji **header** (`🔧`/`✅`/`🤖` + tool/role) must remain valid MarkdownV2 — escape it or keep it outside the converted body; don't double-escape. **Leave `to_code_block`** (used by `/screenshot`'s text fallback) untouched.
  **Don't touch** routing / registry / lifecycle / the relay's send+fallback contract. **Landmines:** don't double-escape the header; a telegramify failure on ONE message must degrade to escaped-plain for that message, never crash the relay; keep it lazy so pure format tests don't need the extra. **Verify:** unit-test that an assistant message with a fenced code block + `*bold*` renders MarkdownV2 with a real code entity (backticks NOT backslash-escaped) when telegramify is available, and that the fallback path (telegramify absent/raising) still produces safe escaped output. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. Report back. Reference: ccbot `src/ccbot/markdown_v2.py` (telegramify usage + `convert_markdown_tables`), `chela/telegram/format.py`, `chela/telegram/relay.py` (send + plain fallback), `pyproject.toml` `[telegram]` extra.

## Open — orchestrator awareness

- [x] **Dashboard toast on dispatcher run-state transitions + `chela dispatch-runs --awaiting` summary.** So the orchestrator/human learns a run needs review without polling. Two small parts. ⚠️ **BEFORE implementing, open `TODO.md` in your worktree and read items 1–4 + Landmines + Verify beneath this line — they carry the load-bearing constraints (edge-trigger, reuse existing SSE + toast + runs-query, the no-auto-push scope guard). Do not implement from this summary line alone.**
  1. **Toast (frontend, EDGE-TRIGGERED):** in the dashboard, when a dispatcher run transitions **→ `awaiting_review`** (secondarily → `done` / → `failed`), pop a toast. **Reuse existing pieces:** the SSE `/api/events` stream that already keeps the Kanban reactive (`chela/dashboard/app.py:~2573`) and the existing toast styling (`.kanban-toast`, `chela/dashboard/static/style.css:1506`; cf. `.kanban-merge-toast`/`.kanban-promote-toast`). The frontend already consumes run/dispatcher state; track the **previous per-run status** and fire the toast **only on the transition** (NOT every ~1s poll while it sits in `awaiting_review`). Text e.g. `🔍 CMX-20 → awaiting review` with the **PR link** when present. Hang a mute toggle off the existing **"Notifications"** overflow-menu entry (persist in localStorage like other dashboard prefs).
  2. **Backend (only if needed):** if `/api/events` doesn't already carry per-run `status` + `pr_url`, add them to the named frame it emits on change (the REST `/api/dispatcher` already exposes them — mirror that source; do NOT add a new poll).
  3. **CLI summary:** add `chela dispatch-runs --awaiting` (or `--status <state>`) — a concise filter listing runs in `awaiting_review` with task id, PR URL, and age, so the orchestrator answers "what's up for review?" instantly on demand. Extend the existing command (`chela/main.py` `cmd_dispatch_runs` + the run listing in `chela/dispatcher.py`); reuse the existing query, don't add a second source of truth.
  4. **Scope guard:** this is the **pull/awareness** feature ONLY — do NOT auto-`send_keys` the event into any agent session (autonomous push into the orchestrator is a separate, deliberate decision — the future "decisions inbox"). Toasts are for the dashboard viewer; the CLI is on-demand.
  **Landmines:** toast MUST be edge-triggered (transition only) or it spams every poll; don't regress the existing Kanban toasts (promote/merge) or the SSE stream; keep the Notifications mute working; `--awaiting` reuses the existing runs query. Keep `uv run ruff check chela tests` green + `uv run pytest -q`. **Reference:** `chela/dashboard/app.py` (`/api/events` SSE ~2573, `/api/dispatcher`), `chela/dashboard/static/style.css:1506` (`.kanban-toast`), the dashboard frontend JS that renders the Kanban + consumes `/api/events`, `chela/dispatcher.py` (run statuses: claimed/running/awaiting_review/done/failed), `chela/main.py` (`cmd_dispatch_runs`). **Verify:** (a) a run entering `awaiting_review` pops exactly ONE toast (not one per poll) with the PR link; (b) muting via Notifications suppresses it; (c) `chela dispatch-runs --awaiting` lists only awaiting-review runs with PR URLs. Unit-test the CLI filter + the edge-trigger (prev vs new status) logic; the toast DOM may need a light JS test or manual check.

## Backlog (not yet dispatchable)

- **Settings view — editable toggles (follow-up)**: in-UI write-back + daemon restart.
- **Retire ccbot** ~07-19 after warm standby: `pm2 delete ccbot` + `pm2 save` + archive repo (ops).
- `/kill` (explicit agent-kill + close topic) — optional, higher-stakes; `/unbind` `/history` `/usage` skipped (don't fit auto-topics).
- Privacy scrub (forum IDs, `CCBOT_PANE_FALLBACK`, abs paths → env) before public `main`.
- **Cost view** (cockpit): transcript tokens × price → $ per agent / run / fleet.
- **Unified graph viewer** (cockpit): memory `[[wikilinks]]` + fleet; renderer = **Sigma.js + Graphology (MIT, WebGL)**, clean-room (not a GitNexus fork — PolyForm-NC). Colorblind-safe.

## Done

- [x] **🎯 Decisions inbox** — agent/run events pushed into the orchestrator's session, GATED ON IDLE (never into a `busy` or — critically — a `waiting` pane, which would answer its permission prompt). Durable queue + watch store (`chela watch <wid> --note`); triggers = watched busy→idle / →waiting / died, plus run `awaiting_review` / `failed`. `CHELA_INBOX_ENABLED=false` to disable.

- [x] telegram-setup (#14) · scaffold+extra (#15) · dashboard workflow discovery (#16) · monitor port (#17) · outbound relay+CLI (#18).
- [x] Fold steps 1-3 — paste-submit (#19), INBOUND (#21), monitor resolve-by-record (#22) [CMX-6/8/9].
- [x] Mobile Kanban — Swipe + Rows (#20, CMX-7).
- [x] Multi-topic Slice A registry (#23) + Slice B auto-create/lifecycle (#24) [CMX-10/11].
- [x] Topic naming by project cwd (#25, CMX-12).
- [x] `CHELA_SHOW_TOOL_CALLS` toggle — hide tool spam (#26, CMX-13).
- [x] Settings view — Connections & Status (#27) + Telegram-bridge row (#28) + Work-dispatcher-badge fix [CMX-14/15].
- [x] Bridge commands `/screenshot` (PNG) + `/esc` + `/` menu (#29, CMX-16) + `/screenshot` control-key keyboard + Refresh (#30, CMX-17).
- [x] **CUTOVER LIVE 2026-07-12** — `chela-telegram` (pm2 id 12) on @chelamuxbot / test forum ↔ 4 agents; tool-spam hidden.
