{
  "experiments": [
    {
      "guard": "the ELLIPSIS gate on every row past the first: a settled/past-tense summary found only by scanning past a banner must be rejected 'at any distance', not just at the farthest lookback row. Both fixtures that exercise it (the stale-spinner pane and the settled-behind-a-banner pane) put the settled line at exactly chrome_idx-4; chrome_idx-2 and chrome_idx-3 are reachable by the widened scan and have NO ellipsis-gate coverage. This mutation enforces the gate only at chrome_idx-4 and accepts a settled line unconditionally at every nearer row.",
      "file": "chela/telegram/panescan.py",
      "before": "            if is_first or _STATUS_ACTIVE in candidate:",
      "after": "            if is_first or i > chrome_idx - 4 or _STATUS_ACTIVE in candidate:",
      "kind": "mutation"
    },
    {
      "guard": "the COLUMN-0 rule on the row immediately above the chrome (chrome_idx-1) — the position BULLETS_PANE's own comment claims to test ('here one is the very last body line — directly above the chrome') but does not: a blank spacer sits at chrome_idx-1, so its indented bullet is actually at chrome_idx-2. No fixture in the file puts an indented '· ' bullet at chrome_idx-1, and that row takes the is_first path, which accepts active-or-settled unconditionally — so relaxing column-0 there relays a prose bullet as a live status, the exact false positive the whole anchoring exists to prevent.",
      "file": "chela/telegram/panescan.py",
      "before": "        if line[0] in STATUS_SPINNERS:",
      "after": "        if (line.strip()[0] if i == chrome_idx - 1 else line[0]) in STATUS_SPINNERS:",
      "kind": "mutation"
    }
  ]
}
