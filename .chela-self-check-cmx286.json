{
  "experiments": [
    {
      "guard": "test_a_malformed_state_file_is_no_daemon_at_all",
      "file": "chela/capabilities.py",
      "before": "    if not isinstance(caps, list):\n        return None",
      "after": "    if not isinstance(caps, list):\n        pass"
    },
    {
      "guard": "test_a_malformed_row_degrades_in_place_not_the_whole_file",
      "file": "chela/capabilities.py",
      "before": "    for cap in caps:\n        if isinstance(cap, dict) and cap.get(\"live_reload\"):\n            if fresh_by_key is None:\n                fresh_by_key = {c.key: c.as_dict() for c in effective()}\n            cap = fresh_by_key.get(cap.get(\"key\"), cap)\n        reconciled.append(cap)\n    return {**data, \"capabilities\": reconciled}",
      "after": "    for cap in caps:\n        if isinstance(cap, dict) and cap.get(\"live_reload\"):\n            if fresh_by_key is None:\n                fresh_by_key = {c.key: c.as_dict() for c in effective()}\n            cap = fresh_by_key.get(cap.get(\"key\"), cap)\n        if not isinstance(cap, dict):\n            return None\n        reconciled.append(cap)\n    return {**data, \"capabilities\": reconciled}"
    }
  ]
}
