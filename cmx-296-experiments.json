{
  "experiments": [
    {
      "guard": "chela.sessions.resolve_window promotes a tier-1 (event log) resolution into the durable chela.sessionids pin (CMX-296)",
      "file": "chela/sessions.py",
      "before": "    if sid:\n        path = transcript_for_session(sid, base)\n        if path is not None:\n            _promote(wid, sid)\n            return Resolution(wid, sid, path, \"event_log\",\n                              f\"the event log's newest session for {wid}\")",
      "after": "    if sid:\n        path = transcript_for_session(sid, base)\n        if path is not None:\n            pass\n            return Resolution(wid, sid, path, \"event_log\",\n                              f\"the event log's newest session for {wid}\")"
    },
    {
      "guard": "chela.sessions.resolve_window promotes a tier-2 (--resume cmdline) resolution into the durable chela.sessionids pin (CMX-296)",
      "file": "chela/sessions.py",
      "before": "    if pane and pane.resumed:\n        path = transcript_for_session(pane.resumed, base)\n        if path is not None:\n            _promote(wid, pane.resumed)\n            return Resolution(wid, pane.resumed, path, \"cmdline\",\n                              f\"`claude --resume {pane.resumed}` in the pane\")",
      "after": "    if pane and pane.resumed:\n        path = transcript_for_session(pane.resumed, base)\n        if path is not None:\n            pass\n            return Resolution(wid, pane.resumed, path, \"cmdline\",\n                              f\"`claude --resume {pane.resumed}` in the pane\")"
    },
    {
      "guard": "chela.sessions._promote is best-effort — a store-write failure must never fail the resolution it rides along with (same contract as chela.spawn._record_session_id)",
      "file": "chela/sessions.py",
      "before": "    try:\n        if sessionids.session_id_for(wid) == session_id:\n            return\n        sessionids.set_session_id(wid, session_id)\n    except Exception:  # noqa: BLE001 — a promotion failure must never fail resolution itself\n        log.warning(\"sessions: failed to promote resolved session id for %s\", wid,\n                    exc_info=True)",
      "after": "    if sessionids.session_id_for(wid) == session_id:\n        return\n    sessionids.set_session_id(wid, session_id)"
    },
    {
      "guard": "chela.sessions._promote skips the write when the pin already agrees, so a caller that resolves the same window on every tick does not turn into a steady stream of file writes",
      "file": "chela/sessions.py",
      "before": "    try:\n        if sessionids.session_id_for(wid) == session_id:\n            return\n        sessionids.set_session_id(wid, session_id)",
      "after": "    try:\n        if False:\n            return\n        sessionids.set_session_id(wid, session_id)"
    }
  ]
}
